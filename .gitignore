# --- Node / 依赖 ---
node_modules/
.npm-cache/
.pnpm-store/
pnpm-lock.yaml

# --- 构建产物 ---
dist/
build/
out/
*.tgz
*.tar.gz

# --- 本地测试 / 会话产物 ---
test-output/
tests/output/
browser-screenshots/
pastes/
.tmp/
.dshhome*/
*.log
.pw-browsers/

# --- Linux 侧本机测试环境（含本机绝对路径，永不入库；2026-09-14 实测补入）---
#   .env.sh 内是 /home/<user>/... 绝对路径；.venv-markitdown 为 markitdown 对比用 Python venv
#   （约 340MB 第三方包）；.home/.debs 为免 root 方案的假 HOME 与解包系统库；_cmp 为临时对比目录
.env.sh
.env.sh.bak-*
.home/
.debs/
.venv-markitdown/
_cmp/

# --- AgentTeams 团队运行时状态（含 inbox 会话数据，不入库）---
.agent-teams/

# --- 私人/策略文档（永不入库：毕设策略/导师沟通/商业化攻略）---
.私档/

# --- 软著/商业化材料与脚本存档（红线 9：永不入库；2026-09-08 第六轮 §1.1）---
docs/copyright/
tools/gen-copyright.mjs
tools/_verify-clean.mjs
tools/_pdf-pages.mjs
.script-archive/

# --- 系统 / 编辑器 ---
Thumbs.db
.DS_Store
.idea/
.vscode/

# tesseract.js node 版缓存残留（写在 process.cwd() 而非 langPath；tools/verify-ocr.mjs 与一次性台架脚本
# 中断/漏清理时会留在仓库根目录）
# 2026-09-11 用户拍板：先忽略，verify:ocr 通过后再删除该文件
# 2026-09-15 补 chi_sim —— 当天真机 OCR 台架脚本漏清理，实产 2,471,033 B 缓存（未跟踪，但会被 git add -A 带走）
# 锚定到根目录，避免误伤 langs/ 下的正式资产（两者均已入库，ignore 规则对已跟踪文件无影响）
/eng.traineddata
/chi_sim.traineddata
