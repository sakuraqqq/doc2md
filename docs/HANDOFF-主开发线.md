# HANDOFF · doc2md 主开发线（2026-09-11 更新）

> 交接对象：新会话（AI 助手）· 项目：doc2md（纯前端离线文档转 Markdown）
> 基线：**以现场实测为准（本文不缓存 HEAD / 远端 SHA —— 见下「易变量规则」）** · 📌 **现值快照（2026-09-20，卡 001-C 起）**：已发布 **v0.1.9**（tag `v0.1.9`）· 线上 = main 构建（**≠ 发布物**，原因已登记）· 契约分两口径：**本机带夹具 `290 / 288 / 0 / 2`**、**CI 干净检出 `287 / 284 / 0 / 3`**（卡 006 后**两口径各 +7** = 新增组 I2；组 Y 在有夹具环境三绿 ⇒ **0 fail**；两个口径**不可混用**）—— **以上数字的唯一真相源 = `docs/BASELINE.json`**（发布物 / 线上 / 交付面由 `tools/baseline-check.mjs` 按 git blob 与磁盘**现算比对**，漂移即 CI 红）；**本节不再缓存具体哈希与文件数** · **原记（保留作历史，勿再引用）**：v0.1.7 已发布、已推送（tag `v0.1.7` = `9d2cd9d`） · 契约：**254/254**（全量 256 tests / 254 pass / 0 fail / 2 skip，skip = real-cid-paper 第三方样例不入库；历史链：… → v0.1.5 组 R R3 后 231/231 → v0.1.6 组 W 后 238/238 → v0.1.7 组 X 后 244/244 → S4 判据门批后 251/251 → v0.1.8（含 S4-15/16 = U2 闭环）后 253/253 → **2026-09-16 P1 批（组 U9）后 254/254**） · 门禁全绿（lint 0/0 · metrics 文件 17 / 函数 **361** / 超限 0 / **重复率 0.5%** · PWA 48/48 · OCR 93% PASS · 产物 `index.html` **121,406 B** / **`18EA70E4…79EB3`（main 现值 = **依赖 PR 落地批**：清 2 处无用初值 + 工具链升 eslint 10 + CI action 升级，**未发布**；线上 v0.1.9 = 121,229 B / `F80E8626…F04D`（页脚 `v0.1.9`）；重构批（行为零变更）= 121,413 B / `3033E65C…1BA4B`；P1 修复态（未单独发版）= 121,229 B / `9EA390B9…0A11`；v0.1.8 发布版本 = 120,987 B / `14773E5F…F3F178`、S4 批 = 120,987 B / `4A15BA51…`、v0.1.7 = 120,988 B / `2EE82D47…` —— **页脚串等长 ⇒ 字节数不变、哈希必变，核对一律以 SHA256 为准**）**） · **v0.1.6/v0.1.7（2026-09-15）**：OCR 输入质量（显式 PSM 3 / 大图预缩放 / 版面质量信号）+ **方向重试**（质量差 → 顺时针 90° 重试一次，取更优）· **独立验收已通过（Linux 侧六项全绿；负对照 0% → 101%）**（真 chromium + 真 OCR 端到端；**v0.1.6 那份交接单的产物哈希已被 v0.1.7 取代**，见 §3 G） · 工作区：与版本库同目录
> ⚠️ **易变量规则（2026-09-15 新增）**：本文**不缓存** `HEAD` / `origin/main` / 「待推送 N」这类**提交即失效**的值 —— 基线一律现场实测（`git rev-parse HEAD`、`git ls-remote origin refs/heads/main`）；确需记录时只写**时点值**并显式标注。**稳定值（tag SHA 等）照常缓存。** 依据 = `docs/DEV-NOTES.md` 2026-09-15「收尾批」坑 ①：本文件自身曾在新增提交里写「HEAD = `e573713`」，而该行**在写入瞬间即过期**（其父提交已改变 HEAD）。
> ⚠️ **产物哈希勘误（2026-09-15 实测）**：本文原记 `087CDB12…BF14B` —— 实为**提交 C（`4344293`）时点**产物；版本 bump 提交改了产物但**字节数恰好不变**（同 118,244 B），故尺寸核对未暴露。现值以 `62B8ADA1…786D` 为准（= 入库 blob = Linux 构建 = Windows 构建）。详见 §3 F.3。
> ✅ **发布（最新）：v0.1.9 已发布（2026-09-16 12:59:40 +08；用户网页发布）** —— tag **`v0.1.9` = `cc504dadd22f632607b761e87825004784f6731e`**（lightweight；本地 `git rev-parse` ↔ 远端 `ls-remote` ↔ Release 页 tag 对象**三端逐字一致**）· Release <https://github.com/sakuraqqq/doc2md/releases/tag/v0.1.9>（**Latest**，附件 0；正文 = `docs/release-notes-v0.1.9.md` 全文，**CRLF 归一后逐字相等**）· **Pages 已复验**：`index.html` = **121,229 B / `F80E862639708D7C27CCE6C1A3E388298E8ECF01536B6B0F912539BBBE74F04D`**（与发布产物**逐字节一致**，页脚 `doc2md v0.1.9`）· **待推送 0** · 版本 bump `0.1.8 → 0.1.9`。修复链：`62f3f84`（先红：`sample-scale-td.pdf` + 组 U **U9**）→ `9b6e9c7`（实现：**PDF 文本空间位移/字号乘 `Tm` 缩放**）→ 独立验收通过 → 本版发布准备。修的是**手机侧真机实测的 P1**（WPS 导出 PDF「字符全对、顺序错乱、零警告」）。等价性台：既有正常 PDF **16/17 逐字节不变**（唯一例外 `real-cid-paper.pdf` 经用户 2026-09-16 拍板接受 —— 同族受影响、**老输出本就交错**，新输出分行正确且 C2 判据两版均通过）。**独立验收（Linux 侧，2026-09-16）五项全绿** —— 真 chromium：契约 256/254/0/2 + 组 U 11/11；**U9 负对照必红**（组 U 11/9/2，乱码串与本侧逐字相同）+ 还原后 11/11；**等价性 A 组 10/10 逐字节相同**（`相同 10 · 变化 1 · 违规 0`）；**V-4 渲染基准**独立确认期望值非自证；**V-5 换数据**（自造 `Tm 0.03/0.045` + 整词 `Tj` 的 PDF）旧产物三行拍平 → 新产物三行正确；Windows 侧回箱核验 **34/34 项字节 + SHA 相符**。⚠️ Linux 侧计划外发现：**负对照态产物 ≡ 线上 v0.1.8 发布产物** ⇒ **P1 缺陷在线上 v0.1.8 真实存在**（用户据此拍板发 v0.1.9）。本版门禁：契约 **256/254/0/2** · lint 0 · metrics 17/**361**/超限 0/**0.5%** · PWA 48/48 · OCR PASS(93%) · **隐私 & 版权审查：通过**（落盘 `docs/RELEASE.md` v0.1.9 节）· 观察期 **2026-09-16 起 → 复盘 ≥2026-09-19**。
> ℹ️ **main 领先线上：行为零变更重构批（2026-09-16）** —— `refactor(pdf)`：把**文本空间 → 用户空间**的缩放折算抽成单一来源（`argAt` / `tdx` / `tdy` / `moveLine` / `newTextState`，只动 `src/pdf.js`，≤50 行配额内）；**三层证据**：函数级台 0 差异 · 函数级**负对照 2 差异**（证明台敏感）· **产物级 19/19 逐字节相同**（真产物 + 真 `convert()`）。产物 = **121,413 B / `3033E65C…1BA4B`**。**线上 v0.1.9 不含此重构**（行为零变更，用户无感）。另如实登记：**重构配额（童子军规则）自 `29f02b5`（2026-09-11）起断档 19 批**（硬门禁未破，属软纪律漏做），本批为补齐第 1 批；机制化自查待用户拍板。
> ✅ **Pages 部署口径已改（2026-09-16 用户拍板 ②，契约组 H15 守卫）** —— 起因：push main 触发自动部署，线上产物一度变成 main 构建（**121,406 B / `18EA70E4…79EB3`**，页脚仍 `v0.1.9`）**≠ v0.1.9 发布物**（121,229 B / `F80E8626…F04D`）；行为零变更已由两台证明（函数级 0 差异 + 产物级 19/19 逐字节相同）。**处置 = `deploy-pages.yml` 改为只在 tag 推送时部署**（`on.push.tags: ['v*']` + `workflow_dispatch`），**push main 不再上线** ⇒「线上 == 发布物」恢复为硬不变量（H15 禁 `branches:` 触发，已做负对照必红）。⚠️ **线上当前仍是 main 构建**（121,406 B / `18EA70E4…`，页脚 `v0.1.9`）—— ✅ **用户 2026-09-17 拍板：接受现状，等下次发版（推新 tag）自然覆盖**（不手动 dispatch；该批行为零变更、用户无感）。**影响**：观察期内的线上实测请以 **`18EA70E4…`** 为基准，不要拿 `F80E8626…`（发布物）去比。
> ℹ️ **依赖 PR 已落地（2026-09-16，用户拍板路线 A：本地统一落地）** —— Dependabot 连开 6 个 PR（#2 checkout v4→**v7** / #3 setup-node v4→**v7** / #4 configure-pages v5→**v6** / #5 deploy-pages v4→**v5** / #6 upload-pages-artifact v3→**v5** / #7 工具链含 **eslint 9→10**）→ **本地三个提交落地**：`e0bd1de`（清 eslint 10 新规则 `no-useless-assignment` 抓到的 2 处无用初值，行为零变更：函数级台 0 差异 + 产物级 **19/19 逐字节相同**）· `5cc1324`（eslint **10.10.0** / @eslint/js **10.0.1** / @playwright/test **1.63.0** / jscpd **5.2.1**）· `9b3620f`（5 个 action，tag 均经 `git ls-remote` 核实）。action tag：checkout `v7=3d3c42e5…` · setup-node `v7=82076278…` · configure-pages `v6=45bfe019…` · deploy-pages `v5=368f8252…` · upload-pages-artifact `v5=fc324d35…`。**处理口径（写死）**：工具链 PR 先本地实测再落提交；运行时库恒 ignore；同 workflow 的多个 action PR 不逐个 merge（互撞文件）→ 本地一批 + 一次 push，PR 由 Dependabot 巡检自动关。C 项（`tar` CRITICAL，非交付面）用户拍板 **接受现状（wontfix）**，复查触发 = pdfjs-dist 大版本升级。
> ℹ️ **依赖更新已制度化（2026-09-16，防屎山 ⑤；用户拍板 A+B+C）** —— 新增 `tools/audit-delivery.mjs`（**交付面**审计门禁：只看 `vendor/`+`langs/` 用到的运行时依赖，未豁免 high/critical 即 red；豁免须在脚本 `ALLOWLIST` 写理由 + 日期）+ `.github/dependabot.yml`（工具链 weekly 自动 PR；**运行时四件套 ignore**，升级须人工立批）+ CI 在 lint 后插步。契约组 H 续号 **H13**（`isEvalSupported: false` = **CVE-2024-4367 缓解守卫**，`pdfjs-dist 3.11.174` 落在受影响区间但该配置下不可触发）/ **H14**（cmaps 同源）。契约 **258 / 256 pass / 0 fail / 2 skip** · lint 0 · metrics 18 文件 / **384 函数** / 超限 0 / 0.4%。⚠️ 遗留（待拍板）：`npm audit fix` 实测**零改动**（tar@6.2.1 修复版只在 7.x，`@mapbox/node-pre-gyp` 声明 `^6` ⇒ semver 内无解），三个处置见 `docs/DEV-NOTES.md` 本批。
> 发布（上一版）：**v0.1.8 已发布（2026-09-15 20:30 +08；用户网页发布）** —— 修复 S4 编码判据门三类静默错（**线上影响已解除**）；tag **`v0.1.8` = `b3a5387e54179d2d06e96b900c06d5273b3deba0`**（lightweight；本地 `show-ref` ↔ 远端 `ls-remote` ↔ Release 页 tag 对象**三端逐字一致**）· Release <https://github.com/sakuraqqq/doc2md/releases/tag/v0.1.8>（**Latest**，附件 0；正文 = `docs/release-notes-v0.1.8.md` 全文，仅行尾 CRLF 差异）· **Pages 已复验**：`index.html` = **120,987 B / `14773E5F…F3F178`**（与发布产物**逐字节一致**，页脚 `v0.1.8`）· 契约 **255/253/0/2** · lint 0 · metrics 17/**361**/超限 0 · PWA 48/48 · OCR PASS(93%) · **隐私 & 版权审查：通过**（落盘 `docs/RELEASE.md` v0.1.8 节）· 观察期 **2026-09-15 起 → 复盘 ≥2026-09-18**。
> 发布（上一版）：**v0.1.7 已发布（2026-09-15；用户网页发布）** —— tag **`v0.1.7` = `9d2cd9d0cec9b1a0a6e57365116b4d367d66e142`**（lightweight；本地 `git rev-parse` ↔ Release 页 commit 链接**逐字一致**）· Release <https://github.com/sakuraqqq/doc2md/releases/tag/v0.1.7>（**Latest**，附件 0）· **Pages 已复验**：`index.html` = `120,988 B / 2EE82D47…1CBB`（与发布产物逐字节一致，页脚 `v0.1.7`）—— 本版**合并 v0.1.5 + v0.1.6 + v0.1.7 三批**（版本号 0.1.4 → **0.1.7**，跳过从未单独发版的 0.1.5/0.1.6）；契约 **246/244/0/2** · lint 0 · metrics 17/**361**/超限 0 · PWA 48/48 · OCR PASS(93%)；**独立验收（Linux 侧）六项全绿**（负对照 0% → 101% 因果锁定）+ **用户级验收通过**（4 张真机照片全可读）；**隐私 & 版权审查：通过**（落盘于 `docs/RELEASE.md` v0.1.7 节）· 观察期 2026-09-15 起 → 复盘 **≥2026-09-18** · 上一版 **v0.1.4**（tag `cfb20b76…`，2026-09-14；观察期未走完即被本版取代，如实登记）· 更早 **v0.1.3 已转正**（tag `d6929e6`，2026-09-12）· 详见 `docs/RELEASE.md` v0.1.7 节
> ⚠️ **线上 v0.1.7 已知受影响（2026-09-15 独立验收附带发现 F-1）**：`src/sniff.js` 的 S4 门在 v0.1.7 中为旧阈值 `2/10`，与 Linux 侧负对照态 A **逐字节等价**（产物 `120,988 B / 2EE82D47…1CBB`）⇒ **C4/C10/C13 三类静默错（小 nonAscii 基数下小文件被 gb18030 整篇改写）在线上真实存在**；修复（`3/4`）已在 main（`14d8544`）· **用户 2026-09-15 拍板发 v0.1.8**（发布准备就绪，见上方「最新」行）—— ✅ **v0.1.8 已于 2026-09-15 20:30 上线，线上影响解除**（Pages 复验 `120,987 B / 14773E5F…` 与发布产物逐字节一致）。
> ⚠️ **tag SHA 勘误（2026-09-14 Linux 侧实测）**：本文原记 tag = `5683b05`，与**双端实测不符**——本地 `git show-ref --tags` 与远端 `git ls-remote --tags` 均为 **`d6929e6`**；`5683b05` 实为 tag 前一个提交（"feat(ci): 部署白名单 smoke"）。差异成因本次未取证（新克隆无 reflog），**以实测值 `d6929e6` 为准**。
> 开场白模板：`项目：doc2md 主开发线 · 基线 <HEAD SHA> · 先读本文件 + AGENTS.md + docs/RELEASE.md`
> **权威源声明**：本文件 = 状态性内容的**唯一权威源**；会话级交接（如 `.私档/HANDOFF-*.md`，gitignored）只是临时补充，**收尾时必须回灌本文件**（基线 / backlog / 门禁链），不得长期并行两套状态。

---

## 1. 现状一句话

> ⚠️ **2026-09-19 更正（调度线门卫核对发现）**：本节标题为「现状一句话」，但正文停在 **v0.1.3（2026-09-12）**；**现值 = v0.1.9**（见本文顶部「发布（最新）」行）。**以下正文保留作历史记录，不再作为"现状"引用。**

**已发布 v0.1.3**（tag `v0.1.3` = `d6929e6`，2026-09-12；**2026-09-14 双端实测勘误，原记 `5683b05` 有误**）。v0.1.2（`f5aed38`）之后 main 继续前进到 **`b29770b`**（第七轮批 + v0.1.3 首提交准备 + 真实样例批 OCR 中文空格合并 + **减脂批：metrics 超限 23 → 0** + **公开仓库合规清扫** + **规范符合性 S2 删除线 / S3 表格列位置** + **metrics 假绿修复与口径定案** + **embed-bline 护栏 / 隐患处置落盘** + **文档漂移对齐（权威源 / 数字回填）** + **`.gitignore` 收口** + **规范符合性 S4 编码全篇判定 / S5 `w:dstrike` 归一** + **收口批：S4 口径 A′ 结构判据门 / S5 单正则交替加固（用户拍板「修完再发」）**），**不重打 tag**（用户拍板）。**2026-09-11/12 两批两相（本会话升权实跑）**：S4/S5 批先红 **168/6** → 后绿 **174/174**；收口批先红 **26 tests / 21 pass / 5 fail** → 后绿 **179/179 pass / 0 fail（46.0s）**、PWA **48/48**、OCR **93% PASS**、build + `git diff --exit-code index.html` **exit 0**、`eslint src/**` **0w/0e**、metrics **文件 16 / 函数 323 / 超限 0**；**重复率 0.6%**（口径 = **只度量 `src/` + `tools/`，`tests/` 排除**，2026-09-10 用户拍板；原记 4% 系 metrics 读旧报告的假绿，工具已修）。**远端同步状态（2026-09-14 复核）**：`origin/main` = `89c7dca`，**未推 0**（原记「落后 5 个提交」已闭结，此后 `f7caa21` / `45b2bec` / `efed461` / `0b7b309` / `89c7dca` 均已推送）。期间经历一次**手机 gitsync 事故**：`567ff2f` 清零 6 个二进制（4 个 PWA 图标 + 2 个测试样例 PNG）→ CI 双 failure 拦住 → `faceb9f` revert 恢复 → 双 success，**线上未受影响**（详见 `docs/DEV-NOTES.md` 2026-09-14 节）。

**v0.1.4 缺陷批（2026-09-14，三提交，均未打 tag）**：真机 7 篇 Chromium 打印 PDF 暴露四类**静默错**（`warnings=[]`、console error 0）——整篇行序反向 / 代码清单无围栏 / `#` 注释裸露成 H1 / 页脚同位置 3–4 倍叠印；经「真机 + 手机端找茬 + Linux 侧跨平台」三方合并清单 + node 侧 pdfjs 算子级取证后修复。

- **提交 A `dd960b1`**（批 1+2，**已推送**）：A1 翻转 Tm 行序（按 Tm 的 d 符号统一屏幕方向）· A2 补 `TL`(36)（leading 存规范值 + `nextLine` 随 d 定向）· A3 等宽代码围栏 · A4 同位置叠印去重；**支撑性修复**：`Td/TD/T*` 平移的是**行矩阵**而非当前笔位（A4 判据成立的前提）。
- **提交 B `fc6cfa8`**（批 3，**已推送**）：B1 `BM*`/`GIF8*` 前缀误判（补结构校验）· B2 `%PDF` 文本误判（版本形态 + `obj`/`%%EOF` 证据）· B3 大写 `META` 漏检（大小写不敏感）· C1 docx alt 取 `descr`；另含 **T3 两项**（脏数字实体三处登记闭环 + C1 映射表 27 项参数化断言）。
- **提交 C `4344293`**（F1 修复，**待推送**；qa-dev 在 t6 独立验收中判「high / 实现级」不通过）：A3 等宽判据从**按页**提升到**全篇**（宽度判据与 `MONO_MIN_LETTERS` 零改动）—— 真实 7 篇栏外 `#` **松 40 → 0 / 严 13 → 0**、围栏块 163 → 124；补契约组 **U8** 回归守护（**先红后绿已验证**：暂存修复后 ✖ 且复现裸露原文 → 恢复后 ✔）。
- 契约新增**组 U**（U-0 字节锁 + U1–U8）与**组 V**（V0–V6，共 13 项）；file:// 换环境 16/16 PASS、console errors = []。
- **未修残余（v0.2 候选）**：① 极端「逐字符一字体子集」PDF 标题行误判（说明书类样张 **10 页 12 个单行块** —— 早先转述的「6 个单行围栏」是**限前 5 页**所测，qa-dev 在 t10 复验时更正）；② 3 条超冻结样例集的对抗假阳性（`%PDF` + 正文含 `obj`/`%%EOF` 裸子串；`GIF89a` 开头且末尾恰为 `;`）——建议收紧为 `\d+ \d+ obj` 形态与 GIF trailer 块完整性校验；③ 第一遍不 `cleanup` 使大文档内存峰值高于旧行为（换取第二遍 `getOperatorList` 开销 ≈ 0，实测 0 ms vs 216/154 ms）。
- **t10 复验（qa-dev，2026-09-14，结论：通过）**：独立复现「真实 7 篇 163→124 / 松 40→0 / 严 13→0」；产品路径 doc 05 = 11 块 / 0 栏外 # / 523ms，与离线逐项一致；**扩大语料 20 文档无一篇劣于提交 B**；兼容路径（不传 `monoFonts`）与提交 B **逐字节一致 20/20**；U8 先红后绿以 blob pin 双版本独立复现（非恒绿空断言）；manifest **36/36** 字节锁一致。两点措辞更正：U8 注释「两页同栏」偏松——两页因分页注释**各成独立围栏块**（实测 2 块），断言不受影响；性能 +7%（523ms vs 488ms）与两遍设计 + 算子列表缓存自洽、无翻倍。
- **发布纪律更新**：推送前隐私/版权审查**先落盘再推送**（提交 A/B 因用户已先推送而成为事后追认，结论为**通过**；提交 C 起改为先审后推）。
- **环境记录更正**：§5 原记「本环境无全局 git 身份，一律 `-c` 覆盖」**已过时** —— 实测全局已配 `user.name=sakuraqqq` / `user.email=…@users.noreply.github.com`、`http.sslverify=false`、`safe.directory=*`。
- **手机同步工具退场（2026-09-14，用户拍板）**：用户已卸载 gitsync。纪律「手机侧只读拉取」**升级为「手机侧不同步本仓库」** —— 实测依据：`.git` 31 MB + 工作树 29.2 MB（`vendor/` 15.3 MB）+ **243 提交**，且历史被重写两次（9/10 合规清扫 + 9/14 隐私清洗）→ 手机端旧克隆与远端**无共同祖先**，此类工具（走 GitHub API 逐文件拉取）只能全量重拉，表现为「一直转圈」。替代：Pages 站点 / GitHub App / 桌面·Termux 浅克隆；**推送一律经桌面终端**。详见 `AGENTS.md`「本地命令约定」与 `docs/DEV-NOTES.md` 2026-09-14 事故节。

## 2. 已完成批次（累计，9/7 起；最近一批在最下）

| 批次 | 内容 | 关键提交 |
|---|---|---|
| v0.1.1 发布 | 106/106 + 48/48 + OCR 93%；tag + GitHub Release + 观察期闭环 | `3265b0c`（tag） |
| v0.1.2 批1 | docx 图片方案 A：阈值归 0 全抽取 + 导出二选一（zip 默认/单文件内嵌） | `ce57be3` |
| v0.1.2 批2 | .doc（OLE2）友好提示「另存为 .docx」 | `d584ff4` |
| 第五轮审查 A+B+C | 8 bug（sheet 映射错位/PDF 质量门/FFFD/zip 边界/OLE2 文案/backend/Cs/库路径裸异常）+ cmaps 许可 + 部署白名单 + 减脂 | `9148f4c`→`c3a8a10` |
| 第六轮审查 P1 | html2md 原文空白判定 + 列表块边界 + PRE 空行；xlsx numFmt 日期格式化 | `8a4722e` / `d19d565` |
| **§8.1 批（9/8 夜）** | §2.4 内嵌单遍 + 20MB 上限自动切 zip · 预览 1MB 截断 · §2.10 死文件清理 · §3 文档漂移 + 数字回填 | `7534990`→`2f38ad3` |
| **第七轮批（9/9）** | §2.2 xlsx rels Target `../` 归一化（先红实测 = 转换直接失败）· §2.4 BR 死字段清理 · §2.5/§2.6 留档 | `5b9bb72`→`fa65152` |
| **v0.1.2 发布（9/9）** | tag `v0.1.2` + GitHub Release + Pages #35；契约 145/145 · PWA 48/48 · OCR 93% · CI tests #25 绿 | `f5aed38`（tag） |
| **v0.1.3 首提交准备（9/9）** | footer `v0.1.1`→`v0.1.2` + 产物重建 + package-lock 版本同步 | `03e3a03` |
| **真实样例批（9/9）** | 用户实转 3 份课程 PDF（2 份文字层为空→OCR）→ 组 R **OCR 中文空格合并**（72-82%→1.4-4.9%）+ README 留档 | `1701efd`→`1abbe18` |
| **减脂批（9/10，AgentTeams doc2md-slim）** | 22 提交 / 19 函数出 OVER 清单；metrics 超限 **23 → 0**、eslint src **0w/0e**；等价性 6 台全 0 差异 + qa-dev 独立台 2912 点；契约 153/153 | `b9a8388`→`1a14b08` |
| **合规清扫（9/10）** | 第三方论文移出公开仓库（`.私档/`，B5/C2 改 skip + 提示）· 调研文档移出 · 未推历史重写为 3 个干净提交 · metrics 接 CI 硬门禁 · 新增规范符合性机制文档 | `1a14b08`→历史重写后 3 提交 |
| **规范符合性 S2（9/10）** | 契约组 S 先红 **6/6 失败** → html2md `strikeFrag`（`<s>/<del>/<strike>` + DOCX `w:strike` → `~~`）→ **159/159** 零回归；产物 109,076 B | `435af14`→`1a6c581`→`2b2a0eb` |
| **规范符合性 S3（9/10）** | 契约组 S 增 S3-1..S3-5 先红 **5/5 失败** → xlsx 按 `c@r` 归位 + html2md `colspan` 补空 → **165/165** 零回归；顺带修 metrics 重复率**假绿**（读旧报告 → N/A） | `3c78ba2`→`5dd4478`/`b9002da`→`c28fa5c`→`a58b067` |
1. ~~**本地陈旧产物陷阱**~~ ✅ **已闭环（2026-09-12 纳入 v0.1.3）**：新增契约组 **T**（现场重建产物比对字节，不一致即 FAIL；实测污染产物即红）——治「只改 `src/` 忘 `npm run build` → 本地对着旧产物假绿」
| **规范符合性 S4/S5（9/11）** | 组 S 续号 S4-1..S4-4（编码全篇判定）+ S5-1..S5-3（`w:dstrike` 归一）；官方先红 **168 pass / 6 fail** → 后绿 **174/174** 零回归；S5 重构抽可 import 纯函数（自检抓修 1 真缺陷） | `a53eaba` → `38e623f` / `162bcc1`→`29f02b5` → `885b548`（产物 111,449 B） |
| **S4/S5 收口批（9/11-12）** | 交叉审查 3 条 finding → 用户拍板「修完再发」：S4 口径 **A′ 结构判据门**（`fffd*10 ≥ nonAscii`）+ S5 **单正则交替**（替代两遍换序）；新断言 S4-5..S4-7/S5-4/S5-5 先红 **21/5** → 后绿 **179/179**；独立验收双产物 pin 回放 + 负对照 5 处红；t12/t13 交叉审查均 pass | `6a891a2`→`217459f`/`bad086a`→`f54d7a6`（产物 112,194 B） |
| **第八轮审查修复批（9/12）** | ZCode 报告 6 条新发现全部闭环：html2md 行内代码动态围栏 / 无文本语义元素移除（label 保留）/ li 告警透传 · xlsx 1904 基准 / 实体单遍解码 / 码点 / 科学计数法；另 §2.1 lint 基线 + §3.3 清理；先红 **24 fail**（K2 17 + G7 5 + 2 壳）→ 后绿 **212/212**；t5 独立验收 + t6 交叉审查均 pass | `4329c21`→`82cc702`/`54cfdf8`→`7854023`/`ce4c743`→`fc9efef`（产物 113,561 B） |
| 隐私清洗 | 全历史重写（5 个真实邮箱→noreply、本地路径脱敏、敏感文档移出）；Actions 旧 run 删除 | 历史重写 + `489b431` |
| **跨平台一致性验证（9/15）** | Linux 侧（WSL2 + 真 chromium）独立复跑：`npm ci` → `build` → 五项门禁，**产物逐字节相同、门禁逐项与 Windows 基线一致**（含 CRLF 工作区对照组）；**未改任何代码** | —（无代码提交；报告与 10 步原始日志在 `.私档/linux/`） |
| **v0.1.5 批（9/15）** | ① 图片直传 OCR 补 CJK 空格折叠（**真机验收驱动的真实缺陷**）+ 契约组 **R3** 先红后绿；② metrics 报告 POSIX 化（跨平台可重复）；③ `.gitattributes` 治检出 CRLF 陷阱（+7 文件归一化）；④ CONTRACT 组 U 建节 / 组 V 转绿；⑤ README 三条真机结论 | `3db5788` / `384308f` / `b47e1f2` / `1847b40` / `10930d9` · **独立验收四项全通过**（Linux 真 chromium + **真 OCR 中文图 + 负对照**） |

**纪律沉淀**（已写进 AGENTS.md / 全局 AGENTS.md）：
- 红线 9：私人/策略文档永不入公开仓库（`.私档/`，gitignore）
- 重构配额（童子军规则）：每次改动顺手 ≤50 行重构，修 bug 与重构分提交
- 全局第 11 条：隐私红线（noreply 提交身份、push 前自查）
- 本地命令：`git --no-pager`（否则 PowerShell 报 `nutc`）

## 3. 未完成 backlog（按优先级）

**A. 第六轮审查剩余**（报告：`docs/doc2md-第六轮审查报告-2026-09-08.md` §2/§3）
1. ~~§2.2 real-cid-paper 再分发~~ ✅ **已闭环（2026-09-10 合规清扫）**：文件移出公开仓库 → 本地 `.私档/`；B5/C2 改为「`tests/data/` → `.私档/` 解析，皆无则整组 skip + 提示」；未推历史改写为 3 个干净提交
2. §2.3 xlsx sheet XML 无解压护栏（sharedStrings 有 4MB，sheet 没有）
3. §2.6 PDF 扩展 B 区 CJK 代理对（`slice(-1)` UTF-16 边界）
4. §2.7 xlsx `<rPh>` 注音重复（collectTTexts 未排除）
5. ~~§2.9 metrics 未接 CI~~ ✅ **已闭环**（2026-09-10 拍板）：CI 新增 `npm run metrics` 硬门禁（**超限函数数必须为 0**）；`AGENTS.md` 基线改为实测（`eslint src/**` 0w / metrics 0 / 重复率 4%）；`eslint src/**` 已 0w/0e、metrics 超限 0
6. ~~§2.4 单文件内嵌导出 O(n²) + 无上限~~ ✅ **已闭环（§8.1 批，`70fb56a`）**——单遍替换 + 20MB 上限自动切 zip（拍板 T-7）
7. ~~§2.10 `patches/router-bootstrap.mjs` 死文件 + 预览全量灌 textarea~~ ✅ **已闭环**——死文件移入 `.私档/`（`9a53d15`）+ 预览 1MB 截断（`24e22bd`）
8. ~~§3 文档口径漂移~~ ✅ **已闭环（`fb0710b` + `2f38ad3`）**——template 徽标/注释 + SW v4 口径 + 体积数字回填 **102KB（104,064 B）** + RELEASE-CHECKLIST 补 CACHE_NAME 硬检查；**architecture §4.4 经实测回读已于 `d19d565` 同步，无残余**

**A2. 第七轮审查剩余**（报告：`docs/doc2md-第七轮审查报告-2026-09-09.md`；本批已闭环 2.2/2.4，2026-09-09）
1. **§2.1 PDF 多栏同行混排**（P2，真实场景收益最大）——需先拍板：算法口径（x 间隙 > 页宽/3 切栏 vs x 聚类）+ 「怎么先红」（仓库无双栏样例；候选 = 纯函数级 runs 数组断言）
2. **§2.3 自定义 formatCode 含字母误判日期**（P3，报告建议观察半年）——做则加引号段剥离 + 「`0.00 "sec"` 不判日期」断言
3. ~~§2.2 rels Target `../` 未归一化~~ ✅ 已闭环（`b95a4ce`；**先红实测 = 转换直接失败，非报告所称「仅降级」——见 CONTRACT.md 组 G6**）
4. ~~§2.4 BR 片段死字段~~ ✅ 已闭环（`8633ffb`）
5. §2.5/§2.6 已**留档**（README PDF 已知限制 + DEV-NOTES），不改代码；§2.7/§2.8 拍板不做（无害/风格）

**A3. 真实样例反馈（9/9，用户实转课程 PDF；样例含个人信息 → 永不入库）**
1. ~~OCR 中文词间空格（CJK 前置空格率 72–82%）~~ ✅ 已闭环（组 R，`68ef2a6`）——实测 72–82% → 1.4–4.9%，文字层页零影响
2. ~~README 提示（图片型 PDF 建议用保留文字层的导出方式）~~ ✅ 已留档
3. **低质页保图**（= 原「PDF 图纸页保图」backlog）：OCR 页误识/乱码多时输出整页图片引用——需拍板触发阈值（置信度/乱码率）与产物形式（assets + zip 联动），且需要可入库的合成样例

**A4. 格式规范符合性（9/10；机制与清单见 `docs/spec-conformance-tests.md`）**
1. ~~S2 删除线语义缺失~~ ✅ **已闭环（2026-09-10，`435af14` 先红 → `1a6c581` 实现 → `2b2a0eb` 产物）**：`<s>/<del>/<strike>` + DOCX `w:strike` 均产出 `~~…~~`（组 S 先红 6/6 失败 → 后绿 6/6；全量 **159/159**）
2. ~~S3 表格列位置错位~~ ✅ **已闭环（2026-09-10，`3c78ba2` 先红 → `5dd4478`+`b9002da` 实现 → `a58b067` 产物）**：XLSX 一律按 `c@r` 列号归位（稀疏补空 + 乱序归位，无 `r` 回落文档序）、HTML `colspan` = 内容放首列 + 其余补空（上限 100）；组 S S3 先红 5/5 失败 → 后绿 5/5，全量 **165/165**
3. ~~S4 编码探测窗口~~ ✅ **已闭环（2026-09-11 用户拍板口径 A）**：`decodeText` 改**全篇判定**（UTF-8 `fatal` 快检 + 全篇 U+FFFD 计数；`<meta charset>` 扫描窗口语义不动）——`a53eaba` 先红 → `38e623f` 实现 → `885b548` 产物；组 S S4 **4/4** 绿
4. ~~S5 OOXML `w:dstrike`~~ ✅ **已闭环（2026-09-11）**：docx 预处理归一（自闭合 `w:dstrike` → `w:strike`；`w:val=false/0/off` 原样保留；无 dstrike 零改写快路径）——`162bcc1` 初版 → `29f02b5` 纯函数重构（自检抓修 1 真缺陷）；组 S S5 **3/3** 绿
5. ~~S1 表格列数对齐~~ ✅ **已闭环（2026-09-18，`tests/contract_v1.test.mjs` 组 S **S1-1..S1-4**，纯加断言、零代码改动）**：实测**确为按最大列宽补齐**（XLSX 表头 3 列 + 数据行 1 列 → 短行补空；最宽行在中间 2/4/1 → 全表含分隔行均 4 列；HTML 不等长行同规则）+ **负对照「不截断」**（最宽行末列内容必须仍在）—— **4/4 绿**（见 A11.10）
6. **S2 残余（待拍板）**：CSS `text-decoration: line-through` 是否一并支持（≈+5 行）
7. ~~重复率口径（待拍板）~~ ✅ **已闭环（2026-09-10 用户拍板）**：口径改为**只度量 `src/` + `tools/`（`tests/` 排除）**→ 实测 **0.6%**（前口径含 tests：8.86% → 9.37%）；`tools/metrics.mjs` 的**假绿已修**（jscpd 运行前删旧报告、仅成功才读、否则 N/A；espree 解析失败 → exit 1）
8. ~~工具/流程「假结果」审计发现~~（2026-09-10；① 已闭环，②③ 转 v0.1.3 见下 E）：
   - ① ~~`tools/embed-bline.mjs` 无护栏~~ ✅ **已闭环（2026-09-10 用户拍板「加 --force 护栏」）**：默认拒绝执行并 exit 1，须显式 `node tools/embed-bline.mjs --force` 才可重新内联（实测 `run=1` 拒绝、`index.html` 零改动、lint 干净）；README 同步标注
   - ② `npm test` 跑的是**已构建的 index.html**：只改 `src/` 忘了 build 时，本地测试会对着**旧产物**全绿（CI 有 `build && git diff --exit-code index.html` 拦，本地无）→ **v0.1.3 再看**
   - ③ `deploy-pages.yml` 站点白名单用 `cp`，缺文件会失败（诚实），但**新增必需顶层文件时会静默漏发**→ **v0.1.3 再看**

**A5. 三方对比与真机反馈产出（2026-09-14；完整清单见 `.私档/项目/复盘/20260914-复盘材料-待修清单-合并三方.md`，决议见同目录 `20260914-复盘决议-v0.1.3转正与v0.1.4范围.md`）**
1. **v0.1.4 缺陷批（已拍板，两批提交）**：**提交 A** = 批 1（A1 翻转 Tm 行序 / A2 缺 `TL`(36) 算子）+ 批 2（A3 等宽代码围栏 / A4 叠印去重）；**提交 B** = 批 3（B1 `BM*`·`GIF8*` 前缀误判 / B2 `%PDF` 文本误判 / B3 大写 `META` 漏检 / C1 docx alt 取 `descr`）+ T3 两项（脏数字实体闭环登记 + C1 映射表 27 项参数化断言）。**每批跑门禁，绿了再进下一批。**
2. **S4 判据门已实施（2026-09-15 收尾批）且独立验收通过；T2/T4 仍排 v0.2**：候选⑤（`fffd>=5 && fffd*4>=nonAscii`）**经 17 例实测被否决**（会打破 F6 短 GBK）→ **实取 `fffd>=3 && fffd*4>=nonAscii`**（契约组 S **S4-8..S4-14**；先红 `33ed402` → 后绿 `14d8544`；详情见 `tests/CONTRACT.md` §7 2026-09-15 条）；**独立验收（Linux 侧，真 chromium）六项全绿**（双向负对照：切回 `2/10` → S4-11/12/13/14 必红；切到 `5/4` → **F6 必红** ⇒ 否决理由独立成立；`iconv` 真 GBK 3/3 全等；Windows 侧回箱核验 **25/25 项字节 + SHA256 相符**）；T2 1904 只补 warning、T4 `%` 收紧版并入（`+` 不纳入）**仍排 v0.2**。
3. ~~**C1/C2/C8 原始夹具缺失**~~ ✅ **已处置（2026-09-15 收尾批，用户拍板「页内现造」）**：① **原始字节不可得**（生成脚本在 `.tmp/qa-t12/`，已被清理；`.agent-teams/archive/doc2md-s4s5/` 只留配方）→ 按 t12 公布配方**重建**为组 S **S4-8/9/10**（守卫，页内现造、不入库样例）；② ⚠️ **勘误**：本条原记「**C8（8 B 真 GBK）**」有误 —— t12 原始输出为 **C8 = S4-5 型 10,796 B + 2 截断**，「8 B」是 F1/F2 的量级；③ 决议附加条件② 的复评已完成 → 候选⑤ **被实测否决**（见上条 A5.2）。
4. **PDF 段落边界**（PDF 三方对比 A 类发现：相邻视觉行单换行 → GFM 折叠成一段）→ **v0.2 待拍板**（口径候选：按行距/缩进/字号判定）。
5. **标题识别 H1–H4**（pdf-inspector 有字号比推断能力，我方暂无）→ **v0.2 候选**。
6. **公开语料基准**（我方目前只有样例级实测，无公开语料跑分口径）→ **v0.2 候选**（对外要数据时做）。
7. **U1 多文件「全部下载 .zip」/ U2 保存位置** → **U2 已移交 APK 线**（见 **A7.6-A1**；Web 端覆盖率过低，用户 2026-09-16 拍板）；**U1 成本已由 M 下调为 S**（复用现成 `downloadZip()` + fflate；实测痛点 = 手机多文件逐个下载要 N 次确认）→ ✅ **2026-09-16 用户拍板：排 v0.2**（手机侧 §3.5「复盘后第一批」与 §4「排 v0.2」冲突，取 §4；见 **A7-#18**）。

**A6. 手机侧产出包（2026-09-16 收到；落 `.私档/验证/手机找茬/20260916-手机侧产出/`，15 文件逐个字节 + SHA256 已核）**
1. ~~**P1 PDF 文本层顺序错乱**~~ ✅ **已修复并已发布（2026-09-16）**：`62f3f84`（先红：`sample-scale-td.pdf` + 组 U **U9**）→ `9b6e9c7`（实现：文本空间位移/字号乘 `Tm` 缩放）。根因与取证见 `docs/DEV-NOTES.md` 2026-09-16 节 + `.私档/项目/复盘/20260916-P1根因-电脑侧.md`；**独立验收（Linux 侧）五项全绿、已通过**；**已随 v0.1.9 发布**（tag `v0.1.9` = `cc504da`，2026-09-16 12:59 +08，线上回读 9/9 PASS）⇒ **线上已不含该缺陷**。
2. ~~**backlog v11 重排**（20 条 + APK 线 A1–A6）~~ ✅ **已复核并入（2026-09-16）** —— 逐条结论见下方 **A7** 节；手机侧那份（25,203 B，`backlog-优先级重排-20260915.md`，**v11**）是**便于传阅的副本**，**权威源 = 本文件 §3**。
3. **`AGENTS断言粒度.patch`**（手机侧称 `git apply --check` 通过）→ **待评审后应用**：先核 patch 是否**仅动 `AGENTS.md`**、是否与既有「断言即规格」条目冲突。
4. **`PDF版面结构还原线-立项与主线划界.md`**（已立项、**按用户拍板不启动**）· **`现成方案调研-20260916.md`**（内存/流式/Worker/中间存储 8 候选 + 许可已核）→ 归档备查。
5. ~~**竖排盲区（`3-实测物证-竖排盲区`）**~~ → ✅ **已并入 A7.2**（钉钉海报 —— 横排正文正确、**竖排被逐字拆散** + 另一组竖排整组丢失；根因 = tesseract 按横排理解版面、整页质量不差故不触发 v0.1.7 重试 → **待拍板**）。

**A7. 手机侧 backlog v11 并入（2026-09-16 逐条复核）**

> **来源**：`.私档/验证/手机找茬/20260916-手机侧产出/doc2md-手机侧产出-20260916/1-审查产出/backlog-优先级重排-20260915.md`（**v11**，20 条 + §4.1 APK 线 A1–A6；与 15 个交件同批收件，字节 + SHA256 已核）。
> **复核口径**：逐条比对本文件既有条目与仓库实测状态 → 标 4 类结论：**📌 已有条目**（不重复排）· **🆕 新登记** · **✅ 已闭环**（本批销账）· **➡️ 已移出主线**（独立线 / APK 线）。
> **观察期约束（沿用 backlog §0.5）**：v0.1.9 观察期至 **09-19**，期间**不改产品行为**；下表「可做」列只含零风险项。

| 手机侧编号 | 项 | 复核结论 | 落点 / 处置 |
|---|---|---|---|
| 1 | 半角标点折叠缺口 | 📌 已有 | §3 **F.5**（改折叠口径 = 需拍板）→ 复盘后第一批 |
| 2 | 坏编码显式暴露给用户 | 📌 已有 | §3 **G.3** |
| 3 | **预览截断提示不可见** | 🆕 新登记 | **A7.1**（高概率 × XS） |
| 4 | S1 表格列数对齐补断言 | 📌 已有 | §3 **A4.5** —— **观察期内唯一可无脑做**（纯加断言） |
| 5 | S2 残余 CSS `line-through` | 📌 已有 | §3 **A4.6**（待拍板，≈+5 行） |
| 6 | 自定义 formatCode 含字母误判日期 | 📌 已有 | §3 **A2.2**（报告建议观察半年） |
| 7 | **图片 OCR 竖排盲区** | 🆕 新登记 | **A7.2**（P2，S-M） |
| 8 | zip 解压改流式 | 🆕 新登记 | **A7.3**（S-M，治本）· **2026-09-18 用户拍板升 P1** |
| 9 | docx/xlsx 挪 Worker + `transferable` 零拷贝 | 🆕 新登记 | **A7.3**（M，收益最直观） |
| 10 | OCR worker 内存回收策略 | 🆕 新登记 | **A7.3**（S，需拍板阈值） |
| 11 | xlsx sheet XML 无解压护栏 | ➡️ 作废 | **被 #8（流式）从根上取代**，不再单独排 |
| 12 | PDF 扩展 B 区 CJK 代理对 | 📌 已有 | §3 **A.3**（XS） |
| 13 | xlsx `<rPh>` 注音重复 | 📌 已有 | §3 **A.4**（XS） |
| 14 | 低质页保图 | ➡️ 已移出 | §3 A3.3 + **独立线**（视觉通道天然覆盖） |
| 15 | S4 小 nonAscii 边界（C4/C10/C13） | ✅ **已闭环** | **v0.1.8（`14d8544`）**：阈值 `2/10 → 3/4` + 契约 **S4-8..S4-14** + Linux 侧独立验收（负对照必红） |
| 16 | S5 三条 low | ❓ 出处未复核 | 沿用手机侧登记；疑似已被 S5 批 / 第八轮批覆盖 → **复盘时定**（不虚报为已闭环） |
| 17 | §8.8 四条 low（`&#x;` / 1904 越界 / 行内 code 换行 / `%` 半合并） | 部分 ✅ | `&#x;`/脏数字实体 → **V6 已闭环**（实测输出 U+FFFD）；行内 code 换行 → **组 K2 已闭环**；**1904 越界 → 排 v0.2**（§3 A5.2）；**`%` 半合并 → ❓ 待核** |
| 18 | **U1 一键全部下载 .zip** | 📌 已有 + ✅ **已拍板排 v0.2** | §3 **A5.7**；成本 **M → S**（复用现成 `downloadZip()` + fflate）。**2026-09-16 用户拍板：排 v0.2**（手机侧原文两处口径冲突 §3.5「复盘后第一批」vs §4「排 v0.2」→ 取 **§4**） |
| 19 | **OLE2 精确识别**（老 `.doc`/`.xls`/`.ppt` 分型） | 🆕 新登记 | **A7.4**（XS，已实测可行） |
| 20 | **zip 进**（打包上传、自动解包逐个转换） | 🆕 新登记 | **A7.5**（M，需护栏设计） |
| §4 | 公开语料基准 / 测试脚本重复 | 📌 已有 | §3 **A5.6** / **E.3** |
| §4 | PDF 版面结构还原（独立线） | ➡️ 已移出 | **已立项、按用户拍板不启动**（见 `PDF版面结构还原线-立项与主线划界.md`） |
| §4.1 | APK 线 A1–A6 | 🆕 整节并入 | **A7.6** |
| §5 | F.6 七项未验证 / 毕设线 | 📌 已有 | §3 **F.6** / **D** |

**A7.1 预览截断提示不可见**（手机侧 #3；高概率 × **XS**；2026-09-16 群友实测驱动）
提示文案 `（预览已截断，完整内容请复制/下载）` 目前**拼在 1MB 预览文本末尾**（`truncatePreview` 返回值内）→ 用户要滚过 100 万字符才看得到，**等于没有提示**（群友已实际误报「输出不完整」）。**修 = 把提示挪到 textarea 外的独立提示条**（照 `warnings` 现成写法，几行）。**⚠️ 契约组 Q 断言锁「提示在预览文本里」→ 改断言需拍板。**

**A7.2 图片 OCR 竖排盲区**（手机侧 #7；**P2**，若整篇竖排则升 P1；S-M）
真机海报实测：横排正文几乎全对（逐字空格问题已由 v0.1.8 修好）；**竖排文字被逐字拆散**（`色情链接不要点` → 逐字成行，且「不」误识为「未」）、另一组竖排**整组丢失**。根因 = tesseract 默认按**横排**理解版面，且**整页质量不差 → 不触发 v0.1.7 的旋转重试** → 竖排部分**静默丢失**。三方案（按顺序）：① 新增「**竖排可疑**」信号触发重试（信号易检：输出出现**大量单字成行**）；②**触发粒度从「整页质量」改为「特征触发」**；③ 重试手段 = **旋转 90°**（v0.1.7 已有机制）或 **PSM 5（SINGLE_BLOCK_VERT_TEXT）**，取更优沿用现有打分口径。**同图另两个次要问题**（建议接受）：大字艺术字误识、logo/二维码被当文字。**待拍板**：是否做、先做哪个方案。

**A7.3 内存与性能 · 架构改进（三条；共同前置 = 先量峰值内存）**
> 性质：**不改输出行为、改运行时形态** → 与「修 bug」分排；三条均已**实测定位到代码位置**。选型已调研：`.私档/…/现成方案调研-20260916.md`（8 候选 + 许可已核；**首选 fflate 自带流式 = 零新依赖**）。
- **#9 docx/xlsx 挪 Worker + `transferable` 零拷贝**（M，**收益最直观**）：两解析器**跑在主线程**（只有 pdf/ocr 在 Worker）→ 大 Office 文件卡死 UI；README 已登记 xlsx「**18s 级案例**」= 主线程阻塞 18 秒。`transferable` 零拷贝要**显式写**（`postMessage(msg, [buf])`）。候选 `comlink`（Apache-2.0，~3KB gzip）**需拍板引入**。
- **#8 zip 解压改流式**（S-M，治本）：① `src/docx.js:301` `F.unzipSync(buf)` **一次性解全 entry** → 改「**只解必需 entry、其余原样拷贝字节**」（概念最简单、收益最大）；② `src/xlsx.js` `zipEntry()` 逐条整解压（sharedStrings 有 4MB 护栏、**sheet XML 没有**）→ 走 `DecompressionStream('deflate-raw')` 或 fflate 流式。**风险**：fflate 流式有已知坑（issue #191）；改 zip 重打包须**字节级回归**（契约组 I/T 会拦）。
- **#10 OCR worker 内存回收策略**（S）：WASM 线性内存**只增不减**（`WebAssembly.Memory` 不能缩小），现状 `ocrWorkerPromise` 缓存复用 → 连续转大文件内存持续涨。改法 = 连续 N 个文件 / 空闲 T 秒后 `terminate()` + 清缓存。**⚠️ 需拍板阈值**（重建 tesseract 慢）→ 先测内存曲线。

**A7.4 OLE2 精确识别**（手机侧 #19；**XS**）
现状：任何 OLE2 都给同一句「另存为新格式（.docx/.xlsx）」，**用户得自己猜**。**已实测可行**：前 64KB 内有流名 —— `WordDocument`@0x580（.doc）、`Workbook`@0x2e80（.xls）；**注意流名是 UTF-16LE**，匹配要用宽字符形式。照 `zipKind()` 写法即可。

**A7.5 zip 进（打包上传 → 自动解包 → 逐个转换）**（手机侧 #20；**M**）
解决「手机不能多选」（ColorOS 系 4/4 不可用）。现成资产：`sniff` 已识别 zip、`convert.js` 已有（友好拒绝）分支、fflate 能解压 → 本质是「就地解包 + 循环 convert」。**需设计**：解包护栏（条目数 / 总大小 / 路径穿越）+ 进度 + 上限拍板。

**A7.6 APK 线 · 已归集待办（A1–A6；用户 2026-09-16 拍板「U2 交给以后的打包 APK」）**

| # | 项 | Web 版的困境（实测） | APK 里的解法 |
|---|---|---|---|
| A1 | 自由选择保存目录（原 U2） | `showDirectoryPicker` 仅 Chromium ≥132；手机端覆盖率低，**且与「Chrome 无文件入口」叠加** | 原生 SAF（`ACTION_OPEN_DOCUMENT_TREE`）+ Filesystem 插件写回 → 一次选目录、批量写出、零弹窗 |
| A2 | 多选文件 | 取决于 ROM：**ColorOS 系（OPPO/realme/一加）4/4 不可用**、夸克不可用 | 自有 webview / 原生选择器，不受 ROM 差异影响 |
| A3 | 选择文件夹（整目录转） | `webkitdirectory` 实测仅真 Chrome 可用 | 原生目录树选择 |
| A4 | 多文件下载 N 次「确定」 | 浏览器默认行为，**无法程序化绕过** | 直接写文件系统，零确认 |
| A5 | 第三方浏览器差异（夸克/ROM 定制） | 需逐个能力检测 + 降级 + README 点名 | 自有容器，变量消失 |
| A6 | 50MB 护栏可放宽/自适应 | 护栏是**我们自己代码里的常量**（`src/convert.js` `MAX_BYTES`）→ Web 版读不到设备内存，无法自适应 | 可读原生内存 → 上限自适应；并可用流式/分块（PDF 按页、xlsx 按行）降低内存 |

> ⚠️ **唯一不白送的是「保存不弹窗」**（需从 `<a download>` 换成 Capacitor Filesystem 插件）。**复用点**：U1 一键打包 zip 的逻辑 **Web / APK 通用**，不重复投资。

**A7 的元结论（本批复核产出）**：手机侧 20 条里 **6 条是新增/升格**（#3 预览提示、#7 竖排盲区、#8/#9/#10 内存三条、#19 OLE2、#20 zip 进）、**1 条已闭环销账**（#15 S4 边界）、**1 条作废**（#11 被流式取代）、**2 条出处待核**（#16/#17 残留）、其余 10 条**本文件早已登记**（不重复排）。**主线下一步的性价比最高动作仍是「复盘后第一批」**：预览提示（XS，需拍板改断言）+ 半角标点（XS，需拍板）+ OLE2 识别（XS）——**U1 一键下载已由用户 2026-09-16 拍板改排 v0.2**（不在本批）。

**A8. 第九轮审查（Codex，2026-09-17）核验与处置**（报告：`docs/doc2md-第九轮审查报告-2026-09-17.md`；**基线 = `25a7266`**；3 P1 / 4 P2 / 4 P3）
1. **逐条核验已完成（14 条无一条被驳回）**：动态复现 8 条（§2.2 两个假绿、§2.3 洞3 假红、§3.1、§3.3、§4.2、§4.3、§3.4 代码事实）+ 代码事实 5 条（§2.1、§2.3 洞1/洞2、§4.1、§4.4）+ **待夹具 2 条**（§3.2 顺序漂移=风险登记；§3.3 真实 PDF 触发面）。复现脚本见 `docs/DEV-NOTES.md` 2026-09-17 节。
2. ~~**批 A：守卫与 CI 加固**~~ ✅ **已落地（2026-09-17，用户拍板「观察期内零风险」；全不动 `src/`，产物字节不变）**：`tools/deploy-smoke.mjs`（单/双引号 + srcset、URL 语义禁逃逸、manifest 校验、禁止路径反扫）· `tools/audit-delivery.mjs`（缺包必红、**vendor 清单交叉校验**、`via` 字符串不再误归因、fetch 超时）· **`tools/vendor-manifest.json`（新增）**· **`tools/guard-selftest.mjs`（新增，13 断言含 6 条负例必红）**· CI（**tag 推送也过门禁** + 守卫自测 + 站点白名单 smoke 前移）。
3. **交叉校验成果**：`mammoth@1.12.2` ✅（`VERSION="1.13.1"` 是内嵌 underscore，虚惊）· **`tesseract.js-core` 记录漂移已订正：`licenses.md` 6.0.0 → 6.1.2**（与 npm tarball 逐字节一致）· `read-excel-file@5.8.7` ✅。
4. **未修项登记（本批复核后仍待做；编号 R9-*，逐条可追踪）**：

| 编号 | 报告条 | 项 | 位置 | 定级 | 成本 | 状态 / 批次 |
|---|---|---|---|---|---|---|
| **R9-1** | §2.1 | **批量失败隔离**（单文件读失败 → 整批中断 + 未处理 rejection + 状态栏卡「正在处理」） | `src/app.js:15-26` · `src/convert.js:117-118` | **P1** | S-M | ~~复盘后第一批（§8-B10）~~ ✅ **已完成（2026-09-18）**：`0b2d1d5`（实现）· `ef2bbb7`（摘掉组 Z 三处 `{ todo: … }` 标记，**断言一字未改**）· **Linux 独立验收 9/9 全 PASS**（A11.15）；✅ **先红用例已落**（2026-09-18，契约组 **Z**：Z1/Z2/Z3 红 + Z4 负对照绿 —— 实测证据见 **A11.10**/**A11.11**）~~⇒ 待实现转绿；Z1/Z2/Z3 现带 `{ todo: … }` 标记（B 方案，为保 CI 绿）—— 实现本项时必须摘掉这三处标记~~（**2026-09-19 执行线 A1 核对：该状态已作废——实现与摘标记均已落地，A11.15**） |
| **R9-2** | §3.1 | docx alt 去扩展名误伤 `descr`（题注 `Figure 3.1` → `Figure 3`） | `src/docx.js:33-38` | P2 | XS | 复盘后第二批（§8-C12）：收窄为「只对 `name` + 已知图片扩展名」 |
| **R9-3** | §3.2 | alt「按回调顺序对号」漂移风险（无数量一致性检查） | `src/docx.js:128-134` · `:268-283` | P2 | S | **待夹具**（DrawingML+VML 双回退），§8-C14 |
| **R9-4** | §3.3 | `BT` 不重置 `flip`（跨 BT 块继承朝向） | `src/pdf.js:132-153` | P2 | XS-S | **待夹具**（多 BT / 仅首块 Tm 的真实 PDF）；mock 已复现继承，触发面待定 → §8-C14 |
| **R9-5** | §3.4 | 导出缺图静默（zip 少图但 md 仍引用 `assets/`；内嵌模式仍留相对引用） | `src/ui.js:61-65` · `:110-121` | P2 | S | 复盘后第二批（§8-C12）：收失败资产 → warnings + 状态栏明示 |
| **R9-6** | §4.1 | CI Node 20 vs 发布/验收 Node 24 不一致；`package.json` 无 `engines` | `.github/workflows/tests.yml:29` · `package.json` | P3 | XS | **需拍板**（§8-A5）：加 `engines` + CI 对齐 24（或 20/24 双矩阵） |
| **R9-7** | §4.2 | `extForContentType` 产出非标准扩展名（`svg+xml` / `x-emf` / `x-wmf`） | `src/docx.js:21-27` | P3 | XS | 复盘后第二批（§8-C12）：补三条映射 |
| **R9-8** | §4.3 | GIF 尾部填充被误判（要求末字节恰为 `0x3B`） | `src/sniff.js:216-220` | P3 | S | 复盘后第二批（§8-C12）：**按 GIF 块结构解析到 trailer**（与 backlog「GIF trailer 完整性校验」条合并做，报告已提醒别看末字节） |

> 已闭环（本批批 A）：**§2.2**（deploy-smoke 两个假绿）· **§2.3**（缺包静默 / mammoth 无门禁 / `via` 误归因 / fetch 无超时）· **§4.4**（tag 无门禁）。

5. ✅ **报告已脱敏入库（2026-09-17）**：第 3 行的本地绝对路径 + Windows 用户名 → 换为 `<工作区>`（红线 11），入库前隐私扫描**零真实命中**；报告作为第三方审查记录随仓保存。

**A9. 大 xlsx 实测：护栏内仍会「冻死 2 分钟」（2026-09-17，用户实测驱动）**
> 起因：用户拿 WizTree 全量导出做「接近 50MB」的边界测试，反馈**超时**。我方用真产物 + 真 msedge 实测复现（`.tmp/convert-file2.mjs`，带外部超时 + 主线程心跳探针）。

| 输入 | 大小 | convert 耗时 | backend | 主线程心跳 | 输出 |
|---|---|---|---|---|---|
| `tests/data/real-big.xlsx`（50,001 行） | 773 KB | **31 ms** | `xlsx-self` | —（未到 200ms） | 1001 行（截断提示） |
| **用户测试件**（436,000 行，WizTree 切件） | **47.41 MB** | **116,772 ms ≈ 117 s** | **`read-excel-file`** | **0 次 / 期望 584 次** ⇒ 页面全程冻死 | **仍只 1001 行** |

**链条**：47.41 MB < 50 MB 护栏（放行）→ 但 `sharedStrings.xml` 解压 **87.4 MB** ≫ 4 MB 护栏 → 按 t33 设计**回退 `read-excel-file`** → 该库在主线程啃完整份工作簿（117 s 冻结）→ **输出仍受 1000 行上限**，即 2 分钟只换来 1000 行。

**定级/定位**：**P2（体验）**，与 **A7.3**（流式/Worker 三条）同族 —— 属「大文件路径缺止损与进度」。
**候选修法（需拍板；观察期内不改行为）**：
1. **预检 + 知情同意**（改动最小）：解析前只读 ZIP 中央目录，估 `sharedStrings`/`sheet` 解压尺寸 → 超阈值先提示「约需 N 分钟且页面会暂时无响应，是否继续」；我们**已有中央目录读取能力**（`findEocd`/`zipEntry`）。
2. **回退路径加止损**：库路径也只取前 N 行即停（避免为了 1000 行啃完 47MB）。
3. **流式 + Worker**（A7.3-#9，治本）：至少不冻 UI、可显示进度/可取消。
**附注（用户文件侧）**：其切件保留了 WizTree 的**标语行**（第 1 行）→ Excel 另存后成为表头行；`.tmp/cut-csv.mjs` 可正确丢弃标语行（表头 + N 行数据，字节级原样复制）。

**A10. 大文件转换的「界面反馈」：提示 / 转圈 / 进度条（2026-09-17 用户提出，登记待排）**
> 用户原话意图：转换大文件时界面上应有「文件过大，正在转换」这类提示，或者旋转图标、进度条。
> **⚠️ 硬约束（本次实测得出）**：转换期间**主线程被完全占住**（心跳探针 0 次 / 期望 584 次）⇒ **任何在主线程渲染的转圈/进度条也会一起冻住**，用户实际看到的仍是浏览器的「页面无响应（停止运行/等待）」。**所以这不是「加个动画」的活，必须分层做**：

| 层 | 做法 | 成本 | 依赖 | 能给用户什么 |
|---|---|---|---|---|
| **L1（最小，建议先做）** | **启动前提示 + 主动让出一帧**：预检文件规模（见 A9 候选修法①）→ 先把「文件较大（约 N MB），转换可能需要约 M 秒，页面会暂时无响应，请勿关闭」画出来，再开始解析（`requestAnimationFrame` + `setTimeout(0)` 双让帧，确保真的画上了） | **XS** | 无（纯主线程） | **知情**：知道要等、别以为卡死 —— 直接消除「浏览器弹窗让人以为崩了」的核心痛点 |
| **L2（真进度/可取消）** | **解析挪进 Worker**（= A7.3-#9）→ 主线程空闲 ⇒ 转圈/进度条**真能动**，可加「取消」按钮 | **M** | Worker 化 + 结果回传协议 | 真·转圈 + 进度 + 取消 |
| **L3（细粒度百分比）** | **自研流式解析**（= A7.3-#8）+ 进度上报（按行/按块） | **S-M** | 流式解析 | 百分比进度条（库回退路径给不出粒度 ⇒ L3 只能覆盖快路径） |

**拍板点（做之前要定）**：① 提示阈值按什么算（**文件字节**还是**解压后规模**：本次实测 47MB 文件的 `sharedStrings` 解压 87MB，字节数完全看不出来）② 提示文案与预计耗时的算法（线性外推实测数据点？还是写死档位：小/中/大/超大）③ 是否要「取消」（**无 Worker 时取消按钮是假的** —— 只能提示用户刷新页面）④ 双让帧是否要写进契约（断言「大文件路径必须先让帧再解析」需要 UI 级断言，成本另计）。
**归属批次**：与 A9 候选修法①同一批（**复盘后第一批**；观察期内不改行为）。**与 A9 是同一件事的两半**：A9 解决「慢/冻」，A10 解决「用户不知道在等什么」。

**A11. 手机侧真机矩阵交件（2026-09-17）核验与处置**
> 交件：`doc2md-mobile-20260917.zip`（2,128,609 B / `8E1C7B51…`，19 条目）→ 落 `.私档/验证/手机实测-20260917/`，**14 文件逐个字节 + SHA256 已记**。主报告 = `手机侧整合报告-20260916至20260917.md`（含 6 条待动作 + 4 个待回答）。

**1. 核验结果（我方实测）**
- ✅ **P1 三端逐字节一致**：手机默认内核 = 手机 Chrome = **我方 Windows headless**，三者产物同 SHA **`c4250253bc52dc07…`（11,549 B / 233 行）**；旧版错乱产物 `c344e9c5…`（10,561 B）作对照 ⇒ P1 修复在真机确认，且 tag 后两次"行为零变更"重构**未观察到差异**。
- ✅ **大文件矩阵产物可复现**：`4-mid-large`（25 万行）手机夸克 **339,797 B** 与我方 headless **339,797 B 同 SHA `013C170650A1FA13…`**。
- 📌 **我补测的桌面基线**（交件里标为"估算"的那格）：`4-mid-large` 桌面 **78,653 ms**（心跳 1/393 ⇒ 主线程全程冻死）；`3-big` 桌面 59,402 ms（用户真机 UI 实测）/ 116,772 ms（我方 headless）。
- 📌 **单位口径澄清**：交件中的「361.0 / 364.8 MB」为 **MiB**，等于 378.6 / 382.5 MB（十进制）⇒ 与我方 zip 条目实测一致，非分歧。

**2. 六个待动作的处置**
| # | 交件建议 | 我方处置 |
|---|---|---|
| 1 | backlog 条目 8 升优先级 + 验收判据「解析到够 `XLSX_ROW_LIMIT`(1000) 行即停」 | ✅ **已拍板（2026-09-18）：升 P1**（手机侧建议 P0，用户定 **P1**）；与 A9/A10 合并为「**大文件路径专项**」（改行为 → 观察期后）**【仍需拍板：验收判据口径 + 先红断言形式】** |
| 2 | 护栏加判「解压后体量 / 行数」 | **与 A9 候选修法①同源，合并**；口径采「**二次确认而非硬拒**」**【需拍板：阈值口径】** |
| 3 | 截断提示写进产出 | **同意**（现 3,054 行输入 → 恰 1,000 行产出且**零标记**，下载转发的 .md 看不出截断）**【需拍板：形态】**；倾向 ① 仅在截断时于 md 首/尾插一行 HTML 注释 `<!-- doc2md: 已截断 … -->`（不污染渲染）② 可见引用块 —— 二选一 |
| 4 | Worker（条目 9）收益重估：只治「冻」不治「慢/炸」 | **同意**；**并纠正一处推断**：交件用「能滑动」推测"解析可能分块让出" —— **合成器线程滚动在主线程被占死时仍可滚动**，故该观察**不能**证明主线程让出 ⇒ 需 JS 侧心跳探针才能定论（下一轮低成本可答） |
| 5 | README 补聊天 App 内置 WebView 口径 + 「⋯ → 在浏览器打开」引导 | **同意**，且**不改行为、不进产物** ⇒ **观察期内即可做**【需用户点头】 |
| 6 | 野生素材入库当第 20 个对照样例 | ⛔ **不能直接入库**：那是真实公文（真实单位名 + 文号）+ 个人路径，撞红线 11 与 `tests/data` 公开性 ⇒ **替代方案**：按同一**结构特征**（翻转 Tm + 缩放 + 逐字 TD + 中文）**合成同构样例**（虚构文字）入库 **【需拍板是否做】** |

**3. 四个待回答**
1. **`accept` / Chrome 无「文件」入口**：**成因与解法 2026-09-15 v6 已定案**（`accept` 含图片类 ⇒ Chrome 走媒体选择器；`showOpenFilePicker()` 实测可用 / 或拆「文档·图片」两入口），但**产品侧"修不修"从未拍板** —— README 只登记为**已知限制 + 「不推荐 Chrome」兜底** ⇒ **属"未拍板"**。且本次新证据（**微信/钉钉内置 WebView 无文件入口且无旁路**）使"换浏览器"兜底在聊天场景失效 ⇒ **新增待拍板项（见下 A11.4）**。
2. **backlog §2.5 编号不一致**：**确认交件的 v8 变更记录陈旧**；权威编号 = **#8 zip 解压改流式 · #9 docx/xlsx 挪 Worker + `transferable` · #10 OCR worker 回收**（与其表格一致，仅变更记录需修）。
3. **是否要手机侧出峰值内存（PSS）**：**要，但排在被修之后** —— 修法（够 1000 行即停）会同时降时间与内存，PSS 的价值在**修前/修后对照验收**；届时用同一 `4-mid-large` 跑两遍即可（需配一次 ADB，顺带取崩溃 logcat）。
4. **是否追精确大文件阈值**：**建议不追** —— ① n=2 分不清「行数」与「表体 XML 体量」哪个是真因；② 真因修掉后天花板会上移、阈值立即作废；③ 真正要定的是**判据口径**（解压后体量/行数）而非具体数值 ⇒ 交件的「补中间档」**降级为修完后复测**。

**A11.4 新增待拍板项：手机端文件输入方式**（= 交件 §5/验收报告 §5 第 8 条的落点）
- 现状：README「不推荐 Chrome」+ 换浏览器兜底；**聊天 App 内置（微信/钉钉）无文件入口且无旁路** ⇒ 主入口用户拿不到文件。
- 候选：① **FSA `showOpenFilePicker()`**（实测可用：多选 ✅ / 限 PDF ✅，需能力检测 + 降级）② **拆「选择文档 / 选择图片」两入口**（符合直觉，兼容性最好）③ 两者都做。
- **需拍板 + 排期**；与 APK 线天然解耦（APK 用原生选择器）⇒ 只在 Web 端有价值。

**A11.5 本批已执行（2026-09-17，用户拍板 A + B① + D）**
- **A｜README 口径**：新增第三类限制 —— **聊天 App 内置浏览器（宿主 WebView）**：微信内置只有相册 · 钉钉内置**「上传文件」灰**且**点下载可能无反应** · **QQ 内置正常**（对照）⇒ 给出 **「⋯ → 在浏览器打开」** 引导。（真机实测驱动；**不改行为、不进产物**）
- **B①｜截断提示写进产出**：`130a927`（**先红**：契约组 G2 续号 **L4c / L4c-2**）→ `f6878e7`（**实现**：`src/convert.js` 仅在 `truncated` 时于 `.md` **首行**插**单行 HTML 注释**，内容取截断原文；未截断件不得出现）· 产物 **121,844 B / `9E30306BAE64D57E…`** · 契约 **262 / 260 pass / 0 fail / 2 skip** · lint 0 · metrics 19 文件 / 415 函数 / 超限 0 / 0.4%。
- **D｜合成同构件样例（替代野生素材入库）**：`6a2c733` —— 新增 `tests/data/sample-scale-multipage.pdf`（**1,960 B / `C5A00DBE…`**）：**3 页** · 逐页 `Tm` 缩放 **0.05 / 0.045 / 0.03**（照真机实测区间）· **逐字 `TD`** · 每页首行**两个文本对象** · 虚构文字 ⇒ 契约组 U 续号 **U10**（行内顺序 + 跨页顺序 + 3 个页标记）；**负对照实测：去掉 `tdx` 缩放 ⇒ U9 与 U10 同时红**，还原后全绿。**局限如实标注**：合成件为 Helvetica ⇒ **不含中文**（中文需内嵌 CID 字体，非本夹具范围）。
- **新增真机数据点**：用户 2026-09-17 自行在**手机 Chrome** 跑 `3-big`（47.4 MB）→ **很快崩溃、要求重新加载** ⇒ 与手机侧矩阵一致（系统浏览器 ❌ / 夸克 ❌ / **Chrome ❌**）。

**A11.6 第二包（ADB 内存取证，2026-09-17 晚）核验**
- 交件：`doc2md-mobile-20260917 (1).zip`（**2,157,540 B / `0A8778C1…`**，23 条目）→ 落 `.私档/验证/手机实测-20260917-第2包/`；**13 个既有物证逐字节不变**，变更 3 份文档（整合报告 / 复验报告 / README）+ 新增 `5-ADB内存取证-20260917/`（PSS 曲线 · top 快照 · logcat 关键行）。
- **我方独立核算**（按时间戳分组；150 个时间点，20:59:45→21:09:18）：

  | 指标 | **我方核算** | 交件口径 |
  |---|---|---|
  | 峰值（PSS+swap，全部 quark 进程） | **13,238,516 KB = 12.63 GiB = 13.24 GB** @21:08:33 | 11.24 GB |
  | 峰值 PSS（不含 swap） | 11,584,928 KB = **11.58 GB** | — |
  | 渲染进程峰值 PSS | 10,653,704 KB = **10.16 GiB** | — |
  | 平台期（21:02→21:07） | total **8.69 GiB** / renderer **6.29 GiB** 稳定不动 | 「渲染 6.5 GB」 |
  | 倍数 | 13.24 GB ÷ 35.86 MB = **369×** · ÷ 解压 XML 382 MB = **34.6×** | 321× / 31× |

  ⇒ **交件口径偏保守，我方核算更大**（其 11.24 GB 更接近"仅 PSS"那一档）⇒ 结论只会更强，**不构成分歧**。
- **证据核验 ✅**：`top` 快照 —— `Mem 15308M total / 14961M used / **346M free**`、`Swap **7171M used**`、渲染进程 `RES 4.8G / %CPU 290 / TIME+ 61:16.96`；logcat —— `am_proc_died` 6 条（含 `:sandboxed_privilege_process0`）+ `am_kill … low memory` **45 条 / 31 个应用**。
- ⚠️ **归因边界（证据不足以定论，非打回）**：交件结论「崩因 = 渲染进程**自身分配失败**，不是被系统回收」—— **`am_kill` 缺失只能排除 AMS 发起的回收，排除不了内核 LMK/OOM-kill**（内核路径不写 `am_kill`）；且交件自述 **`dmesg` 被 SELinux 挡** + logcat 为**带标签过滤** ⇒ **两种假设（渲染进程分配失败自杀 / 内核 OOM-kill）都会产生 `am_proc_died`**。**下一轮低成本取证（具体命令）**：① `adb logcat -b all -v threadtime` **不过滤**（或至少带 `lowmemorykiller|lmkd|chromium|libc|DEBUG`）复跑一次崩溃；② `adb shell dumpsys dropbox --print`（无需 root，常含 `data_app_crash`）；③ Chromium 崩溃页文本（「Aw, Snap」/「页面崩溃」）一眼可辨。
- ⚠️ **交件内部不一致（待澄清）**：README §0.2 列「同一份 47.4 MB 文件…**43 秒转完**」，而 §1 备注「47.4MB 那档的产出（**真机上从未产出成功**）」—— 二者需澄清；若真有 43 s 成功轮次，请附产物或截图（那将是**首次**手机端转完 `3-big` 的证据）。
- **对 backlog 的影响**：① §2.5 的**共同前置「先量峰值内存」✅ 已完成**（用此数据）；② 为 **#8（流式 / 解析到够 1000 行即停）** 提供量化目标 —— **峰值 12.6 GiB → 目标降到 MB 级**；③ 「天花板随当时可用内存浮动」（同一文件 43 s 完成 / 20 s 崩 / 磨 20 分钟无果三种命运）⇒ **印证"不追精确阈值"的判断**；④ 与 A9/A10（预检 + 进度 + 超时）同向。
- **本轮后续产出（用户拍板 A+B）**：
  - **验收判据设计稿**：`docs/大文件路径专项-验收判据-20260917.md` —— **D1–D6 判据**（产物逐字节不变 / 解析量上界 `meta.scan.bytes ≤ 8 MB` / 绝对耗时 < 5 s / 内存 < 200 MB / 护栏语义 / 负对照必红）+ **契约组 Y 先红断言设计**（Y1 先绿守卫锁 D1 · Y2/Y3 先红 · Y4 负对照）+ **5 个拍板点** + 一条**被否掉的候选判据**（时间比值不变性：实测现状比值 1.48 ≈ 线性，判别力不足）。
  - **手机侧下一轮取证单**：`.私档/手机侧/任务-下一轮取证-20260918/交接单.md`（+ 同名 `.zip`）—— **T1 崩溃归因定论**（不过滤 logcat + dropbox + 崩溃页截图；含"三条通路都没有也算完成"的兜底判据）· **T2「43 秒转完」物证或澄清** · **T3 `4-mid-large` 修前 PSS 基线**。

**A11.7 局域网交换通道（2026-09-17 建立；用户要求「局域网 git：放文件就克隆、做完提交回去」）**
- ⚠️ **先记一个被实测否掉的方案**：**Windows 上 `git daemon --enable=receive-pack` 的推送必挂** —— `upload-pack`（ls-remote / clone）正常，`receive-pack`（push）在**服务端广播 ref 之后双方僵住**（`GIT_TRACE_PACKET` 佐证：客户端发出 update 命令后无后续；daemon 日志显示 `Request receive-pack` 后无完成）。**结论：Windows 别用 git daemon 做 push**（试过换绑定点亦然）。
- **采用的方案 = git bundle 双向 + HTTP 上传**（无 daemon、跨平台可靠、仍是真 git 提交）：
  | 环节 | 做法 |
  |---|---|
  | 交换仓 | `.私档/lan-git/doc2md-exchange.git`（裸仓；**LAN-only，永不推 GitHub**）；约定目录 `orders/`（电脑侧放任务）+ `deliveries/`（手机侧放成果）；含 `README-协作协议.md` |
  | 服务 | `.私档/工具/serve-exchange.py`（GET 目录页 + **表单上传** + `PUT /up/<name>`），端口 **8099**，根 = `.私档/lan-git/exchange-www`（含 `orders-<日期>.bundle` + `手机侧怎么用.txt`） |
  | 手机侧 | 下载 `orders-*.bundle` → `git clone <bundle>` → 干活 → `git bundle create delivery.bundle main` → 浏览器表单或 `curl -T` 上传 |
  | 电脑侧收件 | `git fetch <上传的 bundle> refs/heads/main:refs/remotes/phone/main`（再按需 merge/摘取文件） |
  | 大文件 | 夹具（如 47.4 MB xlsx）**不进 git**，走同一页面直接上传/下载 |
- **端到端自测（本机实测，2026-09-17）**：HTTP 下载 bundle（5,538 B）→ `git clone`（历史完整）→ 提交 → `git bundle create`（5,957 B）→ `curl -T` 上传（服务端回 `OK … 5957`）→ 电脑侧 `git fetch` **exit 0**，`deliveries/20260918-取证/结果.md` 到位；**自测痕迹已清理**（ref 与上传件均删）。
- **边界（红线相关）**：该服务**无认证**（HTTP PUT/表单谁都能传）⇒ **仅内网使用**、只承载交换内容（**不含主仓库**、不推公网）；主仓库推送仍走 GitHub（发布动作人执）。
- **换网不用重建（2026-09-18 实测）**：通道是「bundle 文件 + HTTP」，**不绑 IP**；服务绑 `0.0.0.0:8099`，换网络自动覆盖新接口，**不需要重启**。仅需更新页面/说明里的地址。
  - 已见地址：校园网 `172.23.72.249` → 手机热点 **`10.131.114.108/24`（网关 `10.131.114.16` = 手机本身，IPv6 `2409:` 电信移动段）**。
  - **防火墙不用动**：当前配置文件 = **Public / `BlockInbound,AllowOutbound`**，但 GPO 下发有一条**启用**的 `远程协助(TCP-In)`（`Profiles: Public` · TCP · **`LocalPort: Any`** · Action Allow）⇒ **入站 8099 直接通**（上午三个包就是在同一个 Public 配置文件下传上来的，互为证据）。
- **本沙箱读网络信息的可用路径（踩过的坑）**：`netsh … | Select-Object`（管道）与 `netsh … > 文件`（重定向）**都被拒**；要用 `netsh <子命令>` **直接输出**，由 harness 自己落盘后再用 grep 读（`ipconfig` 同理）。另外 **`.NET` 的 `NetworkInterface.GetAllNetworkInterfaces()` 在本沙箱拿不到 IPv4**（WLAN 显示 Up 但 IP 为空，会误判成"没连网"）⇒ 核地址只信 `ipconfig`。
- **暴露面收口（2026-09-18 用户拍板 ①②，已执行）**：交付包**移出服务面** → 归档 `.私档/验证/手机实测-20260918-归因取证/`（三件：458,741 / 473,063 / 476,266 B，SHA256 前 16 位 `16C78C7ED1E084DB` / `255510CC6AAF1DFB` / `B9AF83EE21437595`）；`uploads/` 清空为**收件箱语义**（传完即移走）；服务目录只剩「要发出去的东西」= r2 bundle + 回执 + 交接单 + 用法说明。
  - **服务面本身的边界（已核）**：`safe_name()` = basename + 字符白名单 ⇒ 写入**只能落在 `uploads/`**，`../` 出不去；`SimpleHTTPRequestHandler` 的 GET 也自带目录逃逸防护；主仓库与 `.私档` 其余部分**不在服务目录内**。⇒ 暴露面 = 「服务目录可读 + uploads 可写（单次 ≤400 MB，次数不限）」。
  - **机器级发现（非本服务问题，改不了）**：防火墙规则里那条放行（`远程协助(TCP-In)` · Profiles Public · TCP · **LocalPort: Any** · Allow）**净放行所有入站 TCP**，且**无任何 Block 规则**与之竞争 ⇒ 本机在公用网络上跑**任何**本地服务都可能被同网段扫描到。属域策略（GPO），需管理员。
- **口令保护（2026-09-18 实施，用户要求：白天回校园网必须要）**：`serve-exchange.py` 加 HTTP Basic；**口令从文件读**（默认 `.私档/lan-git/.exchange-token`，可用 `EXCHANGE_TOKEN_FILE` 覆盖）——**文件存在即启用、删掉即回开放模式、每请求现读 ⇒ 换口令不用重启**；用户名随便填，只校验口令；`hmac.compare_digest` 恒时比较；认证失败打 `[deny] <IP> <METHOD> <path>` 便于审计。口令值**只存本机 `.私档`（gitignore）**，不入仓库/文档/聊天（本档只记路径与机制）。
  - **实测四组**（经 `10.131.114.108:8099`）：无口令 `GET /` **401** · 无口令 `GET bundle` **401** · 带口令 `GET bundle` **200 / 484,277 B**（与发出版本同字节）· 无口令 `PUT` **401 且不落盘** · 带口令 `PUT` **201 `OK auth-test.txt 1035`**（测试件已删，`uploads/` 归零）。
- ⚠️ **坑（同一形态第二次踩，值得固化）**：`job_kill` 只杀 **pwsh 壳，不杀 python 子进程** ⇒ 旧服务仍占 `0.0.0.0:8099`；更隐蔽的是**壳死后子进程 stdout 管道断开**，它处理请求时 `print(...)` 抛 `BrokenPipe` ⇒ 表现为「**端口在监听但连接无响应**」（curl 报 `000`，不是 401/拒绝连接，极易误判成网络问题）。**处置**：`Get-Process python` 按 **StartTime** 认新老 → `Stop-Process` 掉旧的；换服务前先核监听者数量。
- **待拍板（设计后果，我没自作主张动）**：手机侧交付提交**并入了同一条历史**（换来"快进合并、不冲突"），代价是**发回去的 r2 bundle 里也带着他们的交付内容**（含日志节选、机型）。要彻底分开须让 deliveries 走独立仓/独立线（历史不再一体）。

**A11.8 手机侧 T1 交件核验（v1/v2/v3 三包，2026-09-18 我方独立复核）**
- **收件**：`deliveries/20260918-下一轮取证/`，三个 bundle 接力线性（v1 `bc2a1c8` → v2 `b6e5014` → v3 `ae8165c`）；56 文件，含 2.24 MB 全量 logcat、两轮窗口切分、两份 bugreport 抽取、采集脚本全套。
- ✅ **第一道核验通过**：`SHA256SUMS.txt` **56/56 逐文件字节 + SHA256 全对**（我方独立脚本 `.tmp/lan-check/verify-sha.mjs`；唯一清单外文件 = 自指的 `README-包内说明.md`，包内已声明）。
- ✅ **复核为真的硬事实**：`/proc/vmstat: oom_kill = 0`（开机 67 h 内核 OOM killer 未开火）· `ro.lmk.kill_heaviest_task=true` / `use_minfree_levels=false` / `psi_complete_stall_ms=700` · bugreport 内核段只覆盖 **79 秒**（`169628.217980 − 169549.467012 = 78.75 s`；31,360 行 ÷ 78.75 ≈ 398 行/秒，算术自洽）· 两轮 5.4 GB 渲染进程均由**用户 cleaner** 收场（`am_kill … due to cleaner` + `reason=10 USER REQUESTED`）· 09-17 dropbox 无 `data_app_crash`。
- ❌ **三处问题（同一根因：只按字符串 `lmkd` / tag `lowmemorykiller` 找杀记录，漏掉真正的杀记录 tag `killinfo`）**：
  1. **"指纹交叉验证"不成立**：`com.quark.browser:push` @**22:07:28** 在 logcat 里**没有任何 `am_kill`**（全包检索：`26133` 只出现在 exit-info 与一条更早的 auditd 行）。那条 logcat 证据真身是 **lmkd（pid 710）的 `killinfo`**：`22:07:28.125 I killinfo: [26133,10302,915,500,148728,…]`，37 ms 后 exit-info 记 `reason=3 LOW_MEMORY`。**报告把「lmkd 杀人」当成「AMS 回收」的范例 —— 两条通路的指纹正好写反。**
  2. **"lmkd 的 kill 记录写 kmsg、本机无通路可读 ⇒ 不可判定"为误**：lmkd 的**杀记录 tag = `killinfo`**（写入者 pid 710；其 `lowmemorykiller` tag 在本机**只写 vistype/RBG 状态迁移、不写杀**）。本轮两轮**共 5 条 killinfo** ⇒ **不需要 root 也能判定 sender**；"唯一剩下通路 = root"不成立（README §7.1 与 `未采说明` 末段同需改）。
  3. **渲染进程 @20:57:24.931 = `reason=2 SIGNALED / status=9` 缺原始证据**：三包 + `.私档` 全域检索，20:57 只有**上轮**的 `I/am_proc_died: [0,20202,…,0,3] @20:57:24.928`；交付的 exit-info（22:18:30 抓）**最早条目 21:14:40**（16 条上限已把 20:57 顶掉），且**全部 `SIGNALED/9` 都属于 `:RemoteStatService`**（21:20:35 / 22:08:11 / 22:18:01，rss=0.00）—— 与渲染进程是**不同进程**。⇒ §6 #1 那条"实质更正"**已按用户 2026-09-18 拍板降级为「推断」**（不计入实测台账；若手机侧能补交当时那份 exit-info 可再恢复为实测）。
- ⭐ **本轮最有价值的新证据（报告未使用）**：`killinfo` 首字段 = 被杀 pid、第三字段 = oom_score_adj；**22:07:28 被 lmkd 杀的正是夸克进程（uid 10302）**。配合 `kill_heaviest_task=true` ⇒ **这台机器的 lmkd 在本负载下确实会挑夸克**（渲染进程是全机最重者）⇒ "sender = lmkd" 先验显著上升（**未定案**，但一次复现即可定案）。
- **与我方上轮判断的关系**：我上轮"`am_kill` 缺失排除不了内核 LMK"的洞**成立**（对方已承认并更正过滤标签写错）；但**正确的补洞证据不是内核日志，而是 logcat 的 `killinfo`**。
- **次要**：README §3 "第 2 轮 ≈30 条 `am_kill`" 我方数到 **44 条 low memory + 20 条 cleaner = 64 条**（不影响定性，建议核对）。
- **已办（2026-09-18 用户拍板三条）**：① **回执 + 新任务已发**（走局域网交换仓，见下）；② backlog 条目 8（解析到够 1000 行即停 / 流式）**升 P1**（手机侧建议 P0，用户定 **P1**）；③ 20:57 那条**降级为「推断」**。
- **发出物（交换仓 `main` = `a8189e8`；历史直线：种子 → 手机 v1/v2/v3 → `2364444` 协议更新 → `a8189e8` orders）**：
  - `orders/20260918-归因定案/复核回执-上一轮T1交件.md` —— 三处更正 + `killinfo` 通路 + **指纹表更正**（判别主力是 logcat 的 tag，不是 exit-info 的 reason）+ 计数待核 + 我方处置。
  - `orders/20260918-归因定案/交接单.md` —— **T1b**：全量 logcat 抓 **`killinfo`** 判 sender；**三条判据写死**（lmkd / AMS / 宿主）；**不需要 root**；**两轮封顶、未复现也要交**；交付物 5 项 + SHA256SUMS。
  - `README-协作协议.md` 更新为 **bundle + HTTP**（撤掉已废弃的 `git daemon` 描述，带上 Windows 推送挂掉的实测结论）。
  - **发布件**：`exchange-www/orders-20260918-r2.bundle` **484,277 B / `330E9B8527346057802D4F5F27E73556F8E0B2562DFB0AFF258C5C81F380B99F`** —— **自包含、无前置**（`git bundle verify` = "The bundle records a complete history"）；另把两个 md 直接挂在页面上，手机侧**不装 git 也能读**；旧 `orders-20260918.bundle`（停在种子提交、已被手机侧克隆反超 ⇒ 误拉会 ff 失败）**移入 `lan-git/archive/`**。
  - **端到端核验**：HTTP 下载回来 = **484,277 B / 同 SHA**（与发出版本逐字节一致）。
  - ⚠️ **本轮踩到的沙箱限制（可复用）**：`git bundle create` 内部要 spawn `pack-objects`（stdio 管道）→ workspace-write 下 **`cannot create standard input pipe for pack-objects: Permission denied`**；**重组路径**（无需管道）：`git pack-objects --all <prefix>` 产出**完整非 thin 包** → `node .tmp/lan-incoming/mk-bundle.mjs` 手写 v2 头组装 → `git bundle verify` 通过。附带好处：**无前置** ⇒ 手机侧 clone/fetch 不需要任何本地对象。

**A11.9 T1b 交件（第 2 轮归因）核验 + 通道运维（2026-09-18 夜）**
- **收件**：`deliveries/20260918-归因定案/`（21 文件；bundle 600,709 B / SHA256 `4F7FF343A0D36D66…`）；**独立核验 19/19 逐文件字节 + SHA256 全对**。
- **他们是怎么拿到任务的**：**没拉 r2**（提交父节点 = `ae8165c`），**直接从网页上那两份 md 读的交接单** ⇒ 「不装 git 也能读任务」这条通道有效；代价是他们的线与我的 `a8189e8` 在 `ae8165c` 处**分叉** ⇒ **已由我方合并**（porcelain `merge` 内部要 spawn `git stash`，被沙箱拒 ⇒ 改**纯 plumbing**：`read-tree -m <base> <ours> <theirs>` + `commit-tree` + `update-ref`；合并提交 **`37c86b9f`，双父** `a8189e8` + `fcdc0c5`）。工作树核对：`orders/` 2 目录 3 文件、`deliveries/` 2 目录 79 文件、根 `README-协作协议.md`（我方更新版）。
- **本轮结论：未复现**（渲染进程存活 ≈15 分钟、PSS 峰值 6.95 GB，最终**用户手动关闭**）⇒ 三条判据全未触发：窗口内 `killinfo` 仅 **1** 条且 pid **17188 = `com.android.filemanager`**（非夸克）；`am_kill` **66** 条**无一指向夸克**。
- ⭐ **上轮"缺证据"那处已撤销**：他们补交了 **`ApplicationExitInfo #13: timestamp=2026-09-17 20:57:24.931 pid=20202 process=com.quark.browser:sandboxed_privilege_process0 reason=2 (SIGNALED) status=9`**。出处 = **他们自己的 DSH 会话记录**（`session.jsonl.zstd` 第 3448 条，2026-09-17 22:09:07 那次 read 的输出）——**原件被他们脚本缺陷覆盖**（`t1e-退出原因.sh` 固定同名文件，22:18:30 的 dump 盖掉了 22:09:07 的）。**他们主动标为「转录（transcript）」并声明"不主张其等价于原始文件"**（比我给的"补交即可恢复为实测"更严格）⇒ 我方按**转录件**采信，**A11.8 §3 那处"缺原始证据"撤销**。
- ⭐ **由该条目得出的新推论（我方）**：`:push`（pid 26133）是 **lmkd 杀 ⇒ `reason=3 LOW_MEMORY`**（`killinfo` 与 exit-info 两条独立记录相隔 37 ms = 正对照）；而渲染进程 @20:57 是 **`reason=2 SIGNALED`** ⇒ 本机存在正对照「**lmkd 出手 ⇒ 记 LOW_MEMORY**」，故 `SIGNALED/9` **削弱 lmkd 假设**、加强「**宿主（UC/Chromium）主动杀**」或「渲染进程自杀式 SIGKILL」。**仍未定案**（那轮过滤器不含 `killinfo`，缺发送方侧记录）。
- ⭐ **他们的实质新证据**：`u4native_ThreadWatchdog → JsAnrThreadNotRespondingCallback.showDestroyCurrentWindowDialog / handleJsAnr`（`full-run1.log` 23:01:04.050，pid 29373 = 夸克主进程）⇒ **「页面无响应」弹窗是宿主自己弹的**，且代码路径含「**销毁当前窗口**」⇒ 宿主**具备**杀/销毁渲染进程的能力与代码路径（对"宿主支"是**间接支持，仍属推断**）。
- **两条硬伤（他们自己写明的）**：① 采集窗被 **23:01:13.470 的 Wi-Fi 断网**截断（无线调试走 Wi-Fi ⇒ adbd 会话结束 ⇒ logcat 子进程被杀），此后到用户关闭 ≈15 分钟**无我方日志** ⇒ **不得当作"没有发生"**；② 他们一度据 PSS 采样判定"渲染进程已死"，被用户证词推翻（真因 = 通道掉线后 `ps` **静默失效**）⇒ 已定纪律：**判"进程死活"必须双通路交叉**（`ps` + `dumpsys meminfo <pid>`）。
- **另一处通道故障（ADB 侧，与我的服务无关）**：`android_adb_shell_exec` 在 **adbd 已停**时会**退化为以 DSH 应用自身身份（uid 10388）本地执行** ⇒ 缺 `DUMP` 权限、`logcat` 只见自身日志；而 DSH 状态文件仍报 `connected=true` ⇒ **权限静默降级**（"文件生成了"≠"命令以预期身份执行了"）。他们已**作废 run1 全部产物** + 加**身份断言**（非 `uid=2000(shell)` 直接 exit 9）后重跑。
- **我方缺陷（已修 + 已验证）**：`serve-exchange.py` 的 `list_directory` 误写 `return self.wfile.write(data)`（返回 **int**）⇒ 基类 `do_GET` 把 int 当文件对象 ⇒ **每次目录列表都抛 `AttributeError: 'int' object has no attribute 'read'/'close'`** 并 abrupt close 连接（页面仍能显示，所以一直没暴露）。已改 `return None`；重启后三次目录列表 **200**、日志**零 traceback**。
- **通道运维**：地址**第二次变更**（`10.131.114.108` → **`10.100.247.108`**，网关 `.183`）—— 服务**无需重建/重启**，只改了页面说明；手机侧（`10.131.114.16`）首发遇两次 401 后 200（口令提示流程正常）。
- **我方方法论教训（新增两条）**：① **目录级 grep 会静默跳过"二进制特征"的大日志**（rg 把 1–2 MB 的 `crash-full*.log` 当二进制跳过）⇒ 我此前"全包检索"的说法**范围不实**（幸好关键结论都是**单独指定文件** grep 验的，结论未受影响）；**日志取证必须指定文件或 `--text`**。② **`.cmd` 脚本里不能写中文**（cmd 按 GBK 读 UTF-8 ⇒ 乱码行被当命令执行，报 `'�' is not recognized`）⇒ 批处理**只写 ASCII**。
- **T1 状态：仍开放**（两轮都没取到 sender）。手机侧建议 **Wi-Fi 可用时重跑**，并建议 `setsid` 独立会话起 logcat（避免随 adbd 断线一起死）。
- **已拍板（2026-09-18 用户）**：**T1 第三轮挂起，"后面再说"** —— 硬前置是 **Wi-Fi**（Android 无线调试的 `adbd` tcp 模式随 wlan 接口存在与否起停，**没 Wi-Fi 连配对都做不了**；换网后还需**重新配对**）。**不做**"用数据线接电脑跑 adb"那条替代路线（那会把手机侧自助采集变成电脑侧介入，且与手机侧工具链不兼容）⇒ **不排期，等条件满足时再提**。③ backlog 条目 8（P1）与 T1 的先后：**T1 挂起期间 条目 8 不受 T1 阻塞**，按观察期后第一批推进。

**A11.10 §2.2/§2.3 复核 + S1 断言 + R9-1 先红用例（2026-09-18 用户指令批次）**
- **§2.2 / §2.3 不是"没修"，而是批 A（`033ddfe` + CI `6c1d7cf`）已闭环** —— 本次逐条复核（不凭记忆）：
  - **§2.2（deploy-smoke 两种假绿 + 三处缺口）→ 建议 4/4 全落地**：单/双引号 + `srcset` 提取 · **URL 语义解析**（`..` 逃逸拒绝）· manifest `icons[]/start_url/scope` 校验 + **禁止路径反扫** · **CI 组装临时 `_site` 后跑 deploy-smoke**（`tests.yml:48-59`）。
  - **§2.3（audit-delivery 四个洞）→ 建议 5/5 全落地**：交付面**缺包必红** · `mammoth` 纳入机器可读 `tools/vendor-manifest.json`（存在性 + 大小 + SHA256 + 版本 ↔ lock）· 字符串 `via` **不再归因父包** · `AbortSignal.timeout` · **`licenses.md` 版本交叉校验**（`audit-delivery.mjs:119-120`）。
  - **守卫自测**：`node tools/guard-selftest.mjs` **13/13 通过**，负例与报告点名的复现例 **1:1 对应**（单引号 / `../` / manifest 图标 / `docs/` 混入 / `srcset` / 字符串 via / 缺包 / vendor 清单漂移）。
  - ⚠️ **残余（登记未做，P3·XS）**：`licenses.md` **漂移必红**那条代码路径**当时没有负例断言**（其余 13 条都有）→ 待补一条负例（改一份临时 licenses 文本 → 断言必红），补进 `guard-selftest.mjs`。
- **S1 断言（A4.5，纯加断言）**：新增组 S **S1-1..S1-4** → **4/4 绿**：XLSX 表头 3 列 + 数据行 1 列 → **短行按最大列宽补空**；最宽行在中间（2/4/1）→ **全表含分隔行均 4 列**；HTML 不等长行同规则；**负对照「不截断」**（最宽行末列内容必须仍在）⇒ **A4.5 闭环**（零代码改动）。
- **R9-1 先红用例**：新增契约组 **Z**（组号避开 **Y** = 大文件专项预留）→ 实测 **Z1/Z2/Z3 红 + Z4 负对照绿**：
  - Z1 `convert()` 在 `arrayBuffer()` **异步失败时抛异常**（应 resolve 成 `{error}` 并带底层原因）→ **红**
  - Z2 同步 `throw` 版 → **红**
  - Z3 **真实 UI 路径**（`setInputFiles`：坏文件在前 + 好文件在后）→ **状态栏停在「正在处理」、好文件未产出** → **红**
  - Z4 **负对照**（只喂好文件）→ 状态栏正常收尾 → **绿** ⇒ 证明三红是**真缺陷**、不是环境假红
  - ⇒ **`npm test` 自本批起在组 Z 上红，直到 R9-1 实现落地**（既定「先红 → 实现」流程；不触及产物与线上）。
- **门禁（本会话实测）**：`npm run lint` **0 错 0 警** · `npm run metrics` **超限 0**（19 文件 / 415 函数）。
  - ⚠️ **重复率本轮 N/A**：jscpd 需 spawn 子进程 → 会话沙箱 EPERM；工具按设计写「不可用」，会**把已记录的 0.4% 降级** ⇒ **已 `git checkout -- docs/CODE-METRICS.md` 还原**（不提交降级记录），请在**终端或 Linux** 重跑 `npm run metrics` 刷新。**这正是 §3.5 分派规则 3 的实例。**
- **环境约束（已写进 §3.5 能力矩阵）**：会话沙箱内 **playwright 与 `node --test` 均 EPERM**；本批两组测试是**经用户一次性升权（`danger-full-access`）**才跑起来（Windows 本机 playwright chromium 未安装 → 实际回退 **msedge**）。

**A11.11 Linux 侧 S1/Z 独立验收（平台分工首次实践闭环）+ B 方案落地（2026-09-18）**
- **派单与回箱**：`.私档/linux/任务-S1Z独立验收/`（交接单 + 开单通知 + 仅含 `tests/` 的补丁）→ 回箱 `.私档/linux/20260918-S1Z验收/`（报告 + 回执 + `summary.json` + 脚本 + **29 份原始日志含 6 份 TAP**）。
- ✅ **Windows 侧逐文件核验**：**33/33 字节 + SHA256 全对**（脚本 `.tmp/lan-check/verify-receipt.mjs`，已存档）。
  - ⚠️ **首跑报 3 处"不符"，是我的解析脚本 bug**：正则把 64 位哈希串**里面的数字**当成了字节数（`7cd13079fa…`→13079、`26b2a4959598…`→4,959,598、`6f96bd25088f…`→25088）—— **交件本身零错误**；修脚本（先剔哈希再找字节）后 33/33 全对。**教训**：核验工具自己也要能被证伪。
- **验收结论（Linux 侧 · 真 chromium · 11/11 PASS）**：**S1 4/4 绿** · **Z 三红一绿**且三条红**逐字命中探针词**（`IO 读取失败（R9-1 注入）`/`同步 IO 失败（R9-1 注入）`/`状态栏停在「正在处理」`）· **反向探针成立**（对 `finished` 取值敏感 ⇒ 断言非恒真）· lint **0/0** · metrics 超限 **0** + **重复率 0.4%**（刷新了我方会话沙箱测不出的 N/A）· pwa-audit **48/48** · verify:ocr **PASS(93)** · 终态 `git status` **0 行**、main 指针未动。
- **B 方案落地**：Z1/Z2/Z3 加 `{ todo: 'R9-1 未实现（先红用例）→ 实现时摘掉本标记' }` ⇒ **CI 恢复绿**。本机先做过机制实测：标 todo 的失败 → **`exit 0`**、报告 `pass 3 / fail 0 / todo 3`；且**标子测试后父组壳不再计 fail**（两个位置都有效，选了标子测试）。
  - ✅ **CI 实证（2026-09-18）**：`tests` 工作流 run **`35245373094`** = **success / 1m25s**（对比：加 todo 前的 `35244299600` = failure / 1m31s，失败步 = **第 12 步 Contract tests**）。⇒ **B 方案端到端成立**（断言一字未改，CI 由红转绿）。
  - ⚠️ **强制记录：实现 R9-1 时必须摘掉这三处 todo 标记**，否则红用例永久静默（已同步写进 A8 的 R9-1 行）。
- **Linux 侧两条方法论发现（纳入我方习惯）**：
  1. **`node --test --test-name-pattern` 只匹配【顶层测试名】** —— 拿子用例名（如 `Z4`）去跑会**整组跳过**，却显示 `✔ 文件 (71ms) / pass 1 / fail 0` ⇒ **假通过**（他们据此推翻了自己前两轮的矛盾结论）。**判别法**：pattern 单跑若 `tests == 1` 且耗时 < 1 s ⇒ **未真跑，结果作废**。
  2. **期望值口径必须含「组壳」**：本单我写「fail 恰好 = 3」，实测 **4** —— 差的 1 条是**组 Z 组壳**（子测试失败 ⇒ 父测试同样计 fail）。**今后出单：期望 fail 一律写「子用例 + 组壳」**（S4 批先例同此）。
- **新登记（P3 · 测试稳定性）**：**组 C 的 `image（sample.png）` 偶发超时**（`10194 ms ≥ 5000 ms` 冷启动豁免窗口，1 次；控制变量实验 **B1 红 / B2 绿** ⇒ **flaky，非本批引入**）。Linux 建议放宽阈值或加预热，但**不改断言**。✅ **已拍板（2026-09-18 用户）：「随条目 8 那批顺手做」** ⇒ 已挂进 **§8 第 1 条**（本批本来就要碰性能/门禁）。
- **U1 关闭**：交接单抬头写的 `da910b1` 当时未 push ⇒ Linux 无法交叉验证补丁与该提交一致；该提交**已随本批推送**，且补丁由 `git format-patch -1 da910b1 -- tests/…` 直接生成 ⇒ **U1 关闭**。

**A13 加载性能专项（2026-09-18 用户提出：「只讨论过内存和文件大小，没讨论过加载速度」）**
- **性质**：**新轴** —— 此前只量过**内存**（PSS 峰值 12.6 GiB）与**文件体积**（50MB 护栏 / 47MB 夹具）；**从未量过"加载"**。⚠️ **口径待拍板**（见末尾）。
- **只读盘点基线（2026-09-18，字节数，尚未有实测时序）**：
  | 分账 | 组成 | 字节 |
  |---|---|---|
  | **首载·应用外壳** | `index.html` 121,844 + `icons` 10,014 + `manifest.json` 730 | **≈ 0.13 MB** |
  | **首载·预缓存（SW install）** | `mammoth` 637K + `pdfjs.pdf.min` 320K + `pdfjs.pdf.worker` **1,087K** + `read-excel-file` ? + `tesseract.tesseract.min` 63K + `tesseract.worker` 111K | **≈ 2.3 MB** |
  | **按需·首次 OCR 冷启动** | tesseract core（simd 优先）3,955K + `langs/eng` **5,199K** + `langs/chi_sim` **2,471K** | **≈ 11.6 MB** |
  | **按需·首次 CID PDF** | `vendor/cmaps/` 168 × `.bcmap` | ≈ 1.1 MB |
  | **疑似死重** | `vendor/tessdata/*.gz`（4.67 MB，**`src/` 零引用**）· `vendor/tesseract-core-lstm.wasm.js`（3.95 MB，非 simd 回退） | ≈ 8.6 MB |
  - ✅ **架构现状（好消息）**：`sw.js` v4 已做**分段缓存**——`PRECACHE` 只含外壳 + 6 个通用库；tesseract core / `langs/` / `cmaps/` 全走**运行时 cache-first**（文件内已写明"预缓存 2 个 core ≈8MB 浪费"）。⇒ **首载并不背那 15 MB**。
- **候选优化（按性价比，均需先测后改）**：
  1. **OCR 语言包改走 gzip**：`src/ocr.js` 现为 `langPath:'./langs/'` + **`gzip:false`** ⇒ 首次 OCR 语言包下 **7.67 MB**；而 `vendor/tessdata/*.gz`（**已压缩、现成资产**）仅 **4.67 MB** ⇒ **−39%**（首次 OCR 合计 ≈11.6 → ≈8.6 MB，−26%）。改动≈2 行，但牵动：`match('./langs/…')` 的"已缓存"提示逻辑、白名单/守卫、`sw.js` 注释与 **H3 契约**（锁 `CACHE_NAME`）。
  2. **资产归一**（二选一，配合 ①）：`langs/` 与 `vendor/tessdata/` **是同一批语言包的两种形态**，交付体积可 −4.67 或 −7.67 MB。
  3. **取舍项**：`pdfjs.pdf.worker`（1.09 MB）移出 `PRECACHE` ⇒ **首载 −1.09 MB**，代价 = **首次 PDF 多等一次网络**（需拍板）。
  4. 首屏本身已轻（≈0.13 MB 外壳），**先测再谈**，不为优化而优化。
- ⚠️ **测量缺口（第一交付物）**：以上**全是字节推算，零实测时序**。⇒ 本线第一步 = **测量脚本 + 基线**（推荐 **Linux 侧真 chromium + CDP**：首载 TTI / SW 冷热 / 首次各格式的 fetch→init→recognize 分段耗时），产出可复算数字后再谈优化。**平台分工**：测量 Linux · 实现 Windows · 真机弱网体感 手机。
- **待拍板**：① **口径** = A 首屏 / B 首次转换冷启动 / **C 两者分账**（我建议 **C，但先做 B**：B 才是用户真正"等"的地方，且我们已有大量转换耗时数据可对照）；② 是否**先补测量基线**再动手（我建议：是）；③ 与 **条目 8（P1 流式）** 的关系：同批做（都碰 vendor 资产 / 性能门禁）还是独立排期；④ 候选 3 的取舍（首载 vs 首次 PDF）。

**A11.12 手机侧「流式调研 + backlog 登记」交件核验（2026-09-18）**
- **收件**：`delivery-20260918-流式调研与backlog登记.bundle`（**612,657 B / SHA256 `C0DABDB661D5FACC…`**，12:25:48 上传）→ 移出收件箱 → 归档 `.私档/验证/手机实测-20260918-流式调研/`；**独立核验 2/2 逐文件字节 + SHA256 全对**（清单外 `README-包内说明.md` 为自指）；**已并入交换仓**（三方合并 **`8d9c197`**，双父 `37c86b9` + `0d1e986`）。
  - ⚠️ 他们**仍未拉取**我方合并（新提交父节点 = `fcdc0c5`）⇒ 历史持续分叉、每次由我方合并；下轮任务单里再提醒一次"开工前先拉 r*"。
- ⭐ **第一篇《现成方案调研-20260918-流式xlsx.md》= 条目 8 的上游调研**：
  - **结论：不引新依赖** —— `fflate 0.7.5`（MIT）**已内联在 `index.html`**，流式 API 齐全（`Unzip`/`UnzipInflate`/`AsyncUnzipInflate`/`UnzipPassThrough`/`Inflate` 实测命中）；且现有代码**已用**原生 `DecompressionStream('deflate-raw')`（`src/xlsx.js:48-54`）。
  - ⭐ **关键洞察（改写实现路径）**：**`XLSX_STRINGS_GUARD_BYTES = 4 MB` 这条"内存保护"恰好把两个会爆的夹具推回最吃内存的 `read-excel-file` 路径** —— 实测解压量：`4-mid-large` 的 sharedStrings **213.9 MB**、`3-big` **87.4 MB**；**sheet XML 更达 204 MB / 291 MB** ⇒ 这是"小文件没事、大文件炸"的**直接触发器**（我方此前把"回退路径加止损"列为候选之一，他们证明**护栏自身就是触发器**）。
  - **可复用基建（t33 遗产）**：中央目录 + `DecompressionStream` ✓ · `zipEntry()` 不解压即得 `compSize` ✓ · `scanSheetRows(xml, limit)` **逻辑上已"够行即停"**（只差物理上停）✓ · 两趟顺序（扫表 → `maxS` → 解字符串）✓ · 截断口径/文案/`backend` 上报 ✓。
  - **三处改动（均在 `src/xlsx.js`）**：① 整条目 `arrayBuffer()` → 持 `reader` 逐块；② 整串 `decode` → 增量 decode（`{stream:true}`）+ 跨块续接缓冲；③ **撤销/降级 4 MB 护栏**。
  - **验收口径建议**：峰值应为 **O(窗口) 而非 O(文件)**（修前基线在手：35.9 MB → 11.24 GB；PC 侧独立核 12.63 GiB/369×；47.4 MB → 11 秒冲到 5 GB 后被 SIGKILL；286.8 s 只换 1000 行）。
  - 外部候选逐个给了判断：fflate 首选（已在内）· zip.js 过度 · **SheetJS `sheetRows` 口径参考价值最高**（但整包替换、且**推断**浏览器端仍整体解压）· xlsx-wasm-parser 属**速度**路线非内存 · openpyxl `read_only` 设计参考 · `read-excel-file` 上游已知大文件问题（#38 堆栈溢出 / #69 无行数上限）。
  - **5 条未验证如实登记**（含"新增库许可未查证 —— 因结论是不新增"）。
- ⭐ **第二篇《backlog-登记-20260918-PDF版面与按需模型.md》= 提案 B1–B5**（缘起：用户 2026-09-18 提出「浏览器端量化模型 + 网页上『高级识别（AI）』按需加载」，且「越来越觉得流式重要」）：
  - **B1 分档 + 载体划界**：① **启发式增强（0 MB）先把这档做扎实**（"不少表格需求卡在几何法没做完，**不是缺模型**"）② **小模型 int8（3–15 MB）页面分割/阅读顺序 = Web 现实档**（WebGPU 优先、WASM+SIMD 兜底）③ 中大模型（50–400 MB）表格单元格结构 → **归 APK 线**。
  - **B2「按需加载模型」机制（先立机制、后选模型；六条）**：体积明示 · **显式同意（opt-in）** · 离线缓存 + 版本可失效 · **硬内存上限 + 主动降级**（不崩） · 逐页/逐块流式 · 失败优雅降级回启发式。
  - **B3 流式由「效率项」升格为「可行性前提」**：把峰值从 **O(文件) → O(窗口)** = **把不可控变量（用户手机剩多少内存）变成可控（我们选的窗口）**；且是「真进度/可取消」（无 Worker+流式则动画也冻死，实测心跳 0/584）与「按需 AI 模型」的**共同地基**。
  - **B4 量化验证点清单**（启发式基线正确率 / 体积 / PSS 峰值 / 三态延迟 / 失败模式 / 热）。
  - **三条硬约束**：内存天花板（已实测）· ⚠️ **`file://` × 多线程 = 推断未实测**（WASM 多线程需 `SharedArrayBuffer` → 需 COOP/COEP → `file://` 下拿不到 ⇒ 大概率单线程推理；**红线 #2 要求双击即用**，须先拿最小样例实测）· ⚠️ **许可未查证**（版面/表格方向权重不少是 CC-BY-NC/研究用途 ⇒ **选型第一步是过 `docs/licenses.md`，不是先跑 benchmark**）。
  - **§7 边界**：**不改变**「PDF 版面结构还原线不启动」（§8-E18）；建议 **B3 流式与 A7.3-#8/#9 合并推进**；**A9/A10 的"预检 + 提示"可先落地**；**A.3/A.4**（PDF 扩展 B 区 CJK 代理对 / xlsx `<rPh>` 注音）属**转换正确性**、仍属主线，**不随本登记冷藏**。
  - **4 条未验证如实登记**（模型体积↔能力为行业估算 / 权重许可未查证 / `file://` 组合未实测 / 启发式基线正确率未测）。
- **与 A13（加载性能线）的关系**：**B2 的"按需加载模型"机制与我方 A13 的「OCR 语言按需 + 质量升级重试」是同一模式** ⇒ 建议两条线**共用一套"按需加载"口径**（体积明示 / 显式同意 / 离线缓存+版本 / 硬上限 / 优雅降级）。
- ✅ **已拍板（2026-09-18 用户）**：① **B1 采纳**（分档 + 载体划界：Web 只做轻量档、表格重模型归 APK 线）；② **B2 采纳**（六条机制**先在设计层定**）；③ **B3 采纳，并入第一批**（流式 = 可行性前提）；④ **B4 采纳**（量化验证点清单）；⑤ **4 MB 护栏**：**顺序约束写进任务书**（**先流式化、后撤/降级护栏；同批；中间态不得单独上线**）；⑥ **A9/A10「预检 + 提示」排第一批**。
- ⭐ **阈值口径（2026-09-18 用户拍板）**：**护栏 / 预检 / 提示的阈值一律按【解压后规模】算**。
  - **现行实现（已核实）**：`src/xlsx.js:489` 比的是 **压缩后**尺寸 —— `stringsEntry.compSize > XLSX_STRINGS_GUARD_BYTES`，而 `compSize` 取自 ZIP 中央目录 **offset+20**（`src/xlsx.js:37`）。
  - **新口径的可行性**：中央目录 **offset+24 = uncompressed size**，**同样不需要解压即可取** ⇒ `zipEntry()` 多返回一个字段 + 比较处换字段，**实现成本≈1 行**。
  - **为什么必须换**：同一份 sharedStrings 实测 **压缩 16.4 MB / 解压 213.9 MB（≈13×）** —— 按压缩尺寸判定的语义严重失真（"4 MB 护栏"实际放行/拦截的是完全不同的量级）。
  - ⚠️ 实现时注意：ZIP64 条目（uncompressed ≥ 4 GiB）的字段语义与普通条目不同 —— 本产品有 50 MB 文件护栏在上游，**不构成本案路径**，但要在注释里写明假设。

**A11.13 R9-1 实现落地 + 门禁 + Linux 验收单已开（2026-09-18）**
- **提交**：**`0b2d1d5`**（feat：`readAndSniff` 读文件/嗅探失败**不抛** + `runOrExplain` 抽出；`handleFiles` → `processOne` **逐文件隔离** + 状态栏**成功/失败计数** + `safeHandleFiles` **两入口兜底**；**产物重建 123,207 B / `7368D23118F3E96F…`**）· **`ef2bbb7`**（test：**摘掉组 Z 三处 `todo` 标记**，三条恢复真阻断；**断言一字未改**）。
- **门禁（本机实测）**：`node --check` 三文件过 · `npm run lint` **0 错 0 警** · `npm run metrics` **超限 0**（19 文件 / 420 函数）。
  - ⭐ **记一次"门禁有效"的实例**：metrics **当场拦下本次改动**（`handleFiles` cyc=11、`convert` cyc=13，上限 10）⇒ 按**重构配额**抽出 `processOne` / `readAndSniff` / `runOrExplain` 三个函数后回到线内（无断言改动、无行为改动）。
- ⚠️ **`npm run build` 会话沙箱跑不了**（esbuild `spawn EPERM`）⇒ 由**用户终端**执行（实测输出 `[build] ok — index.html 122115 chars`）；这是 §3.5 规则 3 的又一实例。
- **Linux 独立验收单已开且补投齐全**：`.私档/linux/任务-R9-1独立验收/` = 交接单 + 开单通知 + `patches/0001…`(9,948 B) + `patches/0002…`(4,531 B) + 补件（提交号 / 产物哈希 / 门禁对照）。验收要点：**组 Z 三条由红转绿**（组壳也应消失）· **组 S1 保持 4/4 绿**（防回归）· **跨平台产物一致性**（Linux `npm run build` 出的 index.html 必须与 `7368D231…` 逐字节相同）· **负对照必红**（临时去掉 `readAndSniff` 的 try/catch ⇒ Z1/Z2 必红，随后完整还原）。
- **待办**：Linux 回箱 → Windows 侧逐文件核验（§10 纪律）→ **通过后再推**（`0b2d1d5` + `ef2bbb7` 现挂在本地，**先别推**，避免 main 出现红）。

**A11.14 T3 补采交件核验 + 第一批 5 条拍板 + 阈值依据复核（2026-09-18）**
- **T3 交件**：`delivery-20260918-T3补采.bundle`（677,765 B / SHA256 `95577390ED51800F…`，12:58 上传）→ 归档 `.私档/验证/手机实测-20260918-T3补采/`；**独立核验 11/11 逐文件字节 + SHA256 全对**；已并入交换仓（`cca4950`）。
- **T3 结论（冷态 · 成功）**：`4-mid-large`（35.9 MB / 25 万行）**≈45 s** · 渲染进程峰值 **≥7.50 GiB（≈219× 文件）** · 平台期 ≈5.04 GiB 而 **swap 1.06 → 4.51 GB 仍在涨** · 产物 **339,797 B / `013c1706…` 与上一轮逐字节相同**；全程 **Thermal 0**、SKIN ≤36.2 °C、未充电、起始 `MemAvailable` 6.07 GB。
  - 与上轮（热 + 内存紧张）对照：**286.8 s / ≥11.24 GB** ⇒ 同档同文件 **≈6× 差**。⚠️ **两条口径**：条件不同**不可直接比**；**两轮峰值都是下界**（`gap=3s` 会漏尖峰）。
  - ⭐ **新洞察**：**"PSS 平稳 ≠ 系统没在受累"**（平台期 PSS 不动、swap 猛涨）⇒ 评估内存影响**不能只看 PSS**。
  - **方法教训（两线共用）**：**逐条 `>>` 追加的日志不能在运行中改名** —— shell 每条命令按路径重开文件 ⇒ 曲线被切成两段；要么先停采样再改名，要么脚本里 `exec >> "$LOG"` 一次性持有 fd。
- **第一批 5 条拍板（用户 2026-09-18）**：① **Y2 白盒为主 + Y3 黑盒兜底** ✅；② 阈值（8 MB / 5 s / 200 MB）**"先给依据，有实测支撑才定"**（见下）；③ **D5：② 预检 + 二次确认先行、① 按需解析随后** ✅；④ **允许加 `meta.scan`，但限定为内部 meta —— 绝不写进用户下载的 `.md`**（只用于测试诊断）；⑤ 验收环境 = **桌面 headless + 手机 PSS 对照**（沿用既有档位）✅。
- ⚠️ **阈值依据复核（逐条给出处，不凭印象）**：
  - **8 MB** —— 出处 = 设计稿 §2 注：**1000 行 × 行均 ~390 B ≈ 0.4 MB，留 20× 余量**。行均可由现成产物复核：**339,797 B ÷ 1000 = 339.8 B/行**（≈0.34 MB）⇒ 数量级成立；**"20×"是选定的安全余量，不是实测**。
  - **5 s** —— 同注：按"与 8 MB 相称"给；另引既有数据点（快路径 5 万行 = 31 ms）与既有门槛 **`real-big < 3 s`（L4a）**作锚 ⇒ **5 s 是新定的较松上界**，非直接实测。
  - **200 MB** —— ⚠️ **设计稿未给任何推导，属判断性上界**。⇒ 按用户要求**拿到实测前不定稿**：先红阶段补一步**桌面峰值实测**（四档 154 KB / 773 KB / 35.9 MB / 47.4 MB，`performance.measureUserAgentSpecificMemory()`），用小档峰值当"地板"，据此校准上界（并检查 200 MB 会不会**被小档就撞破**）。
- **D4 口径更新（据 T3）**：手机侧判据改用 **`O(窗口)` 跨四档 + 条件标注（冷态）**，**不写死单一降幅百分比**（12.63 GiB 是热态、7.50 GiB 是冷态 ⇒ 只记单个数会误导）。

**A11.15 Linux 侧 R9-1 独立验收：9/9 全 PASS（2026-09-18）**
- **回箱**：`.私档/linux/20260918-R9-1验收/`（报告 + 回执 + `summary.json` + 脚本 + **19 份原始日志含 3 份 TAP**）。
- **九项结论（全 PASS）**：① **组 Z 三条由红转绿**（Z1–Z4 全 ok · 组壳 **`ok 38`**，上批为 `not ok 38`）② **组 S1 4/4 未回归** ③ **全量契约 272 tests / 270 pass / 0 fail / 2 skip**，退出码 0，**`# todo 0`** ④ ⭐ **跨平台产物一致性**：`123,207 B / 7368D231…5E03` —— 其 build 前后一致、**与我方提交逐字节相同** ⑤ lint **0/0** ⑥ metrics **超限 0** + **重复率 0.4%**（刷新我方会话沙箱测不出的 N/A）⑦ pwa-audit **48/48** ⑧ verify:ocr **PASS(93)** ⑨ **负对照必红**：去掉 `readAndSniff` 的 `try/catch` ⇒ **Z1/Z2 红**（组 Z `pass 2 / fail 3`），**还原后 5/5 复原**。
- **关键情报**：补件给的三个提交**均未 push**（我方刻意持有）⇒ Linux 侧走**补丁路径**完成验收（`--check` 通过 + cleanly 应用）；其登记 U1/U2（补丁 ≠ 提交无法交叉验证、基线 `9558e72` 不在远端），旁证有利。
- ⇒ **推送闸门解除**：`9558e72 → d857403`（5 个提交）可推；推后 CI 应绿（组 Z 已转绿 + `todo 0`）。
- **另收手机侧新交付**：`delivery-20260918-T1第三轮方案.bundle`（**667,509 B**，13:04）→ 归档 `.私档/验证/手机实测-20260918-T1第三轮方案/`；**待读**（性质是"**方案**"而非结果）。

**A11.16 手机侧 T1 第三轮「浏览器 A/B 对照」方案：已读 + 已并入交换仓（2026-09-18）**
- **交件**：`deliveries/20260918-T1第三轮方案/` = `方案-T1第三轮-浏览器AB对照.md`（**7,520 B**）+ `README-包内说明.md`（2,624 B）+ `SHA256SUMS.txt`（112 B）。
- **核验**：`SHA256SUMS` **1/1 通过**（README 自指，不计）；交换仓合并后 **3/3 文件 blob 与手机交付逐字节相同**（`386cf4bc…` / `43a151eb…` / `661f66a7…`）。
  - **合并**：交换仓 `cca4950` + 手机 `e2ae51b`（base `08b1237`）→ **`d20051a`**（plumbing：`read-tree -m` + `commit-tree` + `update-ref`）。
  - ⚠️ **新踩坑（对象搬运）**：`git fetch` 又一次死在 `cannot create standard input pipe for git-upload-pack … Permission denied`（沙箱）；**可行替代 = 直接搬 pack 文件**（本仓缺的正是 `pack-a82deab0…`，`idx`+`pack`+`rev` 三件一起拷进 `objects/pack/` ⇒ 对象即刻可用，内容寻址天然幂等）。**别再试 `fetch`/`unbundle`。**
- **方案要点（手机侧提案，性质 = 提案不是结果）**：同文件（`3-big` 47.4 MB）× **两个浏览器**（夸克 `com.quark.browser` vs 默认 `com.vivo.browser`）各跑一轮，**唯一自变量 = 浏览器** —— 利用「**系统侧规则相同、主机侧策略不同**」把 T1 卡住的「**主机 vs 系统**」分开（夸克有**自有 JS 看门狗**：实测调用栈 `JsAnrThreadNotRespondingCallback.showDestroyCurrentWindowDialog`；默认浏览器是 stock Chromium ⇒ 推断走 `OomIntervention`）。
- ⚠️ **实施前必须先改（否则采不到）**：包名过滤 `com.quark.browser` → **`com.quark.browser|com.vivo.browser`**，**三条通路都要改**（采样器 / 提取 / `am_kill` 过滤）；默认浏览器渲染进程名（实测）= `com.vivo.browser:sandboxed_process0:org.chromium.content.app.SandboxedProcessService0:0` ⇒ **不改 = 采样器"看不见"渲染进程**（与老坑"没取到 ≠ 不存在"同源）。
- **三条待拍板（等人签字，未动）**：① **是否采用 A/B 设计**（而非再跑一轮夸克，手机侧建议采用）② **档位**——T1 需**全量 logcat** ⇒ 走 §5.6 **v1.1 口径**，**现档位已在 T3 结束后收回只读档，需重新拍板 + 用户本人重新配对**（换网/重启即失效）③ **轮次与顺序**——建议**两轮封顶、先默认浏览器后夸克**；**第一轮即命中判据 1/2 则第二轮可省**（当场停）。
- **判读表已"写死不许临场改"**（含：任一出现 `killinfo` 首字段=渲染进程 pid ⇒ **sender = lmkd 直接定案**；任一出现 `am_kill [… low memory …]` ⇒ **sender = AMS 直接定案**；两轮都没触发 ⇒ **如实写"未复现"**）。
- **成本/风险（手机侧自评）**：每轮 ≈12 分钟 + 收尾 ⇒ 共 **≈30–40 分钟**；一次明显热负荷（上轮实测为保前台杀 **60+ 其他应用**、`Thermal Status` 冲到 **4（CRITICAL）**、SKIN **46.2 °C**）；缓解 = 冷态起跑 + 轮间歇 + 命中即停。
- **如实登记未验证（4 条）**：默认浏览器"更可能留宿主日志"是**推断**；`OomIntervention` 阈值未查证；两浏览器渲染进程内存模型差异未量化（**峰值基线需重建**）；与 20:57 那次 SIGKILL（夸克渲染进程 ≈5 GB）**不可直接相减/相比**。
- ⚠️ **手机侧提请的红线提醒**：压力**必须来自大文件** —— 用 `2-mid`（773 KB）这类"本就能成功"的档**不构成压力、无诊断价值**。

**A11.17 手机侧「内核日志通路复查」+ T1 方案 v2：已收件/已核/已并入 + 我方审查 5 条（2026-09-18）**
- **交件**：`delivery-20260918-内核通路复查.bundle`（**821,880 B / `7CE88B7D6D7CF71E…`**，13:25 上传）→ 归档 `.私档/验证/手机实测-20260918-内核通路复查/`。
  - 内容 = `deliveries/20260918-内核通路复查/`（10 文件：复查报告 9,905 B · 方案 v2 9,363 B · 探针结果 ×2 · 线索 ×2 · 脚本 ×2 · README · SUMS）。该提交**只新增这 10 个文件**（`--stat` 实测 755 insertions），**旧文件零改动**。
- **核验**：`SHA256SUMS` **8/8 通过** ✅。**合并**：交换仓 `d20051a` + 手机 `aec0d9d`（base `e2ae51b`）→ **`0a5b4dc`**；两侧 `deliveries/20260918-内核通路复查` **子树哈希恒等**（`4b0cf895f801ce46…`）= 整目录逐字节相同。
- ⚠️ **记一次自己踩的坑（CRLF 检出）**：首次核验 **8/8 全"字节不符"**，差值**恰好等于各自行数** ⇒ 不是手机的问题，是**我漏了 `-c core.autocrlf=false`**（`checkout-index` 在 `core.autocrlf=true` 下把 LF 写成了 CRLF）。**收 bundle 的标准提取配方**（记牢，别再漏）：
  `git -c safe.directory=* --git-dir=<bare> --work-tree=<dir> read-tree <sha>` → `git -c safe.directory=* -c core.autocrlf=false --git-dir=<bare> --work-tree=<dir> checkout-index -a -f`
  （**顺带旁证**：`T1第三轮方案/` 三个文件与上一轮**逐字节相同**——CRLF 差值 123/50/1 恰好等于行数，反证内容未变。）
- **核心新得（手机侧实测）**：① ⭐⭐ **`dumpstate` 能读 dmesg**（跑在 `u:r:dumpstate:s0`，不受 shell 的 SELinux 限制）—— 22:21 那份 bugreport 的 `KERNEL LOG` 段有 **31,360 行**，而同机 shell `dmesg` = **Permission denied** ⇒ **「定时 bugreport」是本机唯一不用 root 的内核日志通路** ② **`logcat -b kernel` 恒空的根因 = `[ro.logd.kernel]: [false]`**（logd 根本没读 `/dev/kmsg`，**是配置不是权限**）⇒ **"kernel 缓冲空" 不能当 "内核没动作" 的证据**（推理陷阱，值得记）③ **pstore/LAST KMSG 是策略级封死**：`avc: denied { search } dev="pstore"`，**连 incidentd 与 logd 自己都打不开** ④ **tracefs 形式活着、实质不通**：`trace` 可读但 2,497 条目**全是 ROM 自己 atrace 的 `tracing_mark_write`**，8 个内存关键词**全 0**，`tracing_on=0`，且**写 tracefs 属系统配置写面（黑名单）** ⑤ `persist.sys.lmk.reportkills = true` ⇒ **lmkd kill 上报是开着的**（加强"`killinfo` = lmkd 杀记录指纹"这条更正） ⑥ 内核 **6.12**；`/proc/zoneinfo` 可读（PSI 被挡后的替代内存压力证据）。
- **方案 v2 的唯一改动** = 新增 **§4.5：崩溃后 60 秒内抓 bugreport**（与 logcat **互补**：logcat 给"谁下的手(tag)"，bugreport 给"内核层有没有记录"）；**时序要求** = 先确认 logcat 全量采集仍在跑，**再**去抓。
- ⭐ **我方审查 5 条（准备回执给手机侧；其中第 1 条是决定性的）**：
  1. **★★ 缺一个决定性数字：「从 bugreport 开始生成」到「`KERNEL LOG` 段真正被读」的延迟。** 79 秒是**保留窗口（往回看多远）**，不是**提前量** ⇒ 可行性条件其实是 **`KERNEL LOG` 段被读的时刻 ≤ 崩溃时刻 + 79 秒**。用户"60 秒内点"只留了约 19 秒余量，而生成全程要 **1–3 分钟** ⇒ **若该段在 dump 后段才读，这个方案根本采不到**。**可查且便宜**：§3 自己就引到了 `D incidentd: Start incident report section 2007 …` ⇒ **bugreport 内部日志带逐段起始时间戳**，拿现成 bugreport 就能量出来。**这一个数决定 §4.5 是能执行还是空想。**
  2. **窗口在高负载下会自我缩短**：≈400 行/秒 ⇒ 31,360 行 = 79 秒，但**内存风暴时 printk 速率暴涨，同一个行数上限覆盖的时间会远小于 79 秒** —— 而我们要抓的正是风暴时刻 ⇒ **79 秒不能当承诺值**。
  3. **判读表要补一行**：§4.5 引进了新通路，但 §3 那张"**写死、不许临场改**"的表**没有对应判读**（现有行只覆盖"宿主侧日志"）。须**开跑前**补：**内核段出现 lmkd 杀记录指向渲染进程 pid ⇒ sender = lmkd，且构成判据 1 的独立第二证据源**。
  4. **取证动作本身会扰动被测量**：bugreport 生成 1–3 分钟且发热 ⇒ 期间的 **PSS 曲线 / thermal 数据必须打标记（或暂停采样）**；且 bugreport 原件 **≈27 MB、含全机信息** ⇒ 口径写死为**只回传抽出的 `KERNEL LOG` 段 + 抓取命令，原件不出设备**（与 A11.16 的全量 logcat 隐私口径同源）。
  5. **v2 未涵盖我上一轮提的「顺序即变量」**（v2 只改 §4.5；我的意见还没回执过去）。
- ⚠️ **「文档漂移」是我判错了（当场自我更正）**：我在手机侧树里读到 `README-协作协议.md` 还是 **`git daemon` :9418 + pull/push、"认证：无"** 的旧版，一度判为"待更正的过期文档"。**实测三处 blob**：我方 main（`0a5b4dc`）= `611bea69…` = **`2364444`（"docs: 协作协议更新为 bundle+HTTP"）那一版**；手机侧（`aec0d9d`）= `6f8f924e…` = **旧版**。
  ⇒ **结论：我方文件早已更正，漂移的是手机侧那份没 pull 的克隆**；**无需改文档**，手机侧下次 ff 合并（`0a5b4dc` 含 `aec0d9d` 为祖先 ⇒ 可 ff）即自动同步。**教训：读对方树里的文件时，先确认"这是谁的版本"，别把对方的旧克隆当成我方的旧文档。**

**A11.18 复核回执已写并已投递（2026-09-18）**
- **落盘**：交换仓 `orders/20260918-内核通路复查/复核回执.md` —— **10,658 B / SHA256 `46B95C6BBFE7AF5ECC2B96EA5804C8226886ABD8C2980FA3CC787B1F92183E43`**（141 行），提交 **`2918c3c`**，blob **`f33fc7ab85760250b0243045e1cdeef667ebf1e2`**（交换仓 main 与 bundle 内**两侧一致**）。
- **投递通道三条**：① 仓内 `orders/…` ② HTTP 页直读副本 `orders-20260918-内核通路复查-复核回执.md` ③ 新 bundle **`orders-20260918-r3.bundle`**（**838,135 B / SHA256 `743A9F69B8D14C645CBCF1AC392065E109A5786088C5A481B2DF0465A5B33D75`**）· ④ 用法页 `手机侧怎么用.txt` 更新为 r3（4,054 B）。
- **通道实测（带口令，端到端）**：`GET /orders-20260918-r3.bundle` → **200 / 838,135 B**（与本地逐字节同长）；回执 md → **200 / 10,658 B**；目录页列出三项 ✅。
  - **bundle 自检**：新开空仓 `bundle unbundle` 成功 ⇒ **自包含**（无前置）；`merge-base --is-ancestor aec0d9d 2918c3c` **退出码 0** ⇒ **手机侧可 ff 合并，不会冲突**。
- **回执内容（五条 + 一条自我更正）**：① **采信 7 条**（dumpstate 通路 / `ro.logd.kernel=false` 是配置 / pstore 策略封死 / tracefs 形式活实质不通 / `reportkills=true` / 内核 6.12 + zoneinfo 可读 / `killinfo=` lmkd 指纹）② ⭐ **唯一实质质疑 = 量 `Δt`**（生成开始 → `KERNEL LOG` 段被读），并**先写死三分支判据**：`≤19 s` 照 v2 走 / `19–79 s` 改动作口径为"立刻点且不超过 `79−Δt` 秒" / `>79 s` **§4.5 不可执行、撤回该步** ③ **判读表开跑前补一行**（内核段命中 lmkd 指向渲染进程 pid ⇒ sender=lmkd 定案，且为**判据 1 的独立第二证据源**；未命中只能记"窗口内未观察到"）④ **79 秒降级为"安静态参考"**（398 行/秒是安静态的；风暴时同一行数上限覆盖的时间远小于 79 秒，而我们要抓的正是风暴）⑤ **两条纪律**：取证期间 PSS/thermal **打标记或暂停采样**、**bugreport 原件（≈27 MB）不出设备，只回传抽出的 `KERNEL LOG` 段 + 命令** ⑥ **「顺序即变量」并入**（给 (a) 起点等温等内存带 + 写进证据 / (b) 记录真实起点按带协变量写；并请对方若认为 (a) 做不到就**回实测等待时长**，我方据此改口径，不硬套）⑦ **自我更正**：把"文档漂移"判错这件事写进回执，避免对方误以为要改文档。
- ⚠️ **本次又踩一次老坑**：`git init --bare … | Out-Null` ⇒ **native 进管道**被拒（`程序'git.exe'运行失败：拒绝访问`），**命令根本没执行**（并因此让随后的 `unbundle` 报 `Need a repository`）。**native 一律独立语句**，已第二次确认。
- **下一步 = 等对方那一个数**（`Δt`）；数到手再定 T1 第三轮开不开（开跑要占用用户动作：档位重拍 + 用户本人重新配对，不白开）。

**A11.19 手机侧 Δt 实测 + T1 方案 v3：已收件/已核/已并入 + 我方审查 4 条（2026-09-18）**
- **交件**：`delivery-20260918-Dt实测与方案v3.bundle`（**834,809 B / SHA256 `5FAD41390230EC30…`**，13:36:40 上传 —— 距我发回执 **约 11 分钟**）→ 归档 `.私档/验证/手机实测-20260918-Dt实测与方案v3/`；内容 = `deliveries/20260918-段延迟实测与方案v3/`（5 文件：段延迟实测 · Δt 原始依据抽取 · 方案 v3 · README · SUMS；`--stat` 实测 471 insertions）。
- **核验**：`SHA256SUMS` **3/3 通过** ✅（清单外仅 README）。**合并**：`2918c3c` + `f05cddd`（base `aec0d9d`）→ **`120deb9`**；子树哈希两侧恒等 **`1b1c230154bca888…`**。
- ⭐⭐ **要的那个数到手：`Δt ≈ 71.5 – 76.0 秒`**（单例：09-17 22:21:44 那份 bugreport）。**落点 = 我方写死的中间分支** ⇒ **原口径「崩溃后 60 秒内点」作废**（余量仅 **3–8 秒**，人做不到）。
  - **两条锚点**：**[C] 内核内锚点**（`audit(1789654973.408)` 自带 epoch，与内核时钟**同在内核日志里，无跨流误差**）⇒ **71.5 s**；**[D] 跨流交叉印证**（logcat `incidentd: Start incident report section 2007` @22:22:57.898）⇒ **76.0 s**。**两锚点差 4.5 s**（成因未查明）⇒ **报区间不报单点**（这个做法我方认可）。
  - **旁证自洽**：主 txt 的 zip 内时间戳 22:24 ⇒ dump 全程 ≈ **2 分 16 秒**，而 Δt ≈ 72–76 s ⇒ **KERNEL LOG 是在 dump 进行到约一半时读的**。
- ⚠️ **对方更正了我的方法（成立，我认）**：我建议"拿 bugreport **内部日志**量"—— 实测**不可行**：`dumpstate_log.txt`（28,402 B）**整份没有时间戳**，只有每段耗时（`Duration of 'X': 0.03s`）。他们改用**内核日志自带的 `audit(epoch)`** 当锚点 —— **比我的建议强**（不跨流对齐，且做了独立第二锚点交叉印证 + 如实交代 4.5 s 分歧）。
- ⭐ **倒序救法（v3 §4.5 重写）**：窗口 =「**读取时刻往回 ~79 秒**」，而读取发生在 **dump 起始 + ≈72–76 s** ⇒ **在 dump 起始后不久发生的崩溃反而落在窗口内**。**新口径**：先点「抓取错误报告」(t=0) → ≤3 s 内点「开始转换」→ **崩溃须发生在 dump 起始后 ≈72 s 内**（上轮崩溃命运约在解析启动后 **15–20 s** ⇒ **余量约 56 s，充足**）；⛔ 崩溃后**不要再点第二次**（会重启 dump，窗口整体后移）。
- **v3 对我方回执六条逐条落地（全 ✅）**：判读表**补两行**（内核段命中 lmkd 指向渲染进程 ⇒ sender=lmkd + **判据 1 独立第二证据源**；内核段无记录 ⇒ **只记"窗口内未观察到"**）· 79 秒全篇改「**安静态下界 / 不可当承诺值**」· 新增 **§4.5.1** 两条纪律（**取证窗口打标记**"数据受扰，不用于峰值比较" / **原件不出设备，只回传抽出的 KERNEL LOG 段**）· §5 补「**顺序即变量**」(a)(b) 两行。
- ⭐ **我方追问的"降温要等多久"也给了实测**：Thermal **4 → 2 用了 ≈17 分钟**（且当时**在充电**）；回到 0 更久（隔夜数据不足以定量）⇒ **"两轮起点同等"不现实** ⇒ **双方同意宽松口径：记录 + 标注，不追求同等**。
- ⭐ **我方审查 4 条（下一份回执的内容）**：
  1. **★★ 每次运行必须从 logcat 的 `incidentd` 行反算「当日 Δt 与窗口边界」并写进证据。** 否则「内核段**无**记录」这条判读**根本无法应用** —— 分不清是"**没杀**"还是"**没盖到**"（Δt 是单例 71.5–76.0，逐次会漂）。这是让**负结果也变得可解释**的那一步。
  2. **⚠️ 倒序把"仪器"压在了被测窗口上，会把结果系统性推向 `lmkd` 假设。** bugreport 自身 1–3 分钟的重负载**正好叠在崩溃时刻** ⇒ 由此产生的杀，可能是**dumpstate 造成的内存压力**所致，**不等于自然路径**。⇒ 建议顺序：**先自然轮（A/B 各一轮，看结局 + logcat 指纹）→ 只在自然轮仍未触发时才启用倒序轮**；若一上来就用倒序，结论里必须写明"**压力含仪器自身**"，且**正结果只证明"lmkd 在该压力下会杀渲染进程"，不能当作自然崩溃的 sender 定案**。
  3. **窗口滑动（v3 缺）**：若崩溃没在 ≈72 s 内到，**允许在崩溃前再点一次 bugreport 重开窗口**（v3 只禁了"崩溃**后**再点"）—— 否则这一轮白烧。要**记次数**。
  4. **先空跑一遍，量「点 bugreport → 点开始转换」的真实间隔**：这是**跨两个 App** 的动作，v3 的"≤3 秒"是**估的**；而这个数决定倒序是否可行，且**很便宜**。
- **下一步 = 用户拍板**：T1 第三轮**开不开**（含**档位重拍**走 v1.1 全量 logcat + **用户本人重新配对**）；若开，按 v3 倒序口径 + 我方 4 条补丁执行。

**A11.20 复核回执 #2 已投递 + 流式先红前置工具就绪（2026-09-18）**
- **用户拍板（2026-09-18）**：① T1 第三轮 → **先发 5 条补丁回执，再开跑** ② 空档 → **推 6 个提交 + 起流式先红提交**。
- **推送状态实测**：`HEAD == origin/main == 410d2d1`（`rev-list --left-right --count` = `0 0`）⇒ **那 6 个提交已经推上去了**（无需再推）。
  - ⚠️ **但远端无法独立核验**：本机 `github.com` / `api.github.com` **解析到非公网 IP**（hosts 现为空），`read_url` 与 `web_fetch` 双双被"非公网 IP"拦下 ⇒ **只能以 remote-tracking ref 为准**（如实登记，不当作已核验）。
  - 此后本地新增提交（待推）：`26efc12`（A11.16–A11.19 落库）· `2353080`（采点工具）· 本条。
- **回执 #2 已落盘并投递**：`orders/20260918-段延迟实测与方案v3/复核回执.md` —— **8,966 B / SHA256 `4CA775B62410C97692A95C786A37FC1C581ECF86B0ADCA28FEC29244E89EB447`**（130 行），提交 **`cf9c1db`**，blob **`35278eb9…`**（交换仓 main 与 bundle 两侧一致）。
  - **五条进 v4**：① ★★ **每次运行反算「当日 `Δt_read` 与窗口宽度 `W`」并写进证据**（否则"内核段无记录"分不清"没杀"与"没盖到"；`W` 直接从段首/段尾两条内核时间戳读出，几乎免费）② ⚠️ **倒序把仪器压在窗口上 ⇒ 结果系统性偏向 `lmkd`** ⇒ 先自然轮、后倒序轮；若直接用倒序，正结果**只能**写成"在 dumpstate 负载下 lmkd 会杀该渲染进程"③ ★ **「≤3 秒内点」改「把崩溃安排在后半段（t≈+30…+60 s）」**（**与上一版方向相反**：崩溃容许区间 = `[Δt_read − W, Δt_read]` ⇒ **W 越小、崩溃必须越晚**；安静态 78.6 s、若 W 塌到 40 s 则区间变 `[+32, +72]`）④ **窗口滑动**：崩溃前可再点一次重开窗口并记次数 ⑤ **先空跑量「点 bugreport → 点开始转换」真实间隔**。
  - **术语更正（我方自身）**：上一条回执把 Δt 写成「段**起始** − dump 开始」，字面代入得 **−7.1 秒（负数，荒谬）**；钉死为 **`Δt_read` = 段**最后一行**内核时刻 − dump 开始** · **`W` = 段最后一行 − 段第一行**（两者独立，别混）。
- **投递通道**：新 bundle **`orders-20260918-r4.bundle`**（**856,539 B / SHA256 `E1A9770FA828F9556D565C7CC3F4502BFED51A92546761D8F64A002BF3B6A162`**）· HTTP 页直读副本 `orders-20260918-段延迟与v3-复核回执.md` · 用法页更新为 **r4**。
  - **通道实测**：新开空仓 `unbundle` 成功 ⇒ **自包含**；`merge-base --is-ancestor f05cddd cf9c1db` **退出码 0** ⇒ **可 ff**；带口令 `GET` → r4 **200 / 856,539 B**、回执 **200 / 8,966 B**。
- ⭐ **流式先红前置工具已就绪**：**`tools/measure-xlsx-peak.mjs`**（提交 **`2353080`**）—— 一次跑出三样：**四档 PSS 峰值**（D4 阈值校准）· **Y1 基线产物**（markdown 字节 + SHA256，落 `.tmp/y-peak/artifacts/`）· **Y2/Y3 现状值**（`meta.scan` 与端到端耗时）。
  - **设计要点**：内存口径用 **Node 侧读 `/proc/<pid>/smaps_rollup` 的 Pss**（**与手机 ADB 同口径**，且**不受被测页面主线程阻塞影响** —— 页内 `setInterval` 在同步解析期间会停摆、采不到尖峰）；Windows 无 `/proc` ⇒ 自动退化并在报告里标明方法不同源。站点用**硬链接**组装（同卷零额外空间），夹具同源 `fetch` 进页面，避免 66 MB base64 过 CDP。
  - **门禁（本机实测）**：`node --check` 过 · `eslint tools/measure-xlsx-peak.mjs` **0 错 0 警** · `npm run metrics` → **20 文件 / 448 函数 / 超限 0**（重复率 N/A：jscpd 在本沙箱 spawn 失败，与既知限制同源）。
  - **四档夹具位置**：`.私档/传输-手机-20260918/`（`1-small_154KB_3054rows.xlsx` 154,597 B · `2-mid_773KB_50001rows.xlsx` 773,494 B · `4-mid-large_35.9MB_250000rows.xlsx` 35,856,644 B · `3-big_47.4MB_436000rows.xlsx` 49,716,718 B）。
  - ⚠️ **本沙箱不能 spawn 浏览器**（EPERM，§3.5 规则 3）⇒ **采点必须由用户终端或 WSL2 执行**；**PSS 口径只在 Linux 有**，故**优先 WSL2**（§3.5 规则 ④：长跑 + 客观资源数 → Linux）。
  - **命令**（WSL2，仓库根）：`node tools/measure-xlsx-peak.mjs --fixtures ".私档/传输-手机-20260918" --out .tmp/y-peak/desktop-peak.json`；Windows 终端同命令（无 PSS 列）。
  - **拿到数之后**才做：冻结 Y1 基线哈希 → 写组 Y 的 **Y1 先绿守卫 + Y2/Y3 先红** → **与实现同批推**（先红单独上 main 会让 CI 变红）。

**A11.21 ⭐ 桌面四档实测在会话内跑完了（用户纠正 + 我上一条判断被推翻）（2026-09-18）**
- ⚠️ **先记一次能力误判（重要，可复用）**：我上一条写"采点必须由用户终端或 WSL2 执行，因为本沙箱不能 spawn 浏览器"。**用户一句"不是还有浏览器工具吗，不行还可以用 chrome 啊"直接推翻** —— `chrome_*` / `browser_*` 是**插件侧**跑的，其进程**不由我的 pwsh 创建**，因此**不受会话沙箱的 spawn 限制**。
  - **实测**：`chrome_open` → **真 Chromium 窗口起来**（本机解析为 **msedge**）· `chrome_navigate` 到本地站点成功 · `chrome_evaluate` 里 `window.__doc2md.convert` 可用 · `isSecureContext=true`。
  - ⇒ **新纪律：凡"要一个真浏览器"的活，先试插件侧浏览器工具（`chrome_*`/`browser_*`），再谈外派给用户/Linux。**"我不能 spawn 浏览器"**不等于**"我不能用浏览器"。
  - 内存采样的替代路径也随之解决：**跨进程总量从 pwsh 侧用 `Get-Process` 采**（cmdlet，非 native，无需 spawn）。
- **四档实测（真 Chromium · 本地同源站点 · 走 `__doc2md.convert` 产品入口）**：报告落 **`docs/大文件路径专项-桌面实测-20260918.md`**。
  | 档 | 压缩字节 | `backend` | 耗时 | md 字节 | 工作集增量 |
  |---|---|---|---|---|---|
  | 1-small | 154,597 | **xlsx-self** | **25 ms** | 66,140 | 低于采样分辨率 |
  | 2-mid | 773,494 | **xlsx-self** | **22 ms** | 23,611 | 低于采样分辨率 |
  | 4-mid-large | 35,856,644 | **read-excel-file** | **55.2 s** | 339,912 | **+7,247.9 MB** |
  | 3-big | 49,716,718 | **read-excel-file** | **98.6 s** | 352,029 | **+7,378.0 MB** |
- **三条判据结论**：**Y3 先红成立**（超 5 s 门槛 **11× / 20×**）· **Y2 先红成立**（四档 `meta.scan` **均不存在**）· **D4 的 200 MB 桌面上界被撞破约 37×** ⇒ **该数作废，须按实测重定口径与数值**（待拍板）。
- ⚠️ **口径缺陷（必须连缺陷一起引用）**：`Get-Process msedge` 全进程 `WorkingSet64` **之和**，250 ms 采样，基线扣前 20 样本中位 ⇒ ① 跨进程共享页重复计入 ② 含 DSH GUI 自身 ③ 工作集不及时回收。**可信区间是量级不是精确值**；即便按 3× 高估折算仍有 ≈2.4 GB，相对 200 MB 是 12× 以上量级差。
  - **为什么不用页内采样**：**实测解析完全阻塞主线程** —— 50 ms 间隔的页内 `setInterval` 在 55 s / 99 s 的转换里**只命中 2 次**（`samples: 2`）⇒ 页内采不到尖峰。
- ⭐ **交叉发现：桌面 vs 手机同输入产物「没有平台差异」（已完证）**。手机 T3 件 339,797 B / `013C1706…`，桌面件 339,912 B / `F90DCE36…`，差 **115 B** —— **根因 = 桌面产物首行多一条截断注释**（`<!-- doc2md: 内容已截断 … -->`，**正好 115 字节**，行数 1005 vs 1003）。**剥离该注释后重算 = 339,797 B / `013C1706…`，与手机件逐字节相同（`match: true`）**。
  ⇒ ① **产物是跨环境确定的** ⇒ **Y1 冻结的基线哈希对 CI/Linux 同样有效**（不会因换环境而红）② ⚠️ **手机那份来自"不带截断注释"的构建** ⇒ **日后做"手机 vs 桌面逐字节"对照必须先对齐构建**，否则会误判成平台差异。
- **本轮未验证（如实登记）**：小档峰值未被测出（快于采样间隔）· 内存只有量级可信 · 桌面耗时与设计稿 78.7 s/116.8 s 不同源不可相减 · **未在 WSL2 复跑 PSS 口径** · Y4 负对照待实现后补。
- **待拍板**：**D4 的桌面内存上界与口径**（建议与手机统一到 PSS ⇒ 那就必须上 WSL2）· `8 MB`（D2）维持待验（`meta.scan` 未实现，本轮无法验证）· `5 s`（D3）本轮**不改口径**。

**A11.22 用户拍板 D4 口径 + 双口径复测 + 「规则在没执行」记一次（2026-09-18）**
- **用户拍板**：**D4 口径与上界按建议重定**（口径统一到 **PSS**、与手机同源）· **D2/D3 维持不动**（8 MB / 5 s 不改口径）。
- ⚠️ **用户当场指出**：「插件侧浏览器工具那个我**在 AGENTS.md 里写过了**」。**核实：确实写了** —— `~/.dsh/AGENTS.md` **第 162/165/167 行**（"若 raw 被安全工具拦，**改用浏览器工具** `browser_navigate`…"、"全程用现成工具（github_repo / `browser_*`）…**不碰 shell/pwsh**"），记忆库里也有「浏览器插件操作」偏好。
  ⇒ **性质 = "规则在、我没执行"**，不是知识缺口：我把这条**只当成"抓 GitHub 时用"**，没上升为"**我手上有浏览器**"这个事实；又把压缩摘要里"沙箱不能跑 playwright"读成了"没有浏览器"。
  ⇒ **纪律（本轮固化）**：**凡"要一个真浏览器/真页面"的活，先试插件侧工具（`chrome_*` / `browser_*`）；判据是"工具是否存在"，不是"我的 shell 能不能 spawn"。**
- ⚠️ **WSL2 这条路会话内仍不可达**：实测 `wsl.exe --status` → **`Wsl/EnumerateDistros/Service/E_ACCESSDENIED`**（**WSL 服务层拒绝，不是 harness 的 `[sandbox: …]` 标记**）⇒ **Linux `/proc/<pid>/smaps_rollup` 的 PSS 本轮仍缺**（如实登记，未当已验）。
- ⭐ **口径改良复测（报告已追加 §8）**：改用 **`PrivateMemorySize64` 之和**（私有提交字节 ⇒ **不重复计入共享页**，大堆私有 ⇒ **≈PSS 减共享分摊**）。
  | 档 | 耗时 | WS 求和（旧） | **Priv 求和（新 ≈PSS）** |
  |---|---|---|---|
  | `4-mid-large` 35.9 MB | 66,710 ms | +6,385.6 MB | **+6,491.2 MB**（峰值 9,983.9） |
  | `3-big` 47.4 MB | 69,336 ms | +3,488.5 MB | **峰值 12,317.7 MB**（增量不可用，见下） |
  - ✅ **结论一**：**旧口径没有被共享页显著放大**（新口径增量**比旧口径还略大**）⇒ **量级结论不变、更稳**。
  - ⚠️ **结论二（方法缺陷）**：**背靠背连跑会污染基线** —— 第 4 档基线被第 3 档残留抬高（WS 2,838 → **6,931 MB**；Priv 3,493 → **7,655 MB**）⇒ **进程内存不随页重载归还**。**修法**：**每档之间重启浏览器窗口**，否则**只能引用绝对峰值**。
  - ⚠️ **结论三**：**同档耗时波动可达 1.4×**（`4-mid-large` 55.2 s → 58.3 s → 66.7 s；`3-big` 98.6 s → 69.3 s）⇒ **耗时不可跨批相减**；但 **md 产物四次全部逐字节相同** ⇒ **产物确定、耗时不确定**。对 Y3 无影响（余量 11–20×）。
  - ⚠️ **与手机 T3 不可直接相减**：手机 7.50 GiB 是**单渲染进程 PSS**；本轮 9.75 GiB 是**全部 msedge 私有提交之和（含 DSH GUI 自身 ≈3.5 GB 基线）** ⇒ **口径不同源**，只能说**量级相当**。

**A11.23 ⚠️ 更正：D4 的「200 MB」被我误读，A11.21/A11.22 结论相应修正（2026-09-18）**
- **误读一：把「目标」当成了「现状上界」。** `docs/大文件路径专项-验收判据-20260917.md` 的 D4 行是四列 **`判据 | 判据形式 | 现状 | 目标`**：**现状 = 手机 12.63 GiB**，**目标 = < 200 MB（桌面）**。
  ⇒ **200 MB 是"修完后要打到"的目标值**，**不是**"现状不得超过的上界"。因此 A11.21 里 **"D4 的 200 MB 被撞破约 37× ⇒ 该数作废"不成立** —— 现状超标**正是本次要修的东西**，**拿现状否证目标是循环论证**。
- **误读二：口径与 D4 指名的不是同一个量。** D4 指名 **页面内 `performance.measureUserAgentSpecificMemory()`**（页面自身：JS 堆 + DOM + worker）；本轮测的是**全部浏览器进程** `WorkingSet64`/`PrivateMemorySize64` **之和**（含浏览器底座 **2.8–3.5 GB** 基线）⇒ **两者相差一个浏览器底座，不能互相判定**。
- **重新定位本轮数字（有用，但换用途）**：✅ **量化"现状有多糟"**（修前基线，供修后对照：大档增量 **+6.4–7.4 GB**）· ⚠️ **小档地板测试尚未做对**（22–25 ms 完成、峰值低于采样分辨率，且口径含底座）· ❌ **不能否证 200 MB 目标**。
- **该补的一步（待做）**：① 站点加 **COOP `same-origin` + COEP `require-corp`** ⇒ `crossOriginIsolated === true` ② 页面内调 **`measureUserAgentSpecificMemory()`** 取小档前后快照 ⇒ **真·地板测试** ③ 大档因**主线程阻塞**取不到转换中快照 ⇒ 该口径的"峰值"**只能等流式实现之后再测**。
- **教训（口径纪律）**：**引用阈值前先确认它是"现状"还是"目标"、是哪个口径量的** —— 这三样任何一样读错，都会得出方向相反的结论。本轮三处落盘（报告 §9 + 本节 + 报告表头指引）已同步更正，原文保留留痕。
- **状态**：**D4 的"200 MB 作废"结论撤回**；用户在 A11.22 的拍板（口径统一到 PSS）**依然有效**，只是**还需补上述地板测试**才算把 D4 走完。

**A11.24 ⭐ D4 地板测试已补做且通过（同日）—— 「200 MB 不会被小档撞破」（2026-09-18）**
- **做法**：本地站点加 **COOP `same-origin` + COEP `require-corp` + CORP `same-origin`** ⇒ 实测 **`crossOriginIsolated === true`** ✅ ⇒ 页面内调 **`performance.measureUserAgentSpecificMemory()`**（**D4 指名口径**）。
- **实测（真 Chromium）**：
  | 状态 | 页面总内存 | 构成 |
  |---|---|---|
  | 空载基线 | **5.72 MB** | JS 4.64 / Shared 1.03 / DOM 0.04 |
  | `1-small` 154 KB 转完（16 ms） | **6.10 MB** | JS 4.63 / Shared 1.44 |
  | `2-mid` 773 KB 转完（23 ms） | **6.61 MB** | JS 4.79 / Shared 1.79 |
  | 两档都转完 | **6.62 MB** | JS 5.21 / Shared 1.36 |
- ⭐ **结论**：**地板 = 6–7 MB 量级，离 200 MB 目标差约 30 倍** ⇒ **"200 MB 会不会被小档就撞破"的答案 = 不会**；**D4 的目标值在小档侧完全站得住**，剩下的只是**大档流式实现后能否回到同一量级**（实现后测）。
- ⚠️ **大档仍取不到转换中快照**（主线程阻塞）；**这也正是先红阶段不需要它的原因**。⇒ **D4 的"目标可达性"已闭环**；"大档修后达标"留待实现批验收。
- ⚠️ **本轮又踩两个坑（都是"没看错误信息就下结论"）**：① **`job_kill` 杀不掉子进程** —— 只杀 pwsh 外壳，node 服务仍占 8123 ⇒ 重启报 `EADDRINUSE`（与之前 python 那次同源；**按 PID `Stop-Process` 收掉**）② **`Get-NetTCPConnection` 内部走 CIM**，在本沙箱被拒；而我用了 **`-ErrorAction SilentlyContinue`** ⇒ **报"no listener"的假阴性**，差点误判端口空闲。**教训：终态判断不要用 `SilentlyContinue` 吞错误。**

**A11.25 组 Y 已写入（Y1 先绿守卫 + Y2/Y3 先红）+ 四档基线冻结（2026-09-18）**
- **用户拍板**：**D4 口径维持原设计**（桌面用页面 API `measureUserAgentSpecificMemory`、手机用 PSS，**不强求同源**）；**先前那条"同意（统一到 PSS）"基于我误读的前提，作废**；以 **6–7 MB 的地板测试**为准。
- **`tests/contract_v1.test.mjs` 新增契约组 Y**（文件 **283,093 B**，`node --check` 过）：
  - **Y1（先绿守卫·D1）**：四档 md **字节 + SHA256** = 冻结基线（同时校验**输入夹具字节**，夹具被换过会当场报出来）。
  - **Y2（先红·D2）**：`meta.scan.bytes ≤ 8 MB` 且两档差 ≤ 10% ⇒ **现红**（字段不存在）。
  - **Y3（先红·D3）**：两档 `convert < 5000 ms` ⇒ **现红**（实测 55–99 s，超 **11–20×**）。
  - **Y4**：负对照留手工（做法写进 CONTRACT §7）。
  - **实现要点**：每档**独立 context+page**、跑完即关（避免背靠背残留污染耗时，见 A11.22）；`setDefaultTimeout(300000)`（大档单次可达 ~100 s）；**md 字节与 SHA256 在页内算**（避免把 MB 级字符串传回 Node）；夹具经**同源 `fetch`** 取（不走 base64 过 CDP）。
  - ⚠️ **夹具不入库 ⇒ 缺失时整组跳过（不是失败）**：默认 `.私档/传输-手机-20260918/`，可用 **`DOC2MD_BIG_FIXTURES`** 覆盖 ⇒ **CI 不会红，先红的可见性在本机/用户机**（已在 CONTRACT 写明"不要用 CI 绿灯推断 Y2/Y3 已达标"）。
  - ⚠️ **D4 不在本组**：指名口径需 `crossOriginIsolated`（COOP/COEP），`tests/lib/server.mjs` 未开 ⇒ 按「修后再测」处理。
- **四档冻结基线（2026-09-18；小档 2× 复跑、大档 3×·2× 复跑，字节与哈希全同）**：
  `1-small` 66,140 B / `A8A777C0…` · `2-mid` 23,611 B / `BD38B16D…` · `4-mid-large` 339,912 B / `F90DCE36…` · `3-big` 352,029 B / `C2C41F5A…`。
- ⭐ **取件路径先验过（最大的一处插桩风险）**：`startServer(ROOT)` + 逐段 `encodeURIComponent` 的中文目录 ⇒ **四档 200 且字节与磁盘完全一致**（154,597 / 773,494 / 35,856,644 / 49,716,718），**越界路径 404**。
- ⚠️ **我跑不了组 Y**：`node --test` 在本沙箱 **spawn EPERM**（既知限制）⇒ **先红/先绿的确认要由用户终端（或 WSL）跑 `npm test`**。**未跑 = 未验证**，如实登记。
- **`tests/CONTRACT.md` 同步**：§2 新增「契约组 Y」段（含四档基线全文）· §7 新增组 Y 红绿条目与**转绿路径**（解析止损：够 1000 行即停，**两条路径都要改**；新增**内部** `meta.scan = { bytes, rows }`，**只进 meta、绝不写进下载的 `.md`**）。
- **下一步**：用户跑 `npm test` 确认 **Y1 绿 / Y2·Y3 红**（先红成立）→ 进实现（解析止损 + `meta.scan`）→ **与先红同批推**。

**A11.26 ⭐ 组 Y 已实测：先红成立 + 先绿成立 + 跳过路径成立（2026-09-18 · 用户授权升权自跑）**
- **用户指示**：「你直接升权自己跑吧，省下我粘贴这一步」。**流程合规**：先照常跑一次拿到**真实拒绝** —— `Error: spawn EPERM`（`node:internal/test_runner/runner:501`，node 的 test runner 每个测试文件要 spawn 子进程）→ 再以同一条命令 + `sandbox_permissions=danger-full-access` + 一句 justification 重试 ⇒ **通过**。
- **全量实测（`npm test`，TAP 落 `.tmp/y-peak/npmtest.tap` / 43,735 B）**：**276 tests · 271 pass · 3 fail · 2 skip · todo 0 · 220.4 s**（原 272/270/0/2 ⇒ 本组 +4 用例、+1 pass、+3 fail，账目自洽）。
- ⭐ **组 Y 逐条实测**：
  ```
  ▶ 契约组 Y：大文件路径（Y1 产物守卫=先绿 · Y2 解析量/Y3 耗时=先红）
    [Y] 1-small（3054 行）：23 ms · md 66140 B · scan=no
    [Y] 2-mid（50001 行）：25 ms · md 23611 B · scan=no
    [Y] 4-mid-large（250000 行）：65824 ms · md 339912 B · scan=no
    [Y] 3-big（436000 行）：96793 ms · md 352029 B · scan=no
    ✔ Y1 产物不变（先绿守卫·D1）：四档 md 字节 + SHA256 = 冻结基线 (1.39ms)
    ✖ Y2 解析量（先红·D2）：meta.scan.bytes ≤ 8 MB 且两档差 ≤ 10%
    ✖ Y3 绝对耗时（先红·D3）：25 万 / 43.6 万两档 convert < 5000ms
  ```
  - **Y1 🟢 先绿成立**：四档现场读数与冻结基线**逐档对上**（66,140 / 23,611 / 339,912 / 352,029），且是**本机跑出来的**。
  - **Y2/Y3 🔴 先红成立**，报错原文即预期文案（"未提供 `meta.scan.bytes`——当前实现无该字段（本断言先红，属预期）" / "转换 65824 ms ≥ 5000 ms（本断言先红，属预期）"）。
  - **除组 Y 外全绿**（271 pass = 原 270 + Y1）。
- ⭐ **跳过路径实测（我写进 CONTRACT 的"CI 不会红"现在有据）**：把 `DOC2MD_BIG_FIXTURES` 指到不存在目录 ⇒ 组 Y 报 **`﹣ … # 大文件夹具未提供…本组跳过而非失败`**、**`fail 0`**、**`exit 0`**（`--test-name-pattern=Y1` 单跑，177 ms）。
- ⚠️ **成本登记**：组 Y 让**本机**全量测试 **+166 s**（220 s 里占 165.8 s）；**CI 会跳过 ⇒ 不影响 CI**。
- ⚠️ **又一次踩 .cmd 中文陷阱（复发）**：我在 `.tmp/run-y-skip.cmd` 里写了**中文 `rem` 注释** ⇒ cmd 按 GBK 读 ⇒ `'含' is not recognized as an internal or external command`，命令没跑成。**`.cmd`/`.bat` 必须纯 ASCII**（AGENTS.md 早写了，仍复发）——**第二次**，建议以后凡写 .cmd 先自检非 ASCII 字节。
- **状态**：先红/先绿**均已实测**（不再是"设计如此"）。**下一步 = 进实现**（解析止损：够 1000 行即停，`xlsx-self` 与库回退**两条路径都要改**；新增**内部** `meta.scan = { bytes, rows }`）→ **与 `9f746d8` 先红同批推**。

**A11.27 ⭐ 卡 002 批：大文件流式化 + 撤护栏 + A9/A10 预检提示（2026-09-19）**
- **目标达成**：大 xlsx 峰值内存 **O(文件) → O(窗口)**；`3-big`（47.4 MB / 43.6 万行）**96,793 ms → 367 ms**（浏览器实测，≈264×；4-mid-large 65,824 ms → 301 ms）。
- **实现（两提交，顺序写死 = 先流式化 → 再撤护栏；中间态未上线）**：`3fc54a9`（`src/xlsx.js` 三处流式化：工作表 XML / `sharedStrings` 逐块解压 + 增量 `decode({stream:true})` + 跨块续接缓冲；新增内部 `meta.scan`；产物重建）+ `4da6ac4`（**撤** t33 的 4 MB `sharedStrings` 护栏 + `src/convert.js` A9 预检「只读中央目录 offset+24，不解压」+ `src/ui.js` A10 解析前提示与**双让帧**；产物重建）。
- **全量实测（`npm test`，本机 · 升权 · 有夹具）**：**276 tests · 274 pass · 0 fail · 2 skip · 56.1 s**（原 276/271/3/2 ⇒ 组 Y 三条先红转绿，**0 fail**）。
  ```
  [Y] 1-small（3054 行）：18 ms · md 66140 B · scan=yes
  [Y] 2-mid（50001 行）：16 ms · md 23611 B · scan=yes
  [Y] 4-mid-large（250000 行）：301 ms · md 352755 B · scan=yes
  [Y] 3-big（436000 行）：367 ms · md 352759 B · scan=yes
  ✔ Y1 产物不变（先绿守卫·D1）  ✔ Y2 解析量（≤8 MB、两档差 0%）  ✔ Y3 绝对耗时（< 5000 ms）
  ```
- ⭐ **Y1 冻结基线冲突与处置（本批最大发现；用户 2026-09-19 拍板「选项 1」）**：两档大夹具的**原基线是「库回退路径」产物**（护栏按**压缩**体积触发：compSize 16.4 / 10.4 MB > 4 MB ⇒ 自解析 throw ⇒ 走 `read-excel-file`）⇒ **撤护栏后大档必然改走快路径，而两条路径对这些文件本来就不等价**：`4-mid-large` 表宽 **18 → 22 列**（1001 行里 **951 行前 18 列逐字符相同**，另有 3 行在 20+ 列有真实数据；成因 = 快路径按 **`c@r` 列号归位**，2026-09-10 拍板、组 **S3** 守着）。**流式化本身零字节变化**（等价性台：旧快路径（仅关护栏）vs 新流式 ⇒ 四档 `markdown` **逐字节相同**，已证）⇒ 变的是**走哪条路径**，而「撤护栏」是本卡明文要求 ⇒ **大档 Y1 基线重冻**（旧值保留作历史），登记见 `tests/CONTRACT.md` §2 组 Y 与 §7。
- **口径变更 2（T-1 B′，用户拍板）**：image 冷启动改为「**只判防呆上限 ≤20 s（非性能判据）+ 预热后按 `<500 ms` 主口径断言**」——原 5,000 ms 豁免窗口作废（2026-09-18 实测偶发 10,194 ms，Linux 控制变量证明 flaky）。实测：桌面 冷启动 **368 ms** / 预热后 **40 ms**；手机视口 冷启动 **363 ms** / 预热后 **36 ms**。
- **A9/A10 浏览器实证（真 Chrome · 本地 http）**：`3-big` 预检提示在 **t = 149 ms** 画出（「大文件：解压后约 361.0 MB；最坏约 90 秒（按慢机档估算），解析期间页面可能暂时无响应 —— 请勿关闭」），转换 **441 ms** 完成；**1-small（0.9 MB）不触发**（无误报）；提示**不进产物**（markdown / meta 均无）。
- **门禁**：`npm run lint` **0/0** · `npm run metrics` 文件 21 / 函数 **502** / 超限 **0** / 重复率 **0.4%** · `node tools/baseline-check.mjs` **PASS**（**`docs/BASELINE.json` 两条 `contract` 已人工同步**：本机 `276/274/0/2` · CI `273/270/0/3`）。
- ⚠️ **新坑（隐蔽，已落 `docs/DEV-NOTES.md`）**：`let preflightHook = null` 与 ui.js 的顶层注册同处一个 IIFE 作用域，**esbuild 把 convert.js 的顶层语句排在 ui.js 之后执行** ⇒ 钩子被擦成 `null` ⇒ **提示永不出现且零报错**；改为「状态挂（会被提升的）函数对象懒槽」后复验通过。
- ✅ **回箱（2026-09-19 晚；两份回执均逐文件核验：Linux `SHA256SUMS` 33/33、手机抽 3 份 + A10 截图我亲自读图）**：
  - **Linux（弱独立）**：组 Y **三绿** · 契约 **276 / 274 / 0 / 2**（与期望逐项一致）· 组 C 预热后 **41 / 43 ms** · lint/metrics/pwa/OCR 全绿 · `scan.bytes` 两档均 **851,968 B（差 0%）** · `HEAD^{tree}` **== `59fc644^{tree}`**（五笔逐笔相同）。⚠️ **负对照未取得断言级红**（本机 5.78 GiB 内存跑不下旧实现大档 ⇒ 3-big 渲染进程被内核 OOM），改用**谓词级现场量**证伪：md **339,912≠352,755** · **无 `scan`** · **70,026 ms ≥ 5,000 ms** · 树峰 **5,346 MB → OOM**。
  - **手机（真机 V2573A / Android 16）**：两档**都能出**且**指纹与我方重冻基线逐字节相同**（`96127CB7…` / `519D58AA…`）· 夸克 **609 / 742 ms**（修前 286.8 s）· 整包 PSS 峰 **1.13 / 0.86 GiB**（修前 12.63 GiB）· **转换边际 +131 MB**（÷解压后 ≈0.36×，修前 ≈35.5×）· **A10 提示条截图实证**。
  - ⚠️ **口径更正（手机侧推翻我方初判）**：18→22 列**不是"路径语义差异"，而是旧路径丢 4 列真数据** —— 夹具声明 `<dimension ref="A1:R250001"/>`（18 列）而实际有 A–V（22 列）、S–V 有真值；旧路径按声明宽度取宽 ⇒ 静默丢列，快路径按实际单元格算宽 ⇒ 找回。⇒ 登记为**修数据丢失**（`tests/CONTRACT.md` §2/§7 已更正，旧说法标掉）。
  - ⏳ **三条待拍板（均超本卡范围，未实施）**：① **内存判据口径**（保留量 +0.2 MB 强证 vs 窄窗口 +468 MB 达标 vs 宽窗口 +646 MB 超阈 ⇒ 建议写死"转换边际"）② **浮点显示**（`34.200000000000003` 是否在渲染层格式化）③ **A10 在秒级时代读不到**（sticky / 阈值化 / 移除）。
  - ⚠️ **我方单据一处口径错（Linux 发现）**：`DOC2MD_BIG_FIXTURES` 指到**仓库外**会让组 Y 变红（`yFixtureUrl()` 用 `path.relative(ROOT,…)` ⇒ URL 带 `..`）⇒ 须**软链到仓库内**；已落 `DEV-NOTES`。

**A11.28 卡 003：契约数自动对账 TAP（2026-09-19）**
- **目标达成**：`docs/BASELINE.json` 的 `contract.*` 从「文字溯源」升级为「**机器现算比对**」⇒ **四个数字块（released / online / delivery / contract）全部可被守卫独立验证**，不再有"只能靠人写一句话证明"的字段。
- **机制**：`tools/baseline-check.mjs` 新增 `--from-tap <tap 文件> --scope <口径>`（解析 TAP 摘要 ↔ JSON `counts` 逐项比对；不符 **exit 1**、**绝不自动改写 JSON**）；`tools/guard-selftest.mjs` **16 → 19/19**（新增 2 条负例 + 1 条正例）；`.github/workflows/tests.yml`：`npm test` **显式** `--test-reporter=tap` 并落 `tap.txt`，**紧随其后**加对账步（`--scope ci_clean_checkout`）。
- ⭐ **A3 实测结论（卡面标的「待实测」）**：**两版本默认 reporter 不一致** —— Node 24（本机，stdout 重定向到文件 = 非 TTY）默认 = **spec**（`ℹ tests …`）；Node 20（CI/Actions 日志，非 TTY）默认 = **tap**（`# tests …`）⇒ **必须显式指定**（CI 与本地流程已落地；`--from-tap` 对非 TAP 输入直接报错）。
- **本机实跑（有夹具）**：`--from-tap tap-local.txt --scope local_with_fixtures` → **PASS（276/274/0/2 逐项一致，exit 0）**；CI 口径本机复现（`DOC2MD_BIG_FIXTURES` 指向**仓库内不存在**目录）→ **PASS（273/270/0/3，exit 0）**。**未改任何应然值**（A9）。
- **门禁**：lint **0/0** · metrics 21 文件 / **509 函数** / 超限 **0** / 0.4% · `baseline-check`（原模式）**PASS** · `guard-selftest` **19/19**。
- **Linux 配额**：本批**纯工具 / 纯 CI**（零产品行为变更）⇒ 按 §3.5 **豁免**，理由与判据见 `docs/任务台账.md` 卡 003 回执（含"CI 本身跑在 ubuntu-latest"这一层）。

**A11.29 卡 005：库回退路径「降级可见性」+ sheet 名退化（2026-09-20）**
- **目标达成**：卡 004 判定的两类**静默**降级现在都会**明确告知**用户，且进 `meta.warnings`（机器可判通道）：① 表宽/表高被 `<dimension>` 裁剪；② 环境无 `DecompressionStream`（老浏览器）。同时 **007 修复**：回退分支**不再丢 sheet 名**。⇒ **004-b 与 007 两项闭环**。
- **口径（用户 2026-09-19 拍板三条，写死）**：① 静默降级 → **明确警告**；② 日期问题（`<cellXfs>` 缺失）**只加 warning、不改回退逻辑**（原话：别用隐蔽的病换明显的病）；③ 007 与 004-b **同批**。
- **实现（最小改动）**：只动 **`src/xlsx.js` 一个文件**（+199/−12）—— 降级观测（`readDeclaredArea` 取 `<dimension>` + `probeSheetArea` 扫前 1000 行取实际列/行）+ 宽松 sheet 名解析（`parseSheetTagsLoose`）+ `normalizeSheetTarget` 抽出（童子军重构，行为逐字不变）。**`src/convert.js` / `src/ui.js` 一行未动**（`meta.warnings` 汇总与 `.warnings` 渲染早已具备 ⇒ 「若需才改」的若需不成立）。
- **先红 → 后绿**：新增**契约组 G8**（7 条 = 组壳 + G8-1..G8-6；用例**页内现造**、不入库样例）。**先红** = `tests 7 / pass 2 / fail 5`（G8-1/2/3/4 红：`warnings=[]`、sheet 名退化 `Sheet1`）；**后绿** = 全量 **283 tests / 281 pass / 0 fail / 2 skip**、G8 **6/6**。
- **产物不变（A6 硬约束）**：四档 md **字节 + SHA256 = 冻结基线**（离线核对台 4/4 + 浏览器侧组 Y1 绿）：`66140` / `23611` / `352755` / `352759`。
- **A8 闸（不碰 T6）**：`parseStylesDateFormats` / `parseXfIds` / `xlsxSelfParse` 的 styles 调用点 / `xlsxParseSheetStream` 与开工基线 `9965d08` **函数级 byte-identical**（`git worktree` + 切片比对）。
- **门禁**：build ✓ 产物 `index.html` **134,317 B / `23CEC085…4FC7`** · lint **0/0** · metrics 21 文件 / **524 函数** / 超限 **0** / 重复率 0.4% · `baseline-check` PASS（**两口径 TAP 对账 exit 0**）· `guard-selftest` **19/19**。
- **契约数人工同步（nonGoal 5）**：`local_with_fixtures` `276/274/0/2` → **`283/281/0/2`**；`ci_clean_checkout` `273/270/0/3` → **`280/277/0/3`**（后者为**本机复现 CI 口径**实测；CI runner 真实值待推送后由 CI 步自动对账，**不符即红**）。
- **Linux 独立验收**：本批是**行为变更**（改 `meta.warnings` 与用户可见提示）⇒ 按 §3.5 **不适用豁免**，**已出单**：`.私档/linux/任务-005-库回退降级可见性/`（三件套 + 负对照必红要求）。
- **观察期**：按 `RELEASE-CHECKLIST §5` 新规，**期内不发 tag**。
- **一手坑**：为过 `metrics` 拆函数时吃掉了「循环 `continue` 的二次检查机会」⇒ `<dimension>` 永远扫不到；**lint / 单测 / 离线核对台全绿，只有先红断言 G8 抓到** —— 详见 `docs/DEV-NOTES.md` 卡 005 节。
- **回箱（2026-09-20，Linux 弱独立 = 换机 WSL2 + 真 chromium `151.0.7922.34` + 补丁同源）—— 通过**：两口径契约数逐项命中（`283/281/0/2` · `280/277/0/3`，两次对账 **EXIT=0**）· 组 G8 **7/7** · 四档 Y1 字节与 SHA 不变 · `index.html` **134,317 B / `23CEC085…4FC7`** 逐字节一致 · lint/metrics(524/超限 0/0.4%)/baseline-check/guard-selftest(19/19) 全绿 · 附 `pwa-audit 48/48`。**我对回箱材料的独立复核**：哈希清单 **24/24 逐条一致**、关键数字全部从**原始日志**复现。**契约数三方吻合**（我本机 / Linux / 真实 CI 对账步）⇒ nonGoal 5 的"CI runner 真值待对账"**当场闭合**。回执全文 `.私档/linux/20260920-库回退降级可见性/`。
- ⚠️ **负对照实测 4 红（比我单据预期的 3 红更强）**：`xlsxDegradeWarnings` 是**三类告警的唯一出口**，整体短路连 G8-2 正向断言的「日期」告警也关掉 ⇒ G8-2 后半句红。Linux 侧定位根因并用两个更窄注入（A′ 只短路表宽分支 = **预期 3 红**；B 丢 sheet 名 ⇒ G8-3 以**名字**理由红）复现了预期形状；三次注入后产物与工作区**完整还原**。**⇒ 是我的单据对「注入作用域」描述不精确，非实现缺陷**（教训已入 `DEV-NOTES`）。
- ⚠️ **单据状态行两处过期（归我）**：① §1 写的实现提交 `28e3618` 是 **amend 前**哈希（远端实为 `0d4f039`；`git am` 后 tree 只差 `docs/任务台账.md` 4 行，**受测三件套 blob 完全相同**）② §1「未推送」在写单期间已被推送。⇒ 此后出单**同时贴补丁 `From` 哈希 + 远端哈希并注明是否 amend**，判据用**受测文件 blob 级**；状态性内容一律标「以现跑为准」。
- **CI 终态**：首次 run `35458379084`（提交 `0d4f039`）**红在第 8 步** —— **npm 计划维护（17:00–19:00 UTC）⇒ `registry advisories` 503**，**其后 6 步全被跳过（该 run 从未跑测试）**；归因证据（`tools/**` 零改动 + blob 同一份 + 本机复现同错）与处置见 `DEV-NOTES` 2026-09-20 事故节。维护结束后**代跑重跑 → `✓ checks in 1m20s`，16 步全 ✓**，其中 **`Contract ↔ BASELINE.json reconciliation` ✓** 即上条"三方吻合"的第三处。

**A11.30 卡 006：图片 alt 的连续空白折叠（2026-09-20）**
- **来源 = 用户实测**（观察期「去用」）：原话「我自己试了一下，我看这**括号**有问题啊」。现象 = alt 里的空行让 `![alt](src)` 跨行 ⇒ 渲染器断在空行处 ⇒ 页面只剩**裸 `![` / `](`**。
- **根因（两条入口 = 同一条链）**：`src/docx.js:35` `docxAltFromName` 只有 `.trim()`；`src/html2md.js:111` `imgFrag` 只转义 `]` —— 而 **docx 的 alt 最终也经 `imgFrag`**（mammoth → HTML `<img alt>` → `htmlToMarkdown`）。
- **实现（最小）**：`docx.js` 新增 `docxCollapseWs`（`\s+`→单空格，保留既有 trim）；`html2md.js` `imgFrag` 对 alt 折叠空白（**只折叠、不裁剪首尾** —— 裁剪会动到"无换行输入"的字节）。**`index.html` 133,288 chars → 产物 `134,421 B / `27E78682…A665`**。
- **先红 → 后绿**：新增**契约组 I2**（7 条；用例页内现造、**既有夹具字节锁零改动**）。**先红** = `tests 7 / pass 1 / fail 6`（I2-1..I2-5 红，报错原文即裸换行）；**后绿** = I2 **7/7**、全量 **290 / 288 / 0 / 2**。
- ⭐ **A5 前提（卡面要求"先测再判"）**：实测四档 `Y_TIERS` **全是 `.xlsx`**（`xlsx-self`）、**产物中 `![` 出现 0 次** ⇒ 本卡改动**不可能**触及它们 ⇒ A5 走「**一字不变**」分支，并由**离线核对台 4/4 逐字节**（`66140/23611/352755/352759`）+ 浏览器 **Y1 绿**双重证明。**没有出现卡 002 那种"守住错基线"的情形。**
- **A6（html2md 路径）实测**：改前 `<img alt="a\n\nb">` ⇒ `![a\n\nb](x.png)`（**中招**；字面换行 / `&#10;` / CRLF 三写法都中）；改后 I2-4 五形态全绿。
- **A10 端到端**：等价构造「15 图 + 每张 alt 含空行」⇒ 产物 **15/15 单行**、`markdown md2html` 渲染 **15/15 `<img>`、零裸 `![`**（改前 **0/6**）。**用户原件（论文 `实验.docx`）不入库**（红线 11）。
- ⚠️ **A11 新发现（与卡面 non-goal 的**前提**不一致 —— 已更正登记）**：卡面称「docx alt 的 `]` 未转义、另立卡」；**实测不成立** —— `descr="A]B](https://…)&#10;C"` 的产物是 `![A%5DB%5D(https://example.invalid) C](…)`，`]` **已被 `%5D` 转义**（转义就在 `imgFrag`）⇒ 该候选**应撤回/改写**。另一件：`descr` 为 Word 样板句时它**就是 alt 文本**（C1 口径：文档自带属性优先），**本卡不动**，待拍板是否收窄。
- **门禁**：build ✓ · lint **0/0** · metrics 21 文件 / **525 函数** / 超限 **0** / 0.4% · `npm test` **exit 0** · `baseline-check` PASS（两口径对账 exit 0）· `guard-selftest` **19/19**。**契约数人工同步**：`283/281/0/2` → **`290/288/0/2`**；`280/277/0/3` → **`287/284/0/3`**。
- **行为变更** ⇒ **不进豁免**：**Linux 独立验收单已出**（`.私档/linux/任务-006-alt空白折叠/`，含负对照必红）；按 `RELEASE-CHECKLIST §5` 进观察期，期内不发 tag。
- **一手坑**：① 首次量四档时**漏了 `withTruncationNotice` 组装**（直接调 `xlsxConvert`）⇒ 四档**恰好各少 113 B**，差点误判"基线漂移"（卡面警告的正是这类误判）② 造夹具时**字面换行会被 XML 属性值归一化成空格** ⇒ 必须写 `&#10;`，否则红回路造不出来。详见 `docs/DEV-NOTES.md` 卡 006 节。

**A12 局域网交换页 UI 改版（用户 2026-09-18 要求：「下次网页整好看点」）**
- 现状：手写裸 HTML，一个 `<form>` + 文件列表；无样式体系、无拖拽、无进度、无移动端适配（手机上是主用场景，尤其该修）。
- 下次开工先定稿再动手：拖拽上传 + 上传进度 + 文件列表（大小/时间/一键复制 `git bundle` 命令）+ 移动端单列 + 与 doc2md 主站同配色。
  - ⚠️ **口径更正（2026-09-18 晚）**：原文写"仍无认证" —— 现已**加口令保护**（HTTP Basic，口令文件 `.私档/lan-git/.exchange-token`，文件在即启用、删掉即开放、每请求现读 ⇒ 换口令免重启；**口令值不入档/不入聊天**）。UI 改版时**必须保留口令门槛**，并给手机侧留"浏览器记住口令"的顺畅路径。

**B. v0.1.2 剩余功能**：~~PDF 图纸页保图（27 页机械指导书实测触发）~~ → **与 A3.3「低质页保图」同一件事，已合并登记于 A3.3**（2026-09-14 去重）｜~~预览 1MB 截断~~ ✅ 已闭环（§8.1 批）

**C. 发版**：~~v0.1.2~~ ✅ 已发布（`f5aed38`）｜~~v0.1.3~~ ✅ **已发布（2026-09-12，tag `v0.1.3` = `d6929e6`；原记 `5683b05` 系误，2026-09-14 实测勘误）**：S4/S5 规范符合性闭环 + 收口批 + 两条新防线（组 T / 部署 smoke）；观察期 09-12 起 ≥3 天 → **09-15 复盘**

**D. 毕设线（另一条线，非本线）**：视觉检测方向（YOLO26，老师已确认「可以」）；构想 v2 / 环境清单未产出，用户未催

**E. v0.1.3 候选（用户 2026-09-10 拍板「记进 backlog，v0.1.3 再看」）**
1. ~~**本地陈旧产物陷阱**~~ ✅ **已闭环（2026-09-12 纳入 v0.1.3，契约组 T 现场重建比对字节）**（原 §3 状态未同步，2026-09-14 修正；下方为闭环前的原始描述）：`npm test` 断的是已构建的 `index.html` —— 只改 `src/` 忘 `npm run build` 时本地全绿（CI 有 `build && git diff --exit-code index.html` 拦，本地无）。候选做法：测试内加「产物与 src 一致」检查（新增断言，需拍板）或仅加提示。
2. ~~**部署白名单 smoke**~~ ✅ **已闭环（2026-09-12 纳入 v0.1.3）**：新增 `tools/deploy-smoke.mjs` + `deploy-pages.yml` 接线（部署前按「引用即必需」核对 `_site`）——实测正例 PASS（15 引用）/ 负例 exit 1（精确列出缺失文件）
3. **测试脚本重复**：口径已定（重复率只看 src+tools），若日后要压 tests 的重复 → 抽 `withPage()` 共享夹具（只动结构、不动断言）。

**F. 跨平台一致性（2026-09-15 新增；结论与证据详见 `docs/DEV-NOTES.md` 2026-09-15 节）**
1. **`docs/CODE-METRICS.md` 不是跨平台文件**（Linux 侧独立复跑实测）：`tools/metrics.mjs` 输出里的文件路径随平台取分隔符（Windows `src\x.js` / Linux `src/x.js`）→ 换平台跑 `npm run metrics` 会产出**全文件重写** diff（实测 +350/−350）；且该命令**本身就会重写受版本控制文件**。**候选做法**：输出路径按 `path.sep` 归一为 POSIX → 跨平台可重复；顺带评估把报告写到 gitignore 路径。**待拍板**。
2. **无 `.gitattributes` + `core.autocrlf=true` → 检出即 CRLF（字节级陷阱）**：实测 `git checkout/restore` 把 `index.html` 写成 **120,410 B / `A98CAF31…`**（入库 blob 为 **118,244 B / `62B8ADA1…`**），而 **git 内容比较视为「无差异」** → `git status` 看着正常，**只有按字节核验的门禁（契约组 T、`manifest.json` 字节锁 U-0/V0）会红**；`src/pdf.js` 工作区 `w/crlf` 即此机制。**候选做法**：加 `.gitattributes`（`* -text`）让检出字节 = blob 字节，并归一化现存 CRLF 文件。**待拍板：是否纳入 v0.1.5。**
3. **陈旧哈希勘误（2026-09-15 已修）**：本文文首原记产物 `087CDB12…BF14B`（118,244 B）—— 实测该哈希 = **提交 C（`4344293`）时点**产物；版本 bump 提交（`1e2377d`）改了产物但**字节数恰好不变**（`v0.1.3`→`v0.1.4` 同长）→ 尺寸核对「看着是对的」、哈希静默过期。**教训：尺寸相同不能当不变量，pin 必须用哈希。**
4. **v0.1.5 批独立验收：四项全通过（2026-09-15）** —— Linux 侧（WSL2 + **真 chromium** + **真 tesseract chi_sim**）：V-1 契约 233/231/0/2（与本侧逐字一致）；**V-2 真 OCR 中文图端到端 3/3 通过 + 负对照 3/3 变红**（负对照态产物 = `118,244 B / 62B8ADA1…` = v0.1.4 产物，用哈希佐证「摘除即回到修复前行为」）；V-3 `metrics` diff 仅生成时间一行（修前为 +350/−350 整表重写）；V-4 `check-attr` = `text:auto`/`eol:lf`、全树 `w/crlf = 0`、`hash-object == 索引 blob` 5/5。Windows 侧实测核验 **21/21 文件字节 + SHA 吻合**，并**追加关闭 U6**（真删 3 文件再检出 → 仍 `w/lf` 且字节等于 blob ⇒ 该属性在 Windows 上确已生效）。
5. **新候选（待拍板）：「半角标点」折叠缺口** —— V-2 的 g2 实测暴露：OCR 把全角「：」误识为 ASCII `:` 时，输出 `会议纪要 : 项目`（空白未折叠）。根因 = `src/cjk.js` 的 `CJK_PUNCT_CLASS` **只含全角中文标点 + 少数 ASCII（`()[]{}%+`）**，ASCII `:,.;!?` 不在其中。**属显示级瑕疵（不产生错误数据）**，但会产生用户可见的怪异空格。**修 = 加字符进字符类 = 改折叠口径**（铁律 6：改断言 = 改口径 = 拍板）→ **需用户签字 + 先补先红断言**（R1 用例表新增半角标点组）。**本批未修。**
6. **仍未验证（保留登记）**：V-2 轮次 U1 OCR 准确率 / U2 全角半角穷尽 / U3 其他图像条件（小字号·低对比·旋转·手写·噪声）/ U4 多行文本 / U5 PDF OCR 路径（`ocrPageToText`）真 OCR 端到端 / U7 重复取样；另有 v0.1.4 轮次的 U1 日志时间戳跳变根因、U4 其他发行版。

**G. OCR 质量与版面分类（2026-09-15 真机照片复盘产出；根因与实测见 `docs/DEV-NOTES.md` 2026-09-15「真机照片 OCR 复盘」节）**
1. **v0.1.6 —— ✅ 已实施并验收通过（2026-09-15，提交 `03bad6c`；Linux 侧独立验收六项全绿）**：① **显式 PSM 3（AUTO）** —— 根因 = tesseract.js **隐式默认 PSM 6（SINGLE_BLOCK）**，与用户手机端输出**逐字相同**（三条独立证据）；4 图网格实测：PSM 6 在「图旁正文」「90° 旋转页」上分别**整段 / 整页崩**，**PSM 4 在旋转页只剩 4–7 字符**（单图时它还是"最优"——多图才暴露版式依赖）；PSM 3 从不最差且是旋转页唯一可读配置，**旋转无需 OSD 资产**。② **大图预缩放**：长边 > **1500px** → 1500px（**只缩不放**）—— 实测探针 2/5 → **5/5**、提速 30–60%（6.7s → 4.1s）。③ **质量信号**：低置信度 + **高拉丁占比**（仅中文场景判「版面或方向问题」，避免误报纯英文图）。契约组 **W**（W1/W2/W4 先红、W3/W5/W6 守护）；全量 **240 tests / 238 pass / 0 fail / 2 skip**，产物 `119,699 B / 3D044AA0…`；lint 0 / metrics 17/353/超限 0 / pwa-audit 48/0 / verify:ocr PASS(93%)。
   **依据与证据链**：`docs/DEV-NOTES.md` 2026-09-15「v0.1.6 批」节 + `.私档/项目/真机OCR复现/grid-20260915/`（59 文件：40 份网格全文 + summary + 15 份首轮台架 + 4 个可复现脚本）。
   **仍未解决（README 已如实标注）**：手写内容、照片表格结构、行内公式、示意图内标注、形近字。
   ⚠️ ①与②**有交互**（25% 缩放 + PSM 3 反而最差 12/16）→ **必须先做多张样张（≥3–5 张，覆盖纯文字页 / 图文混排页 / 表格页）的参数网格再定值，避免单图过拟合**。
2. **v0.2**：**PDF 绘图算子矩形 → 表格**（用户 2026-09-15 拍板：工作量大，排 v0.2）。依据：pdf-inspector 用「**绘图算子矩形 + union-find 聚类**」重建表格；**我们的 operator list 里已有这些算子，但只消费了文字算子**（`src/pdf.js` 仅 `TEXT_OPS`，全文无 `constructPath`/`rectangle`）→ 这是把「PDF 表格被拍平成文本行」（本仓已登记待改进项）救回来的**现成路径**。
3. **backlog（不急）**：**坏编码显式暴露给用户**（对应 pdf-inspector 的 `hasEncodingIssues` 字段）—— 我们有 FFFD 计数与质量门，但**未把「这页字体编码坏了」作为独立信号提示用户**。
4. **版权口径（本项目纪律，2026-09-15 用户明确）**：**只借鉴思路、不搬代码** —— 不引入其 crate/npm 包作为依赖、不移植其源码结构与实现细节、实现建立在自己的资产上（pdf.js operator list / 文本层统计 / tesseract 参数）、文档仅做**事实性引用**（名称 + 链接 + 结论），不复制原文段落。
   > **事实附注（供决策参考）**：其许可为 **MIT**（允许使用/修改/商用，条件 = 保留版权与许可声明）；不过它是 Rust、我们是浏览器 JS + tesseract，**代码本不可直接移植**，故「独立实现」不额外损失什么。
5. **v0.1.7 —— ✅ 已实施（2026-09-15，提交 `a010bc8`）**：**OCR 方向重试**。根因（**产品路径实测**）：tesseract 的 AUTO **只认一个旋转方向** —— 同一 10 行中文夹具（旋转后 1900×1400，与真机旋转照片同形）：顺时针 90° → CJK **118/123**；**逆时针 90° → CJK 7**（全篇乱码）；对失败方向**再顺时针 90°** → **118/123** 救回（用户的真机旋转照片恰在"支持"那一侧，故 v0.1.6 复测通过）。
   口径（用户拍板）：**不加 OSD 词典资产**（否决 +10MB）；把质量信号**从"提示"升级成"行动"** —— 触发 = `ocrWarnings` **同一口径**；动作 = **顺时针 90° 重试一次**；**只重试一次 / 取更优（写死打分 `conf − 100 × 拉丁占比`）/ 阈值写死**（`60` / `20` / `0.4`）。
   实测（本侧真跑，真 chromium + 真 OCR）：正立 118（1288ms 不重试）/ **逆时针 90°：7 → 118**（2263ms ≈ 1.75×，只在此坏例付费）/ 顺时针 90° 118（873ms 不重试）。契约组 **X**（X1/X3/X5 先红；X2「首次好恰好 1 次」= 成本只在坏例、X4 = 守护）；全量 **246 tests / 244 pass / 0 fail / 2 skip**，产物 **120,988 B / `E5037CE1…`**。
   ⚠️ **交接单状态**：v0.1.6 那份交接单（`任务-v0.1.6独立验收/`）的**产物哈希 `119,699 B / 3D044AA0…` 已被本批取代**；合并验收另出增补单 `任务-v0.1.7独立验收/`（覆盖 v0.1.6 + v0.1.7，含新补丁）。
   **用户级验收（2026-09-15，已通过）**：用户在自己设备复测 **4 张真机照片 → 全部可读**（本侧独立核验探针 5/5 · 5/5 · 6/6 · 5/6；旋转页 CJK **9 → 252**、拉丁占比 0.932 → 0.056）；线上站点实测 = **v0.1.7 构建逐字节相符**。复测用的是**重新导出过的图**（字节与首轮不同）→ 属换数据复测。详见 `docs/DEV-NOTES.md` 2026-09-15 v0.1.7 节。
   **独立验收（Linux 侧，2026-09-15）—— 六项全绿，已通过**：真 chromium 契约 **246 / 244 pass / 0 fail / 2 skip**（与本侧逐字一致）；真机样张 4/4（CJK 438/268/65/**276**）；**方向重试三态均可读（各 101.0%）**；成本 **1.30×**（无 2× 翻倍）；**负对照：切回 v0.1.6 后同一逆时针夹具 CJK 0/84 = 0.0%（全篇乱码），v0.1.7 = 101.0%** → 因果锁定（负对照态产物 = `119,699 B / 3D044AA0…` = v0.1.6 产物哈希）；预缩放矩阵 3000×2000→1500×1000、800×600 原样（只缩不放）+ PSM 3 显式。Windows 侧实测核验 **29/29 文件字节 + SHA 吻合**；**U1 关闭**（补件后探针 5/5·5/5·6/6·5/6，替代路径 **100.00%** 一致）、**U2 由本侧结案**（真机旋转页方向属 AUTO 支持侧 → 不触发重试，两版相同是正确行为）；U3–U6 保留。
   **采纳的工程坑（Linux 侧发现）**：`git checkout <commit> -- <path>` **会同时写索引与工作区** → 之后 `git checkout -- <path>`（语义是"从索引取到工作区"）**还原无效**；正确做法 `git checkout HEAD -- <path>`。已记入 `docs/DEV-NOTES.md`。

## 3.5 平台分工（2026-09-18 用户拍板；backlog 分派依据）

> **目的**：每条 backlog 都写明「**谁实现 / 谁验收 / 谁取证**」，各自做擅长的。**本次 Linux 静默断档**（连续数批无 Linux 任务）就是缺这条规则造成的：近期批次全是文档/CI/守卫（无行为变更）+ 观察期禁改行为 ⇒ 行为变更断档，Linux 任务随之断档。
> **配套**：§9（Linux 侧工作区现状）· §10（与 Linux 侧（WSL2）协作约定：投递箱 `.私档/linux/`、**只读不推**、交接单必备三件、跨机比对口径）——流程与纪律沿用 §10，本节只定**分派**。

**能力矩阵（每一项都有本项目实测依据，不是推测）**

| 平台 | 最擅长 | 硬限制（别再派） |
|---|---|---|
| **Windows（本机 / 主开发）** | 源码实现 · 构建内联（esbuild）· **产物字节 + SHA256 核验** · npm/lock/vendor 供应链审计 · metrics / lint · git 操作与发布准备（只到 dry-run） | **会话内沙箱起不了浏览器/子进程**（2026-09-18 实测：playwright `spawn EPERM`，`node --test` 亦 EPERM ⇒ 契约测试需一次性 `danger-full-access` 才跑得起来）· 本机 **playwright chromium 未安装**（回退 msedge）· 真机（Android）取证做不了 · ⚠️ **但「起不了浏览器」只对「自己 spawn」成立，见本表下方补注** |
| **手机（V2573A / Android 16）** | **用户可感行为**的唯一来源：真机 UI 实测 · ADB 只读取证（PSS / swap / 温度 / `exit-info` / 全量 logcat）· 触摸 / 分享 / 系统下载语义 · **真断网 PWA** | 无 root ⇒ 内核日志不可得（`dmesg` / `/dev/kmsg` / `/proc/kmsg` 全被挡）· 无线调试**绑 Wi-Fi**（adbd 随 wlan 起停，**换网需重新配对**）· 发热/充电污染基线（T3 前置：Thermal ≤ 1、SKIN < 40 °C、不边充边跑） |
| **Linux（WSL2 Ubuntu 24.04 / CI）** | **换环境独立验收** · **真 playwright chromium**（`chromium-1234`，Windows 本机缺）· 负对照必红 · **CI 等价**（两个 workflow 均 `ubuntu-latest`）· 真 OCR 端到端 · bash/`rg` 一行流（隐私扫描、体积分析、跨平台一致性）· 客观资源数（cgroup / `time -v`） | 无 Android 真机 · 造 Office/WPS 对照件不便 · **WSL = 弱独立**（见下） |

> ⚠️ **补（2026-09-19，比本表更新）：「会话内沙箱起不了浏览器」只对「会话自己 spawn」成立**
> **插件侧浏览器工具（`chrome_*` / `browser_*`）不受影响** —— 其进程由插件创建，**不经会话文件沙箱**，故在 `read-only` 与 `workspace-write` 下**均零升权可用**。
> - **来源 1（2026-09-18 晚，Windows 侧，转记）**：`chrome_status` / `browser_navigate` / `browser_eval` / `web_search` 在 `read-only` 下全部可用。原始记录在**调度工作区**的 `..\doc2md-调度\平台\windows.md` §2（路径相对本仓库根），该条自标「应回灌本节」。
> - **来源 2（2026-09-19 00:50，调度线独立复现，非转述）**：用 `chrome_navigate` + `chrome_evaluate` 成功读取 GitHub Releases 页；**同一时刻** `read_url` 对 `github.com` 报「解析到内网地址已阻止」—— 与 `docs/DEV-NOTES.md` 8.12 记载一致。
> ⇒ **要真浏览器 / 真页面（查上游 issue、看渲染效果、抓 API、核 Release/Pages）时，先试插件侧工具，别默认外派给 Linux 或用户。**
> ⇒ 本表 Windows 行「会话内沙箱起不了浏览器」应读作：**起不了「自己 spawn 的」浏览器**。

**WSL 的定位（2026-09-18 用户拍板）**：**合格的「弱独立」环境** —— 能验证**跨平台行为**（路径 / 权限 / 换行 / 构建一致性 / 真实 chromium 与 OCR），**不能替代跨机器硬件验证**（同机同内核，硬件、电源、热、驱动层面同源）。⇒ **验收报告必须写明档位**（弱独立 / 强独立）；**将来有第二台物理机时升格**为强独立。

**分派规则（四条）**
1. **谁离真相最近就谁做**：观测对象是**浏览器行为** → 桌面（用户终端 / Linux）；是**用户可感想** → 手机；是**产物字节与仓库状态** → Windows。
2. **必须换环境才算数**（修验分离）→ **Linux**；**同机换个终端不算换环境**；WSL 记「弱独立」并写明。
3. **要 spawn 浏览器/子进程的** → **默认派 Linux**：那里有**真 chromium**（`chromium-1234`），而 Windows 本机 playwright chromium **缺失**（只能回退 msedge），且**会话内沙箱一律 `spawn EPERM`**（`node --test` 亦然）⇒ **契约测试 / 真 OCR / 负对照**这类门禁走 Linux，Windows 侧做静态门禁 + 产物核验 + 供应链审计。Windows 确需跑时走**一次性升权**（`danger-full-access`，2026-09-18 首次实测通过）。**注意分工边界**：Windows 侧的职责是**出单 + 收件核验**，执行在 Linux 侧 DSH —— **不要自己伸手去 WSL 跑**（2026-09-18 用户驳回一次此类升权，规矩即此）。
4. **长跑 + 要客观资源数** → **Linux**（cgroup、`/usr/bin/time -v`）或**手机**（PSS + thermal）；Windows 的"任务管理器读数"只作参考。

**Linux 验收配额（2026-09-18 用户拍板）**：**每批行为变更至少 1 条 Linux 独立验收任务**（换环境 + 换数据 + 负对照必红）。
> **豁免条款**：**纯文档 / 纯 CI / 纯守卫工具**批次可豁免，但**必须在该批记录里写明豁免理由** —— 目的是防止 Linux 又静默消失，不是强制凑数。

**首批映射（C 项：随第一批执行时按本节补三格，现不预填）** —— 待办里最典型的形态已可见：**backlog 条目 8（流式/P1）** = 实现 Windows · 验收 **Linux**（真 chromium 契约 + 负对照 + 客观 RSS）· 取证 **手机**（真机 PSS/热）；**R9-1（批量失败隔离）** = 实现 Windows · **先红/后绿在用户终端或 Linux 跑**（会话内沙箱跑不了契约）—— ✅ **该条已于 2026-09-18 完成（A11.15），此处保留其平台分工形态作范例**。

## 4. 工作方式（省 token 模式，用户 2026-09-08 拍板）

- **小改动自干**（AI 直接实现 + 用户机验证）；跨模块/高风险才拉 AgentTeams（契约→实现→验收三件套）
- **团队报告精简**：成员消息只写「结论 + 提交 hash + 关键证据 ≤10 行」，细节落 CONTRACT/DEV-NOTES
- **不随手调 `agent_teams_status`**（回吐全量任务 output，极耗 token）；只在派活/查阻塞时调
- 对话回复简短、不加表情装饰；不重复已汇报内容
- **时间一律写绝对时间**（`YYYY-MM-DD HH:mm`），不写「今天/明天/后天」——2026-09-14 踩过：凌晨 03:05 时，人对「明天」的理解与机器相差一天（`git log` 的提交时间一律以绝对时间引用）

## 5. 命令与环境坑（必读）

| 场景 | 做法 |
|---|---|
| git log | `git --no-pager log --oneline -N`（PowerShell 下分页器报 `'nutc': unknown terminal type.`） |
| **native 命令（`git`/`node`/`python`/`npm`/`npx`/`curl`/`wsl`… 一律，不限 git）** | **不加管道/重定向/表达式**：`… \| Select-Object` / `2>&1` / `\| Out-String` / `"…" + (git …)` / `$x = python …`，**以及任何重定向 —— `> 文件` / `>> 文件` / `> $null`**（`$null` 也是重定向目标，不是「丢弃输出」的语法糖） → `Program 'git.exe' failed to run: Access is denied` 或 `StandardOutputEncoding is only supported when standard output is redirected`（沙箱禁命名管道，**命令根本没执行**——易误判为 commit/fetch 失败）；native 输出一律写成**独立语句**。⚠️ 正因命令没执行，**此时 `$LASTEXITCODE` 是上一项的残留值、一律不可采信**（2026-09-19 卡 004 自测：先 `git --version`(0) 再撞 `python … > $null` 坑 → `$LASTEXITCODE` 仍是 **0**，看着像成功）；且 **`> 文件` 会留下 0 字节的假产物**（PS 先建目标文件、进程没起来）⇒ **"文件存在"≠"命令跑了"**，判成败要看**副作用（且非空）**。安全目录前缀写 **`-c safe.directory=*`（不带引号）**——`'*'` 在 pwsh 下引号被吞 → `fatal: detected dubious ownership`（2026-09-15 实测；同进程三件套 `$env:GIT_CONFIG_COUNT=1` / `GIT_CONFIG_KEY_0=safe.directory` / `GIT_CONFIG_VALUE_0=*` 亦可用） |
| 版本内容比对（按字节） | 取历史提交的文件内容时**必须加 `-c core.autocrlf=false`**：`git -c safe.directory=* -c core.autocrlf=false restore --source=<commit> --worktree -- <path>`。否则检出过滤器写入 CRLF（`index.html` 118,244 B → **120,410 B**），且 **git 视为无差异**；比完记得还原（同命令 `--source=HEAD`）并核 `git status` 与 SHA256 |
| git 网络命令（**Linux 侧**） | **必须加 `-c http.sslVerify=false`**：Steam++ 对 `github.com` 做 TLS 中间人（hosts 指向 `127.0.0.1` 反代），其根证书不在 WSL 信任库 → 裸 `git fetch/pull/push` 报 `server certificate verification failed. CAfile: none CRLfile: none`（2026-09-14 实测）；加该参数后 `ls-remote`/`fetch` 实测通。同理 Chromium 需 `--ignore-certificate-errors`（见全局 AGENTS.md「本机浏览器」节） |
| 提交身份 | **本环境无全局身份**（`.git/config` 无 `[user]`；裸 `git commit` 报 `Author identity unknown` → `fatal: empty ident name`）——一律 `-c user.name=sakuraqqq -c user.email=sakuraqqq@users.noreply.github.com` **局部覆盖**（noreply，防邮箱泄露） |
| build / test | **用户终端**跑（沙箱禁 esbuild spawn / node --test / 浏览器 spawn）；经用户批准的一次性升权可在会话内实跑（§8.1 批已实证：`npm run build` + `npm test` 145/145） |
| 发布动作 | `git push` / `tag` / `gh release` **用户执行**（AI 只到 dry-run） |
| 工作树 | `.私档/`（私人文档）· `docs/copyright/` `tools/gen-copyright.mjs` `.script-archive/`（软著材料）——均 gitignore，**禁止 add** |
| 提交范围 | 只 `git add` 指定路径，**禁用 `git add -A`**（并行线文件混入） |

## 6. 关键文件

- 纪律：`AGENTS.md`（红线 + 约定）· `~/.dsh/AGENTS.md`（全局，第 11 条隐私红线）
- 发布权威源：`docs/RELEASE.md`（v0.1.0/v0.1.1 记录 + 观察期）
- 契约权威源：`tests/CONTRACT.md`（~170KB，组 A-P）· `tests/contract_v1.test.mjs`
- 决策史：`docs/design-decisions.md`（DD-4~17）· 架构：`docs/architecture.md` · 许可：`docs/licenses.md`
- 审查报告：第五轮 `docs/doc2md-第五轮审查报告-2026-09-08.md`（已闭环）· 第六轮 `docs/doc2md-第六轮审查报告-2026-09-08.md`（§2.4/§2.10/§3 已闭环；§2.2/§2.3/§2.6/§2.7/§2.9 未修）· 第七轮 `docs/doc2md-第七轮审查报告-2026-09-09.md`（§2.2/§2.4 已闭环；§2.1/§2.3 待做；§2.5/§2.6 已留档）
- 上游对照：`docs/upstream-markitdown-checklist.md`（格式支持对照）· `docs/spec-conformance-tests.md`（**格式规范符合性测试机制 + 对照表**；外部线索台账在本地 `.私档/`，不入公开仓库）· `docs/doc2md-vs-pdfinspector-对比报告-2026-09-14.md`（**PDF 三方输出对比**：doc2md / MarkItDown / pdf-inspector WASM，独立检出取证）
- **本轮复盘（本地私有）**：`.私档/项目/复盘/20260914-复盘决议-v0.1.3转正与v0.1.4范围.md`（v0.1.3 转正 + v0.1.4 范围与两批提交计划）· `20260914-复盘议程-v0.1.3观察期.md`（含 §7 T1–T4 结论）· `20260914-复盘材料-待修清单-合并三方.md`（主材料）
- 私人文档（`.私档/`，不进仓库）：沟通包、面谈准备、论文选题构想、软著 AI 声明速查、HANDOFF-商业化线（已从 docs/ 移入）

## 7. 团队状态

`doc2md-v012`（上一会话）：qa-dev / conv-dev / core-dev 三人，任务 t1-t16 全 completed。**§8.1 批未组队**（单会话自干）；**减脂批（9/10）组队 `doc2md-slim`**：conv-dev / core-dev / qa-dev 三人，t1-t12 全 completed（t6 独立验收通过；t7 权威跑由用户终端执行）。；**规范符合性 S4/S5 批（9/11）组队 `doc2md-s4s5`**：core-dev（S4-x/S5-x 断言定稿 + S4 实现）/ conv-dev（S5 实现）/ qa-dev（独立验收 t5），captain 负责产物构建与官方两相跑；t1/t3/t4 **completed**，t5 进行中。；**S4/S5 收口批（9/11-12）续跑同队并增补专职审查员 `reviewer`**（core-dev 接管 t10 后拒绝自审 t13，处置正确）；成员 `conv-dev` 因会话被挂死子进程吊住（40+ 分钟无进展）经 interrupt 无效后**移出花名册**，其未提交工作由 core-dev 复核后提交为 `bad086a`。

## 8. 下一批建议

**⭐ 第一批（2026-09-18 拍板；观察期 9/19 结束后启动；平台分工按 §3.5）**

1. ~~**backlog 条目 8 / B3「流式」**（**P1**）~~ ✅ **已完成（2026-09-19，卡 002；见 A11.27）** —— `3fc54a9`（流式化）+ `4da6ac4`（撤护栏 + A9/A10）；`npm test` 本机 **276/274/0/2**、组 Y 三绿（`3-big` 96.8 s → **367 ms**）；**Linux 独立验收单已开**（含负对照）、**手机侧取证单已开**。
   - **顺序约束（写死，不许反）**：① 先**流式化**（逐块 `reader` + 增量 `decode({stream:true})` + 跨块续接缓冲）→ ② **再**撤 / 降级 4 MB 护栏；**同一批，中间态不得单独上线** —— ✅ 已遵守（两提交，未推送）。
   - **阈值口径**：按**解压后规模**（ZIP 中央目录 offset+24；`zipEntryMeta()` 取 `uncompSize`）—— ✅ 已落地（护栏**撤除**，判据移交 A9 预检）。
   - **不引新依赖**：`fflate 0.7.5` 已内联；主路径用原生 `DecompressionStream` —— ✅ 无新依赖。
   - **验收口径**：峰值 **O(窗口) 而非 O(文件)**。修前基线：35.9 MB → **11.24 GB**（PC 独立核 **12.63 GiB / 369×**）· 47.4 MB → **11 秒冲到 5 GB 后被 SIGKILL** · **286.8 s 只换 1000 行**。**客观 RSS 实测 = Linux 验收单**（分工：Windows 出单 + 收件核验）。
   - 三处改动均在 `src/xlsx.js` ✅（工作表 XML / sharedStrings 逐块 + 增量 decode + 跨块续接缓冲；护栏撤除）。
2. ~~**R9-1「批量失败隔离」实现**（**P1**）~~ ✅ **已完成（2026-09-18，观察期内提前落地）** —— `0b2d1d5`（实现）· `ef2bbb7`（摘掉组 Z 三处 `{ todo: … }` 标记，**断言一字未改**）· `7b77998`（组 Y 实测三成立）；**Linux 独立验收 9/9 全 PASS**（组 Z 三红转绿 + 组壳消失 + `# todo 0` + 跨平台产物逐字节一致 + **负对照必红**；见 A11.15）；**已推送**。
   - 📌 **排期漂移登记（2026-09-19 调度线门卫核对发现）**：本条至 2026-09-19 仍被列为"待做"，成因 = 观察期内**用户指令提前落地**后**未回填排期栏**（非漏做）。⇒ 已立规则：观察期内的提前落地须**当日回填**。
3. ~~**A9/A10「预检 + 提示」**~~ ✅ **已完成（2026-09-19，卡 002；见 A11.27）** —— `4da6ac4`：A9 预检**只读 ZIP 中央目录**（按**解压后规模**，≥32 MB 触发）+ A10 解析前提示与**双让帧**；浏览器实证提示在 t=149 ms 画出、**不进产物**。
4. **B2「按需加载模型」机制（仅设计层）**：体积明示 / 显式同意 / 离线缓存+版本 / **硬内存上限 + 主动降级** / 逐页流式 / 失败优雅降级 —— **与 A13 的「OCR 语言按需 + 质量升级重试」共用同一套口径**（同一模式两处用）。
5. ~~**组 C 冷启动 flaky 阈值处置**~~ ✅ **已完成（2026-09-19，卡 002 · D 项；用户拍板 B′）** —— image 用例改为「冷启动只判防呆 ≤20 s（非性能判据）+ **预热后**按 `<500 ms` 主口径断言」；实测桌面 368/40 ms、手机视口 363/36 ms；口径登记见 `tests/CONTRACT.md` T-1 追加条 + C3 行。
6. ~~（**本批必做**）**Linux 独立验收单**~~ ✅ **已开单并已回箱（2026-09-19）**：见 A11.27「回箱」段（`.私档/linux/20260919-大文件流式化验收/`）；**手机侧取证单**同样已回箱（`.私档/验证/手机实测-20260919-大文件流式化/`）。


> 依据：§3（含 **A7 手机侧 backlog v11 复核结果**）+ 观察期约束（**v0.1.9 观察期至 2026-09-19**，期间按 §0.5 原则**不改产品行为**）。
> 标注含义：**【零风险】**= 可立刻做 · **【需拍板】**= 涉及口径/断言/依赖 · **【需量化】**= 先测量再动手。

**A. 观察期内（现在 → 09-19）**
1. ~~**S1 表格列数对齐补断言**（§3 A4.5 = A7-#4）~~ ✅ **已闭环（2026-09-18，`da910b1`；Linux 侧独立验收 4/4 绿 + 反向探针成立）**
   - **新增（并入本批，2026-09-18 用户拍板「随条目 8 那批顺手做」）**：**组 C `image（sample.png）` 冷启动阈值 flaky 处置**（P3）—— `10194 ms ≥ 5000 ms` 豁免窗口偶发超时（Linux 侧控制变量实验：基线全量绿 / 补丁全量 B1 红 B2 绿 ⇒ **flaky，与补丁无关**）。处置候选：**放宽 5000ms** 或**加一次预热**；**不改断言口径**（若改阈值 = 改口径 ⇒ 需重新拍板并同步 `CONTRACT.md` 的 T-1 拍板点）。选在本批做，是因为本批本来就要碰性能/门禁。
2. **去用**：真实文档多转 + 真机复查 + 把发现**登记**下来（观察期的第一优先不是从 backlog 挑，而是发现还不知道的）
3. **设计先定**（零成本）：U1 一键下载的交互/重名规则 · A7.3 内存三条的拍板点清单
4. **机制项【需拍板】**：重构配额自查 `tools/quota-check.mjs`（可选，只提醒不阻断）
5. **R9-6（第九轮 §4.1）【需拍板】**：`package.json` 加 `engines` + CI Node 版本与发布/验收运行时对齐（现 CI 20 / 本机 24）—— 纯配置，不动 `src/`
6. **CI 加固（2026-09-20 事故驱动）【需拍板】—— 待立卡材料**：`tests` run **`35458379084`**（提交 `0d4f039`）红在第 8 步 `Delivery-face dependency audit`：`registry advisories HTTP 503`，body 自述 **maintenance**（npm 官方 **Scheduled maintenance 2026-09-19 17:00–19:00 UTC**，该 run 在 17:32Z）；**同服务商 `/-/npm/v1/security/audits/quick` 此刻 200 且数据同源** ⇒ 是**端点级维护**，非整站/非我方网络。**后果**：其后 6 步（metrics / site smoke / **Contract tests** / TAP 对账 / PWA / OCR）**全部被跳过** —— 该 run **从未执行测试**。**与卡 005 代码无关**（硬证据：`git diff --stat 9965d08..HEAD -- tools/` 空 + `tools/audit-delivery.mjs` blob 前后同一份 `dbae1d16…` + 本机现在跑同一步同样 503）。详见 `docs/DEV-NOTES.md` **2026-09-20 事故节**。三条候选（**全部属 `tools/**` / `.github/**`，卡 005 范围外 ⇒ 本卡未动**）：
   1. **步序后移**【零风险·推荐】：把 `Delivery-face dependency audit` 挪到 `Contract tests` **之后** —— 判据一字不改，只保证**外部故障不再遮蔽测试信号**（本次真正的教训是"红的位置让人误判成自己改坏了"，而不是"审计太严"）。
   2. **退避重试**【需拍板】：503/超时按 2–3 次指数退避重试；**保留**「数据不可得 ⇒ 宁可红，不假绿」的立场（2026-09-16 用户拍板），只削瞬时抖动。
   3. **端点降级**【需拍板】：bulk 端点不可用时改用 `/-/npm/v1/security/audits/quick`（实测此刻 200 且返回 advisory 数据）；需先确认两端口径等价（findings 结构不同 ⇒ 要写映射 + 断言），且**降级本身要留痕**（否则等于把"数据不可得"悄悄变成"没问题"）。

**B. 复盘后第一批（09-19 起；高概率 × 低成本）**
5. **A7.1 预览截断提示挪到 textarea 外**（XS）**【需拍板：改契约组 Q 断言】**
6. **F.5 半角标点折叠缺口**（XS）**【需拍板：改 `CJK_PUNCT_CLASS` 口径 + 先红 R 组用例】**
7. **A7.4 OLE2 精确识别**（XS；已实测可行：前 64KB 流名，UTF-16LE 匹配）
8. ~~**U1 一键打包 zip 下载**~~ → ✅ **2026-09-16 用户拍板：排 v0.2**（见下方 **F**；不在复盘后第一批）
9. **G.3 坏编码显式暴露给用户**（S）
10. ~~**R9-1 批量失败隔离**~~ ✅ **已完成**（同 §8 第 1 条标记）—— 实现与原计划一致：`readAndSniff` 读文件/嗅探失败**不抛** + `processOne` **逐文件隔离** + 状态栏**成功/失败计数** + `safeHandleFiles` **两入口兜底**；**「须契约先红」已履行**（组 Z：Z1/Z2/Z3 红 + Z4 负对照绿，见 A8.4 / A11.10），实现后**红转绿**（A11.15）。

**C. 复盘后第二批（XS 打包一轮）**
11. **A.3 PDF 扩展 B 区 CJK 代理对** + **A.4 xlsx `<rPh>` 注音**（均 XS）→ 做完后**主线 PDF 文本通道可宣告无已知待修项**
12. **第九轮 B 批四条（均 XS-S；编号 R9-2 / R9-5 / R9-7 / R9-8）**：§3.1 `descr` 去扩展名收窄（只对 `name` + 已知图片扩展名）· §3.4 导出缺图**显式提示** · §4.2 SVG/EMF/WMF 扩展名映射 · §4.3 GIF 按**块结构**解析到 trailer（顺带把 backlog 那条一起做）
13. **待核项销账**：A7-#16（S5 三条 low 出处）/ #17（`&#x;` · `%` 半合并）——逐条定去留（不虚报为已闭环）
14. **待夹具确认（R9-3 / R9-4）**：第九轮 §3.2（alt 顺序一致性检查）· §3.3（多 BT / 仅首块 Tm 的真实 PDF 触发面）

**D. 复盘后独立批（架构/性能，按量级排；共同前置 = 先量峰值内存）**
15. **A7.3 内存三条**：量基线（0.5d）→ **#9** Worker + transferable（1–2d）**【需拍板：是否引入 comlink】→ 收益最直观（18s 卡死 → 不卡）** → **#8** zip 流式（1–2d，零新依赖；须字节级回归，契约组 I/T 会拦）→ **#10** OCR worker 回收（0.5d）**【需拍板：阈值】**
16. **A7.2 竖排盲区**（P2 / S-M）**【需拍板：三方案择一】**
17. **A7.5 zip 进**（M）**【需设计：解包护栏（条目数/总大小/路径穿越）+ 进度 + 上限】**

**E. 不进本线**
18. **PDF 版面结构还原**（独立线，已立项、按用户拍板**不启动**）——含 PDF 表格、多栏混排、段落边界、标题识别、低质页保图
19. **APK 线 A1–A6**（见 A7.6）——**不是 Web 版欠债**，是 APK 线的需求清单
20. **毕设线**（视觉构想 v2 + YOLO26 环境清单）待用户开口

**F. v0.2 已拍板 / 已登记（不在观察期与本批内）**
21. **U1 一键打包 zip 下载**（S）—— ✅ 用户 **2026-09-16 拍板排 v0.2**（§3 A5.7 / A7-#18）
22. **T2 1904 只补 warning** + **T4 `%` 收紧版（`+` 不纳入）**（§3 A5.2；已定「仍排 v0.2」）
23. **formatCode 含字母误判日期**（报告建议观察半年，§3 A2.2）· **公开语料基准**（对外要数据时做，§3 A5.6）· **测试脚本重复抽取**（要压才做，§3 E.3）
24. **PDF 段落边界 / 标题识别 H1–H4**（原 v0.2 候选）→ 已随划界**移入独立线**（不启动；见 §3 A5.4/A5.5）

**G. 大文件体验（A9 + A10；复盘后第一批，**同一批做**）**
25. **A9 候选修法①「预检 + 知情同意」+ A10 L1「启动前提示 + 双让帧」**（合计 **XS**）—— 解析前只读 ZIP 中央目录估规模 → **先把提示条画出来**（「文件较大（约 N MB），预计约 M 秒，页面会暂时无响应，请勿关闭」）→ 再开始解析。**【需拍板：阈值按文件字节还是解压后规模；文案与耗时档位算法（实测数据点可作档位外推）】**
26. **A10 L2/L3（真转圈 / 进度条 / 取消）** → **依赖 A7.3-#9（Worker）与 #8（流式）**，随那条线一起做；**关键约束**：主线程冻死期间动画也冻死（实测心跳 0/584）⇒ 没有 Worker 就没有真进度，**没有 Worker 时「取消」按钮是假的**（只能提示刷新页面）。

---

## 9. Linux 侧工作区现状（2026-09-14 实测落盘）

> 本机：WSL2 Ubuntu-24.04 · 工作区 `~/dsh-workspace/doc2md`（与 Windows 侧**同一仓库**的另一检出）。
> 本节只记**仓库级事实**（tag / 远端 / 产物）+ **本检出的环境事实**；随下次 Linux 侧会话更新。

| 项 | 实测值 |
|---|---|
| 检出方式 | **浅克隆**（`.git/shallow` 存在，depth=1）→ 2026-09-14 已 `fetch --unshallow --tags` **恢复完整历史（228 提交）** |
| 本地 HEAD | **与 `origin/main` 完全同步、工作区干净**（快照值 `936518b`，2026-09-15；随远端前进会变） |
| tag（4 个，均 lightweight） | `v0.1.0`=`1636027` · `v0.1.1`=`3265b0c` · `v0.1.2`=`f5aed38` · **`v0.1.3`=`d6929e6`** |
| 产物 | `index.html` **113,561 B** / SHA256 前缀 `22CB96C2…` —— 与第八轮产物记录**一致** ✅ |
| 依赖 | `node_modules` 就绪（eslint / jscpd / prettier / playwright / tsc） |
| 本机环境文件 | `.env.sh`（免 root 方案：假 HOME + 解包系统库 + 全局 Playwright 缓存）——**已 gitignore** |

**2026-09-14 三处修正（均由实测驱动，非推断）**：

1. **tag SHA 勘误** —— `v0.1.3` 实为 `d6929e6`（原记 `5683b05` 有误；详见文首勘误块）。
2. **`.gitignore` 补 6 条** —— `.env.sh` / `.env.sh.bak-*` / `.home/` / `.debs/` / `.venv-markitdown/` / `_cmp/`。修前逐项 `check-ignore` **均为 TRACKABLE**，一次 `git add -A` 会推入约 **515MB**（其中 `.env.sh` 含本机绝对路径），违反全局红线 #11；修后逐项 **`IGNORED`**、`git status` 仅剩预期改动。
3. **`.env.sh` Playwright 路径修复** —— 原值 `$D2M_ROOT/.pw-browsers` 是自 `trial/doc2md` 复制来的**空壳**（仅剩 `.links`），实测 `chromium.executablePath()` → **`exists = false`**；改为 `${HOME}/.cache/ms-playwright`（真实全局缓存；`$HOME` 在 source 时展开 = 真实 HOME）后实测 **`exists = true`**。

**隐私 / 许可复查（发布前强制审查，2026-09-14）**：

- 跟踪文件全量 grep：**零真实命中** —— 命中项均为 `actions/checkout@v4` 类 `@` 误报、noreply 提交身份（合规）、二进制压缩噪声。
- `assets/screenshot.png`（50,935 B）经**读图人工核验**：为脱敏演示界面（「项目季度报告（样例）」「关键令牌：DOC2MD-DOCX-OK-2026」），**无本机路径 / 用户名 / 邮箱**；PNG 仅含 `IHDR`/`IDAT`，无 `tEXt` 元数据块。
- 提交身份：`sakuraqqq <sakuraqqq@users.noreply.github.com>` ✅ noreply 合规。
- **结论：通过**（本次仅本地改动，未发布任何内容）。

**2026-09-14 收口：分叉已消除** ✅

本检出一度与远端分叉（共同祖先 `f6c84b2`：本地 = `.gitignore` + 本文件改动，远端 = `f7caa21`）。收口走了三步：

1. 本地改动导出为**单提交 patch**，在真实基线 `f7caa21` 上**预演 `git am` 通过**（零冲突；应用后两个文件与本地版本**逐字节一致**）；
2. patch 交付至 `.私档/linux/`，由 Windows 侧应用并 push —— 落库提交 `45b2bec`，并由其把散落在 `docs/DEV-NOTES.md` / `docs/RELEASE.md` / 第八轮审查报告里的**同一处 tag 勘误一并同步**（本侧 patch 只覆盖了 HANDOFF）；
3. 本侧 `git fetch && git reset --hard origin/main` → **分叉消除**（HEAD = `origin/main`，工作区干净）。

**Linux 侧定位（策略，2026-09-14 用户拍板）**：**只读不推** —— 产出与交接一律落 `.私档/linux/`（见该目录 `_约定.md`），push 统一由 Windows 侧执行。

> ⚠️ 两个独立的坑，别混：**证书**（Steam++ TLS 中间人 → 本机 git 网络命令一律带 `-c http.sslVerify=false`，见 §5）；**认证**（本沙箱为**非交互**环境，`push --dry-run` 实测报 `fatal: could not read Username for 'https://github.com'`）→ 后者决定了 **push 只能在用户终端执行**。

**遗留（未处理，待拍板）**：

- `.pw-browsers/`（12 KB，已 gitignore）为自 `trial/doc2md` 复制来的废弃残留（真浏览器已在 `~/.cache/ms-playwright`），**可删**。

**2026-09-15 收尾（时点值，非当前基线）**：v0.1.7 已发布、已推送（tag `v0.1.7` = `9d2cd9d`）· 工作区干净。**此后 main 仍在前进**（当日又落 §10 等文档提交）→ 当前基线请现场实测，勿引用本行。

## 10. 与 Linux 侧（WSL2）协作约定（2026-09-15 固化）

**投递箱** = `.私档/linux/`（gitignore，永不入库）。流程：**Windows 出单 → Linux 只读取证 / 独立验收 → 产出回箱 → Windows 实测核验并落库**；**Linux 只读不推** —— 任何 push / tag / Release 一律由用户从桌面终端执行（与红线 3 同源）。

| 目录 | 内容 |
|---|---|
| `任务-<主题>/` | **交接单**（Windows→Linux）+ 开单通知 + 补丁（`git format-patch`）+ **补件** |
| `<YYYYMMDD>-<批次>/` | Linux 侧产出：报告 + `summary.json` + 原始日志 + `回执.md` + 脚本 |

**交接单必备三件（缺一不可）**：**期望值 + 判据（探针词 / 归一化规则）+ 预期例外**。
> 2026-09-15 教训：只给了「探针命中 5/5 · 5/5 · 6/6 · 5/6」却没给**探针词与匹配口径** → 验证方无法复现，只能登记未验证项 U1；后出**补件**（探针词表 + 写死的去空白子串口径 + 预期未命中说明 + 隐私口径澄清）才关闭。**期望值必须与判据成对给出。**

**Linux 侧纪律（写进每份交接单）**：① **只读不推**（不改受控文件、不 commit/push/tag；**不跑 `npm run build`**、不装依赖，除非交接单**显式豁免**）② **只报告不修改**（发现疑似缺陷 → 另开小节 + 最小复现）③ **换环境 + 换数据 + 负对照必红** ④ 回执须含**每文件字节 + SHA256**（Windows 侧逐个实测核验，不符打回）⑤ 🔒 **隐私**：用户真机材料只在 `.私档/` 内只读，**不得复制进仓库、不得出现在报告截图或正文引用**（报告只写文件名与数字）。

**跨机比对口径**：**源码用 git blob**（`git rev-parse :<path>`，**不用**工作区 SHA256 —— Windows `core.autocrlf` 会使同内容哈希不同）；**产物用字节 + SHA256**。

**为什么交给 Linux 侧**：真 playwright chromium（Windows 本机缺，只能回退 msedge）· CI 等价（两个 workflow 均 `ubuntu-latest`）· **真 OCR 端到端**（契约组 W/X 只有假 worker 的确定性断言）· bash + `git grep`/`rg` 一行流（全历史隐私扫描 / 体积分析 / 跨平台一致性）。

**已闭环记录（可作模板）**：`任务-跨平台产物一致性/` → `20260915-跨平台一致性/`（产物逐字节相同 + 门禁逐项一致；挖出 CRLF 字节陷阱）· `任务-v0.1.5独立验收/` → `20260915-v0.1.5验收/`（真 OCR + 负对照）· `任务-v0.1.7独立验收/` → `20260915-v0.1.7验收/`（六项全绿 + 补件关闭 U1；`核验回执-Windows侧.md` 为回执样板）。

**2026-09-15 Linux 侧全量复跑（跨平台一致性验证；任务单与产出在 `.私档/linux/`）**：

| 项 | 实测值 |
|---|---|
| 环境 | WSL2 Ubuntu 24.04.5 · Node **v24.21.0** · npm 11.19.0 · **playwright 1.62.1 + 真 chromium**（`chromium-1234`，`exists=true`） |
| 取码 | `b4626f6`（detached；**先 `git status` 确认干净再 checkout**，未用 `reset --hard`；实测 fetch 时 `origin/main` 已前进到 `936518b`，如实登记为偏差） |
| 构建 | `npm run build` → `index.html` **118,244 B** / `62B8ADA1…786D` = 入库产物（构建后 `git status` **0 行** → 幂等、无需还原） |
| 契约 | **232 tests / 230 pass / 0 fail / 2 skip**（与 Windows 基线逐项一致，原始日志第 286–293 行） |
| 其余门禁 | lint 0 warning · metrics 17 文件 / 349 函数 / 超限 0 / 0.5% · pwa-audit 48/48 · verify:ocr PASS(93) |
| 终态 | `git status --porcelain` **0 行**；**未 commit / 未 push**（只读不推纪律） |

**结论：产物平台无关 + 门禁环境无关。** Windows 侧已逐文件实测核验（**15/15 字节 + SHA256 吻合**）并复核结论；四重证据与三条附带发现见 `docs/DEV-NOTES.md` 2026-09-15 节。

**未验证项（Linux 侧主动登记 7 条 → Windows 侧处置）**：**U5**（Windows 侧 CRLF 工作区能否产出同字节产物）**已由 Windows 侧关闭**（重建结果逐字节相同）；**U7** 部分关闭（构建确定性现有 4 条跨平台证据，门禁仍单次）；**U1** 日志时间戳跳变根因 / **U2** metrics 差异未穷尽 / **U3** 未跑 `test:contract` / **U4** 未验其他发行版 / **U6** 未验 `install-scripts approve` 后行为 —— **保留登记，不编根因**。
